(globalThis["webpackChunk"] = globalThis["webpackChunk"] || []).push([["main"],{

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/11x12Chars.json"
/*!*******************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/11x12Chars.json ***!
  \*******************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/11x12Chars.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/7x9Chars.json"
/*!*****************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/7x9Chars.json ***!
  \*****************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/7x9Chars.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/8x11Chars.json"
/*!******************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/8x11Chars.json ***!
  \******************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/8x11Chars.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat10pt.json"
/*!*****************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat10pt.json ***!
  \*****************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/chat10pt.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat12pt.json"
/*!*****************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat12pt.json ***!
  \*****************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/chat12pt.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat14pt.json"
/*!*****************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat14pt.json ***!
  \*****************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/chat14pt.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat16pt.json"
/*!*****************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat16pt.json ***!
  \*****************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/chat16pt.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat18pt.json"
/*!*****************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat18pt.json ***!
  \*****************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/chat18pt.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat20pt.json"
/*!*****************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat20pt.json ***!
  \*****************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/chat20pt.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat22pt.json"
/*!*****************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat22pt.json ***!
  \*****************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/chat22pt.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/fonthash.batch.json"
/*!***********************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/fonthash.batch.json ***!
  \***********************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/fonthash.batch.json");

/***/ },

/***/ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/spritehash.batch.json"
/*!*************************************************************************************************************!*\
  !*** ../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/spritehash.batch.json ***!
  \*************************************************************************************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (__webpack_require__.p + "data/spritehash.batch.json");

/***/ },

/***/ "./Entrance/index.tsx"
/*!****************************!*\
  !*** ./Entrance/index.tsx ***!
  \****************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react/jsx-runtime */ "../node_modules/react/jsx-runtime.js");
/* harmony import */ var _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../util/patchrs_napi */ "./util/patchrs_napi.ts");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react */ "../node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_2__);
/* harmony import */ var react_dom_client__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! react-dom/client */ "../node_modules/react-dom/client.js");
/* harmony import */ var _reflect2d_reflect2d__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../reflect2d/reflect2d */ "./reflect2d/reflect2d.ts");
/* harmony import */ var _reflect2d_spritecache__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../reflect2d/spritecache */ "./reflect2d/spritecache.ts");
/* harmony import */ var _reader_xpReader__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../reader/xpReader */ "./reader/xpReader.ts");
/* harmony import */ var _overlay__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../overlay */ "./overlay/index.ts");
/* harmony import */ var _overlay_OverlayPositionEditor__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../overlay/OverlayPositionEditor */ "./overlay/OverlayPositionEditor.tsx");
/* harmony import */ var _util_renderStream__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../util/renderStream */ "./util/renderStream.ts");
/* harmony import */ var _overlay_DebugZoneOverlay__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ../overlay/DebugZoneOverlay */ "./overlay/DebugZoneOverlay.ts");

// src/Entrance/index.tsx










// RS3 tick = 0.6 seconds
const TICK_MS = 600;
// Rolling window for XP rate calculation (5 minutes)
const ROLLING_WINDOW_MS = 5 * 60 * 1000;
// Max drops to keep in history
const MAX_DROP_HISTORY = 100;
// Time bucket duration for stacked bar chart (1 minute)
const BUCKET_DURATION_MS = 60 * 1000;
// ============================================================================
// API SETUP
// ============================================================================
function getApi() {
    _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_1__.hookFirstClient();
    return _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_1__.native;
}
const Alt1ApiContext = react__WEBPACK_IMPORTED_MODULE_2___default().createContext(null);
// ============================================================================
// UTILITY FUNCTIONS
// ============================================================================
function formatXp(xp) {
    if (xp >= 1000000)
        return (xp / 1000000).toFixed(2) + "M";
    if (xp >= 1000)
        return (xp / 1000).toFixed(1) + "K";
    return xp % 1 === 0 ? xp.toFixed(0) : xp.toFixed(1);
}
function formatXpInt(xp) {
    if (xp >= 1000000)
        return (xp / 1000000).toFixed(2) + "M";
    if (xp >= 1000)
        return (xp / 1000).toFixed(1) + "K";
    return xp.toFixed(0);
}
function calcXpPerHour(xp, elapsedMs) {
    if (elapsedMs <= 0)
        return 0;
    return (xp / elapsedMs) * 3600000;
}
function formatTime(ms) {
    const h = Math.floor(ms / 3600000);
    const m = Math.floor((ms % 3600000) / 60000);
    const s = Math.floor((ms % 60000) / 1000);
    return `${h}h ${m}m ${s}s`;
}
function formatTimeShort(ms) {
    const s = Math.floor(ms / 1000);
    if (s < 60)
        return `${s}s ago`;
    const m = Math.floor(s / 60);
    if (m < 60)
        return `${m}m ago`;
    return `${Math.floor(m / 60)}h ago`;
}
function getDropTypeLabel(dropType) {
    switch (dropType) {
        case _reader_xpReader__WEBPACK_IMPORTED_MODULE_6__.XpDropType.SINGLE_SKILL: return "Single";
        case _reader_xpReader__WEBPACK_IMPORTED_MODULE_6__.XpDropType.COMBAT_SPLIT: return "Combat";
        case _reader_xpReader__WEBPACK_IMPORTED_MODULE_6__.XpDropType.CELEBRATION_LAMP: return "Lamp";
        case _reader_xpReader__WEBPACK_IMPORTED_MODULE_6__.XpDropType.ALL_SKILLS_LAMP: return "All Skills";
        case _reader_xpReader__WEBPACK_IMPORTED_MODULE_6__.XpDropType.UNKNOWN_MULTI: return "Multi";
        default: return "Unknown";
    }
}
// Get device pixel ratio for crisp rendering
function getDPR() {
    return window.devicePixelRatio || 1;
}
// Setup canvas for high DPI rendering
function setupHiDPICanvas(canvas, ctx, width, height) {
    const dpr = getDPR();
    canvas.width = Math.round(width * dpr);
    canvas.height = Math.round(height * dpr);
    canvas.style.width = `${width}px`;
    canvas.style.height = `${height}px`;
    ctx.scale(dpr, dpr);
    return dpr;
}
// Line chart for XP over time
function LineChart({ data, width = 280, height = 80, color = "#00ff88", label, showRate = false, }) {
    const canvasRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(null);
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        const canvas = canvasRef.current;
        if (!canvas || data.length < 2)
            return;
        const ctx = canvas.getContext("2d");
        if (!ctx)
            return;
        // Setup for crisp high-DPI rendering
        ctx.setTransform(1, 0, 0, 1, 0, 0); // Reset transform
        setupHiDPICanvas(canvas, ctx, width, height);
        // Clear
        ctx.fillStyle = "#0a0a1a";
        ctx.fillRect(0, 0, width, height);
        // Get min/max values
        const values = data.map((d) => d.value);
        const minVal = showRate ? 0 : Math.min(...values);
        const maxVal = Math.max(...values);
        const range = maxVal - minVal || 1;
        // Draw grid lines
        ctx.strokeStyle = "#1a1a3a";
        ctx.lineWidth = 1;
        for (let i = 0; i <= 4; i++) {
            const y = (height / 4) * i;
            ctx.beginPath();
            ctx.moveTo(0, y);
            ctx.lineTo(width, y);
            ctx.stroke();
        }
        // Draw line
        ctx.strokeStyle = color;
        ctx.lineWidth = 2;
        ctx.beginPath();
        const padding = 5;
        const chartWidth = width - padding * 2;
        const chartHeight = height - padding * 2;
        data.forEach((point, i) => {
            const x = padding + (i / (data.length - 1)) * chartWidth;
            const y = padding + chartHeight - ((point.value - minVal) / range) * chartHeight;
            if (i === 0)
                ctx.moveTo(x, y);
            else
                ctx.lineTo(x, y);
        });
        ctx.stroke();
        // Fill area under curve
        ctx.lineTo(padding + chartWidth, padding + chartHeight);
        ctx.lineTo(padding, padding + chartHeight);
        ctx.closePath();
        ctx.fillStyle = color + "20";
        ctx.fill();
        // Draw label
        if (label) {
            ctx.fillStyle = "#888";
            ctx.font = "11px 'Segoe UI', Arial, sans-serif";
            ctx.fillText(label, 5, 13);
        }
        // Draw current value
        const current = data[data.length - 1]?.value ?? 0;
        ctx.fillStyle = color;
        ctx.font = "bold 13px 'Segoe UI', Arial, sans-serif";
        ctx.textAlign = "right";
        ctx.fillText(showRate ? formatXpInt(current) + "/hr" : formatXp(current), width - 5, 13);
    }, [data, width, height, color, label, showRate]);
    return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("canvas", { ref: canvasRef, style: { borderRadius: "4px", display: "block", width, height } }));
}
// Clean horizontal bar chart for skill comparison (like RuneScape leaderboards)
function SkillBarChart({ skills, width = 300, height = 300, onColorChange, }) {
    const rowHeight = 32;
    if (skills.length === 0) {
        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: {
                width,
                height: 60,
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                color: "#444",
                fontSize: "12px",
            }, children: "No skill data yet" }));
    }
    const maxXp = Math.max(...skills.map(s => s.xp));
    const handleColorChange = (skill, color) => {
        saveSkillColor(skill, color);
        onColorChange?.(skill, color);
    };
    return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { width, maxHeight: height, overflowY: "auto" }, children: skills.map((skill, i) => {
            const barPercent = maxXp > 0 ? (skill.xp / maxXp) * 100 : 0;
            return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: {
                    display: "flex",
                    alignItems: "center",
                    height: rowHeight,
                    padding: "0 4px",
                    backgroundColor: i % 2 === 0 ? "transparent" : "rgba(255,255,255,0.02)",
                    borderRadius: "4px",
                }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: {
                            width: "20px",
                            color: "#555",
                            fontSize: "11px",
                            textAlign: "center",
                        }, children: i + 1 }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(SkillColorPicker, { skill: skill.name, color: skill.color, onColorChange: handleColorChange }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: {
                            width: "80px",
                            color: "#ccc",
                            fontSize: "13px",
                            fontWeight: 500,
                        }, children: skill.name }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: {
                            flex: 1,
                            height: "8px",
                            backgroundColor: "#1a1a2e",
                            borderRadius: "4px",
                            overflow: "hidden",
                            marginRight: "10px",
                        }, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: {
                                width: `${barPercent}%`,
                                height: "100%",
                                backgroundColor: skill.color,
                                borderRadius: "4px",
                                transition: "width 0.3s ease",
                            } }) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: {
                            width: "70px",
                            color: "#00ff88",
                            fontSize: "13px",
                            fontWeight: "bold",
                            textAlign: "right",
                        }, children: formatXpInt(skill.xp) })] }, skill.name));
        }) }));
}
// Backwards compat alias
const MiniChart = LineChart;
function StackedBarChart({ buckets, skills, width = 320, height = 200, maxBuckets = 10, }) {
    const canvasRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(null);
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        const canvas = canvasRef.current;
        if (!canvas)
            return;
        const ctx = canvas.getContext("2d");
        if (!ctx)
            return;
        // Setup for crisp high-DPI rendering
        ctx.setTransform(1, 0, 0, 1, 0, 0);
        setupHiDPICanvas(canvas, ctx, width, height);
        // Chart dimensions
        const leftPadding = 50;
        const rightPadding = 10;
        const topPadding = 30; // Space for legend
        const bottomPadding = 25;
        const chartWidth = width - leftPadding - rightPadding;
        const chartHeight = height - topPadding - bottomPadding;
        // Clear canvas
        ctx.fillStyle = "#0a0a1a";
        ctx.fillRect(0, 0, width, height);
        // Get visible buckets (last N)
        const visibleBuckets = buckets.slice(-maxBuckets);
        if (visibleBuckets.length === 0) {
            // No data yet message
            ctx.fillStyle = "#444";
            ctx.font = "12px 'Segoe UI', Arial, sans-serif";
            ctx.textAlign = "center";
            ctx.fillText("Waiting for XP data...", width / 2, height / 2);
            return;
        }
        // Find max total for Y-axis scale
        const maxTotal = Math.max(...visibleBuckets.map(b => b.total), 1000);
        // Round up to nice number for Y-axis
        const niceMax = (() => {
            if (maxTotal >= 1000000)
                return Math.ceil(maxTotal / 1000000) * 1000000;
            if (maxTotal >= 100000)
                return Math.ceil(maxTotal / 100000) * 100000;
            if (maxTotal >= 10000)
                return Math.ceil(maxTotal / 10000) * 10000;
            if (maxTotal >= 1000)
                return Math.ceil(maxTotal / 1000) * 1000;
            return Math.ceil(maxTotal / 100) * 100;
        })();
        // Draw Y-axis grid lines and labels
        const gridLines = 5;
        ctx.strokeStyle = "#1a1a3a";
        ctx.lineWidth = 1;
        ctx.font = "10px 'Segoe UI', Arial, sans-serif";
        ctx.fillStyle = "#666";
        ctx.textAlign = "right";
        for (let i = 0; i <= gridLines; i++) {
            const y = topPadding + (chartHeight / gridLines) * i;
            const value = niceMax - (niceMax / gridLines) * i;
            // Grid line
            ctx.beginPath();
            ctx.moveTo(leftPadding, y);
            ctx.lineTo(width - rightPadding, y);
            ctx.stroke();
            // Y-axis label
            ctx.fillText(formatXpInt(value), leftPadding - 5, y + 4);
        }
        // Calculate bar dimensions
        const barCount = Math.max(visibleBuckets.length, 1);
        const barGap = 4;
        const barWidth = (chartWidth - barGap * (barCount + 1)) / barCount;
        // Draw bars
        visibleBuckets.forEach((bucket, barIndex) => {
            const barX = leftPadding + barGap + (barWidth + barGap) * barIndex;
            let stackY = topPadding + chartHeight; // Start from bottom
            // Draw stacked segments for each skill (in reverse order so first skill is at bottom)
            const orderedSkills = [...skills].reverse();
            for (const skill of orderedSkills) {
                const xp = bucket.skills[skill.name] || 0;
                if (xp <= 0)
                    continue;
                const segmentHeight = (xp / niceMax) * chartHeight;
                stackY -= segmentHeight;
                // Draw segment
                ctx.fillStyle = skill.color;
                ctx.fillRect(barX, stackY, barWidth, segmentHeight);
                // Add subtle border between segments
                ctx.strokeStyle = "rgba(0,0,0,0.3)";
                ctx.lineWidth = 0.5;
                ctx.strokeRect(barX, stackY, barWidth, segmentHeight);
            }
            // X-axis label (time in minutes)
            ctx.fillStyle = "#888";
            ctx.font = "10px 'Segoe UI', Arial, sans-serif";
            ctx.textAlign = "center";
            // Format time as M:SS (e.g., "0:00", "1:00", "2:30")
            const totalSec = Math.floor(bucket.startTime / 1000);
            const min = Math.floor(totalSec / 60);
            const sec = totalSec % 60;
            const timeLabel = `${min}:${sec.toString().padStart(2, '0')}`;
            ctx.fillText(timeLabel, barX + barWidth / 2, topPadding + chartHeight + 15);
        });
        // Draw legend at top
        const legendY = 12;
        let legendX = leftPadding;
        const boxSize = 10;
        const spacing = 8;
        ctx.font = "10px 'Segoe UI', Arial, sans-serif";
        for (const skill of skills.slice(0, 6)) { // Max 6 skills in legend
            // Color box
            ctx.fillStyle = skill.color;
            ctx.fillRect(legendX, legendY - boxSize + 2, boxSize, boxSize);
            // Label
            ctx.fillStyle = "#aaa";
            ctx.textAlign = "left";
            const labelWidth = ctx.measureText(skill.name).width;
            ctx.fillText(skill.name, legendX + boxSize + 3, legendY);
            legendX += boxSize + 3 + labelWidth + spacing;
            if (legendX > width - 50)
                break; // Stop if running out of space
        }
        // Y-axis label
        ctx.save();
        ctx.fillStyle = "#666";
        ctx.font = "10px 'Segoe UI', Arial, sans-serif";
        ctx.translate(12, topPadding + chartHeight / 2);
        ctx.rotate(-Math.PI / 2);
        ctx.textAlign = "center";
        ctx.fillText("XP gained", 0, 0);
        ctx.restore();
    }, [buckets, skills, width, height, maxBuckets]);
    return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("canvas", { ref: canvasRef, style: { borderRadius: "4px", display: "block", width, height } }));
}
function StackedAreaChart({ series, width = 320, height = 180, showLegend = true, maxDataPoints = 50, // Limit data points to prevent memory issues
 }) {
    const canvasRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(null);
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        const canvas = canvasRef.current;
        if (!canvas || series.length === 0)
            return;
        const ctx = canvas.getContext("2d");
        if (!ctx)
            return;
        // Setup for crisp high-DPI rendering
        ctx.setTransform(1, 0, 0, 1, 0, 0);
        setupHiDPICanvas(canvas, ctx, width, height);
        // Chart dimensions - adjusted for larger chart sizes
        const leftPadding = 50;
        const rightPadding = 15;
        const topPadding = 12;
        const bottomPadding = 28;
        const legendHeight = showLegend ? 28 : 0;
        const chartWidth = width - leftPadding - rightPadding;
        const chartHeight = height - topPadding - bottomPadding - legendHeight;
        // Clear canvas with dark background
        ctx.fillStyle = "#1a1a2e";
        ctx.fillRect(0, 0, width, height);
        // Get only the last N data points from each series to limit memory
        const limitedSeries = series.map(s => ({
            ...s,
            data: s.data.slice(-maxDataPoints)
        }));
        // Find all unique time points
        const allTimes = new Set();
        limitedSeries.forEach(s => s.data.forEach(d => allTimes.add(d.time)));
        let sortedTimes = Array.from(allTimes).sort((a, b) => a - b);
        // Further limit if still too many points
        if (sortedTimes.length > maxDataPoints) {
            const step = Math.ceil(sortedTimes.length / maxDataPoints);
            sortedTimes = sortedTimes.filter((_, i) => i % step === 0 || i === sortedTimes.length - 1);
        }
        if (sortedTimes.length < 2)
            return;
        // Find max value across all series (not stacked - individual max)
        let maxValue = 1000;
        for (const s of limitedSeries) {
            for (const d of s.data) {
                if (d.value > maxValue)
                    maxValue = d.value;
            }
        }
        // Round up to nice number
        const magnitude = Math.pow(10, Math.floor(Math.log10(maxValue)));
        maxValue = Math.ceil(maxValue / magnitude) * magnitude;
        const minTime = sortedTimes[0];
        const maxTime = sortedTimes[sortedTimes.length - 1];
        const timeRange = maxTime - minTime || 1;
        // Draw horizontal grid lines
        ctx.strokeStyle = "#2a2a4a";
        ctx.lineWidth = 1;
        const gridLines = 5;
        for (let i = 0; i <= gridLines; i++) {
            const y = topPadding + (chartHeight / gridLines) * i;
            ctx.beginPath();
            ctx.moveTo(leftPadding, y);
            ctx.lineTo(width - rightPadding, y);
            ctx.stroke();
            // Y-axis labels
            const value = maxValue - (maxValue / gridLines) * i;
            ctx.fillStyle = "#888";
            ctx.font = "10px 'Segoe UI', Arial, sans-serif";
            ctx.textAlign = "right";
            ctx.fillText(formatXpInt(value), leftPadding - 5, y + 4);
        }
        // Draw each series as a line with dots (not stacked)
        for (let seriesIdx = 0; seriesIdx < limitedSeries.length; seriesIdx++) {
            const s = limitedSeries[seriesIdx];
            if (s.data.length < 2)
                continue;
            const color = s.color;
            const points = [];
            // Build array of points for this series
            for (const d of s.data) {
                const x = leftPadding + ((d.time - minTime) / timeRange) * chartWidth;
                const y = topPadding + chartHeight - (d.value / maxValue) * chartHeight;
                points.push({ x, y });
            }
            // Draw the line
            ctx.beginPath();
            ctx.strokeStyle = color;
            ctx.lineWidth = 2;
            ctx.lineJoin = "round";
            ctx.lineCap = "round";
            for (let i = 0; i < points.length; i++) {
                if (i === 0) {
                    ctx.moveTo(points[i].x, points[i].y);
                }
                else {
                    ctx.lineTo(points[i].x, points[i].y);
                }
            }
            ctx.stroke();
            // Draw dots at each data point
            for (const pt of points) {
                // Outer dot (filled circle)
                ctx.beginPath();
                ctx.fillStyle = color;
                ctx.arc(pt.x, pt.y, 4, 0, Math.PI * 2);
                ctx.fill();
                // Inner dot (darker center for depth)
                ctx.beginPath();
                ctx.fillStyle = "#1a1a2e";
                ctx.arc(pt.x, pt.y, 2, 0, Math.PI * 2);
                ctx.fill();
            }
        }
        // Draw X-axis time labels - evenly spaced based on chart width
        ctx.fillStyle = "#888";
        ctx.font = "10px 'Segoe UI', Arial, sans-serif";
        ctx.textAlign = "center";
        // Calculate optimal number of labels based on chart width (min ~50px between labels)
        const numLabels = Math.min(Math.max(3, Math.floor(chartWidth / 55)), sortedTimes.length);
        for (let i = 0; i < numLabels; i++) {
            const idx = Math.floor((i / (numLabels - 1)) * (sortedTimes.length - 1));
            const time = sortedTimes[idx];
            const x = leftPadding + ((time - minTime) / timeRange) * chartWidth;
            const y = topPadding + chartHeight + 15;
            // Format time as minutes:seconds if over 1 minute, otherwise just seconds
            const totalSec = Math.floor(time / 1000);
            const min = Math.floor(totalSec / 60);
            const sec = totalSec % 60;
            const label = min > 0 ? `${min}:${sec.toString().padStart(2, '0')}` : `${sec}s`;
            ctx.fillText(label, x, y);
        }
        // Draw legend
        if (showLegend && series.length > 0) {
            const legendY = height - legendHeight + 15;
            let legendX = leftPadding;
            const boxSize = 10;
            const spacing = 12;
            ctx.font = "10px 'Segoe UI', Arial, sans-serif";
            for (const s of series) {
                // Color box
                ctx.fillStyle = s.color;
                ctx.fillRect(legendX, legendY - boxSize, boxSize, boxSize);
                // Label
                ctx.fillStyle = "#aaa";
                ctx.textAlign = "left";
                const labelWidth = ctx.measureText(s.name).width;
                ctx.fillText(s.name, legendX + boxSize + 4, legendY - 1);
                legendX += boxSize + 4 + labelWidth + spacing;
            }
        }
        // Y-axis label
        ctx.save();
        ctx.fillStyle = "#666";
        ctx.font = "10px 'Segoe UI', Arial, sans-serif";
        ctx.translate(12, topPadding + chartHeight / 2);
        ctx.rotate(-Math.PI / 2);
        ctx.textAlign = "center";
        ctx.fillText("XP/h", 0, 0);
        ctx.restore();
    }, [series, width, height, showLegend, maxDataPoints]);
    if (series.length === 0 || series.every(s => s.data.length < 2)) {
        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: {
                width,
                height: 80,
                display: "flex",
                alignItems: "center",
                justifyContent: "center",
                color: "#444",
                fontSize: "12px",
                backgroundColor: "#0a0a1a",
                borderRadius: "4px",
            }, children: "Not enough data yet" }));
    }
    return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("canvas", { ref: canvasRef, style: { borderRadius: "4px", display: "block", width, height } }));
}
// ============================================================================
// STYLES
// ============================================================================
const styles = {
    container: {
        backgroundColor: "#0d0d1a",
        color: "#eaeaea",
        padding: "12px",
        fontFamily: "'Segoe UI', Tahoma, sans-serif",
        minHeight: "100vh",
        minWidth: "400px",
    },
    header: {
        color: "#00d4ff",
        marginBottom: "12px",
        fontSize: "20px",
        borderBottom: "1px solid #00d4ff",
        paddingBottom: "6px",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
    },
    card: {
        backgroundColor: "#12122a",
        borderRadius: "6px",
        padding: "10px",
        marginBottom: "10px",
        border: "1px solid #1a1a3a",
        overflow: "visible",
    },
    cardHeader: {
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        marginBottom: "8px",
    },
    skillRow: {
        display: "flex",
        alignItems: "center",
        gap: "10px",
        padding: "6px 0",
        borderBottom: "1px solid #1a1a3a",
        cursor: "pointer",
    },
    skillIcon: {
        width: "20px",
        height: "20px",
        imageRendering: "pixelated",
    },
    xpValue: {
        color: "#00ff88",
        fontWeight: "bold",
        fontSize: "16px",
    },
    xpRate: {
        color: "#ffaa00",
        fontSize: "12px",
    },
    rollingRate: {
        color: "#00d4ff",
        fontSize: "12px",
    },
    label: {
        color: "#666",
        fontSize: "11px",
        textTransform: "uppercase",
        letterSpacing: "0.5px",
    },
    button: {
        backgroundColor: "#1a1a3a",
        color: "#00d4ff",
        border: "1px solid #00d4ff",
        padding: "4px 12px",
        borderRadius: "4px",
        cursor: "pointer",
        fontSize: "12px",
    },
    tabButton: {
        backgroundColor: "transparent",
        color: "#666",
        border: "none",
        padding: "6px 12px",
        borderRadius: "4px 4px 0 0",
        cursor: "pointer",
        fontSize: "12px",
    },
    tabButtonActive: {
        backgroundColor: "#1a1a3a",
        color: "#00d4ff",
        border: "none",
        padding: "6px 12px",
        borderRadius: "4px 4px 0 0",
        cursor: "pointer",
        fontSize: "12px",
    },
    statRow: {
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        padding: "4px 0",
    },
    dropRow: {
        display: "flex",
        alignItems: "center",
        gap: "8px",
        padding: "4px 0",
        borderBottom: "1px solid #1a1a2a",
        fontSize: "11px",
    },
    dropXp: {
        color: "#00ff88",
        fontWeight: "bold",
        minWidth: "50px",
    },
    dropSkills: {
        color: "#888",
        flex: 1,
    },
    dropSource: {
        color: "#ffaa00",
        fontSize: "10px",
    },
    dropType: {
        color: "#666",
        fontSize: "9px",
        padding: "1px 4px",
        backgroundColor: "#1a1a3a",
        borderRadius: "2px",
    },
    dropTime: {
        color: "#444",
        fontSize: "9px",
        minWidth: "45px",
        textAlign: "right",
    },
};
// Calculate rolling XP/hr from recent drops within window
function calcRollingXpPerHour(recentXp, windowMs) {
    const now = Date.now();
    const cutoff = now - windowMs;
    const relevant = recentXp.filter(d => d.time >= cutoff);
    if (relevant.length === 0)
        return 0;
    const totalXp = relevant.reduce((sum, d) => sum + d.xp, 0);
    const elapsed = now - Math.min(...relevant.map(d => d.time));
    if (elapsed < 1000)
        return 0; // Not enough time
    return (totalXp / elapsed) * 3600000;
}
// ============================================================================
// MAIN APP
// ============================================================================
function App() {
    const glapi = react__WEBPACK_IMPORTED_MODULE_2___default().useContext(Alt1ApiContext);
    const [isLoading, setIsLoading] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(true);
    const [loadingStatus, setLoadingStatus] = react__WEBPACK_IMPORTED_MODULE_2___default().useState("Initializing...");
    const [startTime, setStartTime] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(Date.now());
    const [skillXp, setSkillXp] = react__WEBPACK_IMPORTED_MODULE_2___default().useState({});
    const [totalHistory, setTotalHistory] = react__WEBPACK_IMPORTED_MODULE_2___default().useState([{ time: 0, value: 0 }]);
    const [recentTotalXp, setRecentTotalXp] = react__WEBPACK_IMPORTED_MODULE_2___default().useState([]);
    const [dropHistory, setDropHistory] = react__WEBPACK_IMPORTED_MODULE_2___default().useState([]);
    const [totalXp, setTotalXp] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(0);
    const [elapsedMs, setElapsedMs] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(0);
    const [actionCount, setActionCount] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(0);
    const [expandedSkill, setExpandedSkill] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(null);
    const [activeTab, setActiveTab] = react__WEBPACK_IMPORTED_MODULE_2___default().useState("overview");
    // Sparkline stores timestamped rate data for the full session
    const [sparklineHistory, setSparklineHistory] = react__WEBPACK_IMPORTED_MODULE_2___default().useState([]);
    const [colorVersion, setColorVersion] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(0); // Triggers re-render on color change
    const [xpBuckets, setXpBuckets] = react__WEBPACK_IMPORTED_MODULE_2___default().useState([]); // Time buckets for stacked bar chart
    const dropIdRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(0);
    const startTimeRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(startTime);
    const rollingRateRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(0); // Tracks current rolling XP/hr for sparkline
    // Debug zone overlay
    const debugZoneRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(null);
    const [debugZoneVisible, setDebugZoneVisible] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(false);
    const [debugZone, setDebugZone] = react__WEBPACK_IMPORTED_MODULE_2___default().useState({ x: 600, y: 984, width: 700, height: 94 });
    const detectorRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(null);
    // Keep startTimeRef in sync with startTime state
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        startTimeRef.current = startTime;
    }, [startTime]);
    // Update rolling rate ref whenever recentTotalXp changes
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        const now = Date.now();
        const cutoff = now - ROLLING_WINDOW_MS;
        const relevant = recentTotalXp.filter(d => d.time >= cutoff);
        if (relevant.length > 0) {
            const totalXpInWindow = relevant.reduce((sum, d) => sum + d.xp, 0);
            const windowElapsed = now - Math.min(...relevant.map(d => d.time));
            if (windowElapsed >= 1000) {
                rollingRateRef.current = (totalXpInWindow / windowElapsed) * 3600000;
            }
        }
        else {
            rollingRateRef.current = 0;
        }
    }, [recentTotalXp]);
    // Update elapsed time, sparkline, and clean up stale data every second
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        const interval = setInterval(() => {
            const now = Date.now();
            const elapsed = now - startTime;
            setElapsedMs(elapsed);
            // Update sparkline history with ROLLING rate (shows actual activity, not flattening average)
            // Use the rollingRateRef which is updated whenever XP drops occur
            if (elapsed > 0) {
                const rollingRate = rollingRateRef.current;
                setSparklineHistory(prev => {
                    const newPoint = { time: elapsed, rate: rollingRate };
                    const newHistory = [...prev, newPoint];
                    // Keep max 600 points (10 mins at 1/sec), downsample if needed
                    if (newHistory.length > 600) {
                        // Downsample by keeping every other point but preserve peaks
                        const downsampled = [];
                        for (let i = 0; i < newHistory.length; i += 2) {
                            // Keep the higher value between pairs to preserve spikes
                            if (i + 1 < newHistory.length) {
                                downsampled.push(newHistory[i].rate > newHistory[i + 1].rate ? newHistory[i] : newHistory[i + 1]);
                            }
                            else {
                                downsampled.push(newHistory[i]);
                            }
                        }
                        return downsampled;
                    }
                    return newHistory;
                });
            }
            // Clean up stale recentTotalXp entries
            setRecentTotalXp((prev) => {
                const cutoff = now - ROLLING_WINDOW_MS;
                const filtered = prev.filter((d) => d.time >= cutoff);
                // Only update if something changed to avoid unnecessary re-renders
                return filtered.length === prev.length ? prev : filtered;
            });
            // Clean up stale per-skill recentXp entries (prevents memory leak)
            setSkillXp((prev) => {
                const cutoff = now - ROLLING_WINDOW_MS;
                let hasChanges = false;
                const updated = {};
                for (const [skill, data] of Object.entries(prev)) {
                    const filteredRecent = data.recentXp.filter((d) => d.time >= cutoff);
                    if (filteredRecent.length !== data.recentXp.length) {
                        hasChanges = true;
                        updated[skill] = { ...data, recentXp: filteredRecent };
                    }
                    else {
                        updated[skill] = data;
                    }
                }
                return hasChanges ? updated : prev;
            });
        }, 1000);
        return () => clearInterval(interval);
    }, [startTime]);
    // Track total accumulated XP in a ref so it persists across renders
    // but can be reset without re-creating the stream
    const totalAccumRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(0);
    // Main render stream - uses renderStream helper for proper 1080p/4K UI handling
    // Only depends on glapi - doesn't restart on reset
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        let uistream = null;
        let detector = null;
        let isMounted = true;
        const init = async () => {
            setLoadingStatus("Loading sprite cache...");
            const spritecache = new _reflect2d_spritecache__WEBPACK_IMPORTED_MODULE_5__.SpriteCache();
            await spritecache.downloadCacheData();
            if (!isMounted)
                return;
            setLoadingStatus("Initializing atlas tracker...");
            const atlas = new _reflect2d_reflect2d__WEBPACK_IMPORTED_MODULE_4__.AtlasTracker(spritecache);
            detector = new _reader_xpReader__WEBPACK_IMPORTED_MODULE_6__.XpDetector(atlas);
            detectorRef.current = detector;
            if (!isMounted)
                return;
            setLoadingStatus("Connecting to RS3...");
            setIsLoading(false);
            console.log("[XP Init] Starting render stream...", { ready: glapi.getRsReady(), isMounted });
            // Use renderStream helper which properly handles both 1080p and 4K UI scaling
            // At 1080p: captures from framebuffer 0 (screen)
            // At 4K: detects UI scaler and captures from UI framebuffer texture
            // The getUIState() in reflect2d.ts then filters by isUi to only process UI programs
            uistream = (0,_util_renderStream__WEBPACK_IMPORTED_MODULE_9__.renderStream)(glapi, async (renders) => {
                console.log(`[XP Stream] Callback fired: ${renders.length} renders`);
                if (renders.length === 0 || !detector || !isMounted)
                    return;
                const result = detector.detect(renders);
                if (!result)
                    return;
                const { xpValue, skill, skillSprite, dropType, skills, skillXp: xpAttribution, likelySource } = result;
                const now = Date.now();
                const elapsed = now - startTimeRef.current;
                // Update total using ref
                totalAccumRef.current += xpValue;
                const currentTotal = totalAccumRef.current;
                setTotalXp(currentTotal);
                setTotalHistory((prev) => [...prev.slice(-100), { time: elapsed, value: currentTotal }]);
                setActionCount((prev) => prev + 1);
                // Track recent XP for rolling rate
                setRecentTotalXp((prev) => {
                    const cutoff = now - ROLLING_WINDOW_MS;
                    const filtered = prev.filter((d) => d.time >= cutoff);
                    return [...filtered, { time: now, xp: xpValue }];
                });
                // Add to drop history
                const dropEntry = {
                    id: dropIdRef.current++,
                    timestamp: now,
                    xpValue,
                    dropType,
                    skills: skills.map((s) => s.label),
                    skillXp: xpAttribution.map((a) => ({ skill: a.skill.label, xp: a.xp })),
                    likelySource,
                };
                setDropHistory((prev) => [dropEntry, ...prev.slice(0, MAX_DROP_HISTORY - 1)]);
                // Add XP to time buckets for stacked bar chart
                const bucketIndex = Math.floor(elapsed / BUCKET_DURATION_MS) + 1;
                setXpBuckets((prev) => {
                    const buckets = [...prev];
                    let bucket = buckets.find(b => b.bucketIndex === bucketIndex);
                    if (!bucket) {
                        bucket = {
                            bucketIndex,
                            startTime: (bucketIndex - 1) * BUCKET_DURATION_MS,
                            skills: {},
                            total: 0,
                        };
                        buckets.push(bucket);
                        buckets.sort((a, b) => a.bucketIndex - b.bucketIndex);
                    }
                    // Add XP for each skill
                    xpAttribution.forEach((attr) => {
                        bucket.skills[attr.skill.label] = (bucket.skills[attr.skill.label] || 0) + attr.xp;
                    });
                    bucket.total += xpValue;
                    return buckets;
                });
                // Use the pre-calculated skillXp attribution for accurate per-skill tracking
                if (xpAttribution.length > 0) {
                    setSkillXp((prev) => {
                        const updated = { ...prev };
                        xpAttribution.forEach((attr) => {
                            const existing = updated[attr.skill.label] ?? {
                                total: 0,
                                drops: 0,
                                history: [],
                                rateHistory: [],
                                recentXp: [],
                                icon: null,
                            };
                            const newRecentXp = [
                                ...existing.recentXp.filter((d) => d.time >= now - ROLLING_WINDOW_MS),
                                { time: now, xp: attr.xp },
                            ];
                            const rollingRate = calcRollingXpPerHour(newRecentXp, ROLLING_WINDOW_MS);
                            updated[attr.skill.label] = {
                                total: existing.total + attr.xp,
                                drops: existing.drops + 1,
                                history: [...existing.history.slice(-100), { time: elapsed, value: existing.total + attr.xp }],
                                rateHistory: [...existing.rateHistory.slice(-100), { time: elapsed, value: rollingRate }],
                                recentXp: newRecentXp,
                                icon: existing.icon,
                            };
                        });
                        return updated;
                    });
                    // Capture icon for single skill drops (only if we don't have one yet)
                    if (skill && skillSprite) {
                        setSkillXp((prev) => {
                            // Skip if we already have an icon for this skill
                            if (prev[skill.label]?.icon)
                                return prev;
                            const icon = (0,_reader_xpReader__WEBPACK_IMPORTED_MODULE_6__.captureSprite)(skillSprite.sprite);
                            if (!icon)
                                return prev;
                            return {
                                ...prev,
                                [skill.label]: {
                                    ...prev[skill.label],
                                    icon,
                                },
                            };
                        });
                    }
                }
            }, {
                framecooldown: TICK_MS,
                features: ["vertexarray", "uniforms", "texturesnapshot"],
            });
        };
        init().catch(e => console.error("[XP Init] Error during initialization:", e));
        return () => {
            isMounted = false;
            if (uistream) {
                uistream.close();
            }
        };
    }, [glapi]);
    const reset = () => {
        const now = Date.now();
        setStartTime(now);
        startTimeRef.current = now;
        totalAccumRef.current = 0;
        setSkillXp({});
        setTotalXp(0);
        setTotalHistory([{ time: 0, value: 0 }]);
        setRecentTotalXp([]);
        setDropHistory([]);
        setActionCount(0);
        setExpandedSkill(null);
        setSparklineHistory([]);
        setXpBuckets([]);
        dropIdRef.current = 0;
    };
    const toggleDebugZone = react__WEBPACK_IMPORTED_MODULE_2___default().useCallback(() => {
        if (!debugZoneRef.current) {
            debugZoneRef.current = new _overlay_DebugZoneOverlay__WEBPACK_IMPORTED_MODULE_10__.DebugZoneOverlay();
        }
        if (debugZoneVisible) {
            debugZoneRef.current.hide();
            setDebugZoneVisible(false);
        }
        else {
            debugZoneRef.current.show(debugZone);
            setDebugZoneVisible(true);
        }
        // Also update detector's zone filter
        if (detectorRef.current) {
            detectorRef.current.setDetectionZone(debugZoneVisible ? null : debugZone);
        }
    }, [debugZoneVisible, debugZone]);
    const updateDebugZone = react__WEBPACK_IMPORTED_MODULE_2___default().useCallback((field, value) => {
        setDebugZone(prev => {
            const updated = { ...prev, [field]: value };
            if (debugZoneRef.current && debugZoneVisible) {
                debugZoneRef.current.updateZone(updated);
            }
            return updated;
        });
    }, [debugZoneVisible]);
    // Update detector's zone filter when debug zone changes
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        if (detectorRef.current) {
            detectorRef.current.setDetectionZone(debugZoneVisible ? debugZone : null);
        }
    }, [debugZoneVisible, debugZone]);
    const copyZoneCoords = react__WEBPACK_IMPORTED_MODULE_2___default().useCallback(() => {
        const text = `Zone: x=${debugZone.x}, y=${debugZone.y}, w=${debugZone.width}, h=${debugZone.height}`;
        navigator.clipboard.writeText(text).then(() => {
            console.log("[DebugZone] Copied:", text);
        });
    }, [debugZone]);
    // Cleanup debug zone on unmount
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        return () => {
            debugZoneRef.current?.dispose();
        };
    }, []);
    // Calculate rates
    const xpPerHour = calcXpPerHour(totalXp, elapsedMs);
    const rollingXpPerHour = calcRollingXpPerHour(recentTotalXp, ROLLING_WINDOW_MS);
    const avgTicksPerAction = actionCount > 0 && elapsedMs > 0 ? (elapsedMs / TICK_MS) / actionCount : 0;
    // Sort skills by XP - memoized to prevent infinite loops in useEffect
    const sortedSkills = react__WEBPACK_IMPORTED_MODULE_2___default().useMemo(() => Object.entries(skillXp).sort((a, b) => b[1].total - a[1].total), [skillXp]);
    // Prepare skill bar chart data (colorVersion forces recalc on color change)
    const skillBarData = react__WEBPACK_IMPORTED_MODULE_2___default().useMemo(() => sortedSkills.slice(0, 10).map(([name, data]) => ({
        name,
        xp: data.total,
        color: getSkillColor(name),
    })), [sortedSkills, colorVersion]);
    // XP Chart Overlay - compact by default, can expand
    const overlay = (0,_overlay__WEBPACK_IMPORTED_MODULE_7__.useXpChartOverlay)({ enabled: true, defaultExpanded: false });
    // Overlay settings state
    const [showOverlaySettings, setShowOverlaySettings] = react__WEBPACK_IMPORTED_MODULE_2___default().useState(false);
    // Get primary skill color (top skill by XP)
    const primarySkillColor = sortedSkills.length > 0 ? getSkillColor(sortedSkills[0][0]) : "#00ff88";
    // Update overlay when data changes - throttled to prevent excessive updates
    const lastOverlayUpdateRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(0);
    react__WEBPACK_IMPORTED_MODULE_2___default().useEffect(() => {
        if (!overlay.isAvailable)
            return;
        // Throttle updates to max once per second
        const now = Date.now();
        if (now - lastOverlayUpdateRef.current < 1000)
            return;
        lastOverlayUpdateRef.current = now;
        // Prepare skill breakdown for overlay
        const skillBreakdown = sortedSkills.slice(0, 6).map(([name, data]) => ({
            name,
            xp: data.total,
            color: getSkillColor(name),
        }));
        // Downsample sparkline history to ~60 points for overlay display
        const maxOverlayPoints = 60;
        let sparklineData;
        if (sparklineHistory.length <= maxOverlayPoints) {
            sparklineData = sparklineHistory.map(p => p.rate);
        }
        else {
            // Downsample by selecting evenly spaced points
            sparklineData = [];
            const step = sparklineHistory.length / maxOverlayPoints;
            for (let i = 0; i < maxOverlayPoints; i++) {
                const idx = Math.min(Math.floor(i * step), sparklineHistory.length - 1);
                sparklineData.push(sparklineHistory[idx].rate);
            }
            // Always include the last point
            sparklineData[sparklineData.length - 1] = sparklineHistory[sparklineHistory.length - 1].rate;
        }
        // Prepare per-skill rate history for expanded multi-line chart
        const skillRateHistory = sortedSkills.slice(0, 6).map(([name, data]) => ({
            name,
            color: getSkillColor(name),
            data: data.rateHistory.slice(-30), // Last 30 data points
        }));
        overlay.updateChart({
            totalXp,
            xpPerHour,
            rollingXpPerHour,
            elapsedMs,
            skillBreakdown,
            sparklineData,
            primarySkillColor,
            skillRateHistory,
        });
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, [overlay.isAvailable, sortedSkills, totalXp, xpPerHour, rollingXpPerHour, elapsedMs, sparklineHistory, primarySkillColor]);
    // Show loading screen
    if (isLoading) {
        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { ...styles.container, display: "flex", flexDirection: "column", alignItems: "center", justifyContent: "center" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { fontSize: "18px", color: "#00d4ff", marginBottom: "12px" }, children: "Loading XP Tracker..." }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { fontSize: "12px", color: "#666" }, children: loadingStatus })] }));
    }
    return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.container, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.header, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { children: "XP Tracker" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { display: "flex", gap: "8px", alignItems: "center" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { style: {
                                    ...styles.button,
                                    backgroundColor: overlay.isExpanded ? "#00ff88" : "#1a1a3a",
                                    color: overlay.isExpanded ? "#000" : "#00ff88",
                                    padding: "4px 8px",
                                }, onClick: overlay.toggleExpanded, title: overlay.isExpanded ? "Compact view" : "Expanded view with skills", children: overlay.isExpanded ? "-" : "+" }), overlay.isAvailable && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { style: {
                                            ...styles.button,
                                            backgroundColor: overlay.isVisible ? "#00d4ff" : "#1a1a3a",
                                            color: overlay.isVisible ? "#000" : "#00d4ff",
                                        }, onClick: overlay.toggle, title: "Toggle XP overlay on game screen", children: overlay.isVisible ? "Hide Overlay" : "Show Overlay" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { style: {
                                            ...styles.button,
                                            backgroundColor: showOverlaySettings ? "#ffaa00" : "#1a1a3a",
                                            color: showOverlaySettings ? "#000" : "#ffaa00",
                                            padding: "4px 8px",
                                        }, onClick: () => setShowOverlaySettings(!showOverlaySettings), title: "Overlay position settings", children: "Pos" })] })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { style: styles.button, onClick: reset, children: "Reset" })] })] }), showOverlaySettings && overlay.isAvailable && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: {
                    ...styles.card,
                    marginBottom: "10px",
                    border: "1px solid #ffaa00",
                }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { marginBottom: "8px", color: "#ffaa00", fontWeight: "bold" }, children: "Overlay Position" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(_overlay_OverlayPositionEditor__WEBPACK_IMPORTED_MODULE_8__["default"], { positionX: overlay.position.x, positionY: overlay.position.y, onPositionChange: overlay.setPosition, screenWidth: overlay.uiBounds.width, screenHeight: overlay.uiBounds.height, overlayWidth: overlay.size.width, overlayHeight: overlay.size.height, isVisible: overlay.isVisible })] })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { display: "flex", gap: "2px", marginBottom: "10px" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { style: activeTab === "overview" ? styles.tabButtonActive : styles.tabButton, onClick: () => setActiveTab("overview"), children: "Overview" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { style: activeTab === "skills" ? styles.tabButtonActive : styles.tabButton, onClick: () => setActiveTab("skills"), children: "Skills" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { style: activeTab === "drops" ? styles.tabButtonActive : styles.tabButton, onClick: () => setActiveTab("drops"), children: "Drops" })] }), activeTab === "overview" && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)(react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.Fragment, { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.card, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.statRow, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.label, children: "Session" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { children: formatTime(elapsedMs) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.statRow, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.label, children: "Actions" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("span", { children: [actionCount, " (", avgTicksPerAction.toFixed(1), " ticks/action)"] })] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.card, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.statRow, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.label, children: "Total XP" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.xpValue, children: formatXp(totalXp) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.statRow, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.label, children: "Session Rate" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("span", { style: styles.xpRate, children: [formatXpInt(xpPerHour), "/hr"] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.statRow, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.label, children: "Rolling Rate (5m)" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("span", { style: styles.rollingRate, children: [formatXpInt(rollingXpPerHour), "/hr"] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { marginTop: "8px" }, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(MiniChart, { data: totalHistory, label: "Total XP", color: "#00ff88" }) })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { ...styles.card, padding: "10px 8px" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.cardHeader, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.label, children: "XP Per Minute" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("span", { style: { fontSize: "11px", color: "#00ff88" }, children: [formatXpInt(totalXp), " total"] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(StackedBarChart, { buckets: xpBuckets, skills: sortedSkills.slice(0, 8).map(([name, data]) => ({
                                    name,
                                    color: getSkillColor(name),
                                    icon: data.icon,
                                })), width: 380, height: 200, maxBuckets: 12 })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { ...styles.card, padding: "10px 8px" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: styles.cardHeader, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.label, children: "XP/hr Over Time (All Skills)" }) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(StackedAreaChart, { series: sortedSkills.slice(0, 8).map(([name, data]) => ({
                                    name,
                                    color: getSkillColor(name),
                                    data: data.rateHistory,
                                })), width: 380, height: 260, showLegend: true, maxDataPoints: 60 })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.card, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.cardHeader, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.label, children: "Skill Breakdown" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: { fontSize: "10px", color: "#555" }, children: "Click color to customize" })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(SkillBarChart, { skills: skillBarData, onColorChange: () => setColorVersion(v => v + 1) })] })] })), activeTab === "skills" && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.card, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { ...styles.label, marginBottom: "8px" }, children: ["Skills (", sortedSkills.length, ")"] }), sortedSkills.length === 0 ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { color: "#444", padding: "8px", textAlign: "center" }, children: "Waiting for XP drops..." })) : (sortedSkills.map(([skill, data]) => {
                        const skillRollingRate = calcRollingXpPerHour(data.recentXp, ROLLING_WINDOW_MS);
                        return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.skillRow, onClick: () => setExpandedSkill(expandedSkill === skill ? null : skill), children: [data.icon && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("img", { src: data.icon, alt: skill, style: styles.skillIcon })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { flex: 1 }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { fontSize: "13px" }, children: skill }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { fontSize: "10px", color: "#555" }, children: [data.drops, " drops"] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { textAlign: "right" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: styles.xpValue, children: formatXp(data.total) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.xpRate, children: [formatXpInt(calcXpPerHour(data.total, elapsedMs)), "/hr"] }), skillRollingRate > 0 && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.rollingRate, children: [formatXpInt(skillRollingRate), "/hr (5m)"] }))] })] }), expandedSkill === skill && data.history.length > 1 && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { padding: "8px 0" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(MiniChart, { data: data.history, label: `${skill} Total XP`, color: getSkillColor(skill) }), data.rateHistory.length > 1 && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { marginTop: "8px" }, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(LineChart, { data: data.rateHistory, label: `${skill} XP/hr`, color: getSkillColor(skill), showRate: true }) }))] }))] }, skill));
                    }))] })), activeTab === "drops" && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.card, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { ...styles.label, marginBottom: "8px" }, children: ["Recent Drops (", dropHistory.length, ")"] }), dropHistory.length === 0 ? ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { color: "#444", padding: "8px", textAlign: "center" }, children: "No drops recorded yet..." })) : ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { maxHeight: "400px", overflowY: "auto" }, children: dropHistory.map((drop) => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.dropRow, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("span", { style: styles.dropXp, children: ["+", formatXp(drop.xpValue)] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.dropType, children: getDropTypeLabel(drop.dropType) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.dropSkills, children: drop.skillXp.map((s) => `${s.skill}: ${formatXp(s.xp)}`).join(", ") }), drop.likelySource && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.dropSource, children: drop.likelySource })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.dropTime, children: formatTimeShort(Date.now() - drop.timestamp) })] }, drop.id))) }))] })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: {
                    marginTop: 12,
                    padding: 8,
                    background: "rgba(0,0,0,0.3)",
                    borderRadius: 6,
                    border: "1px solid rgba(255,255,255,0.1)",
                    fontSize: 12,
                }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { display: "flex", alignItems: "center", gap: 8, marginBottom: 8 }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: { fontWeight: 600, color: "#0f0" }, children: "Debug Zone" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { onClick: toggleDebugZone, style: {
                                    padding: "2px 8px",
                                    fontSize: 11,
                                    background: debugZoneVisible ? "#0a0" : "#333",
                                    color: "#fff",
                                    border: "1px solid #555",
                                    borderRadius: 3,
                                    cursor: "pointer",
                                }, children: debugZoneVisible ? "Hide" : "Show" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { onClick: copyZoneCoords, style: {
                                    padding: "2px 8px",
                                    fontSize: 11,
                                    background: "#333",
                                    color: "#fff",
                                    border: "1px solid #555",
                                    borderRadius: 3,
                                    cursor: "pointer",
                                }, children: "Copy" })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: { display: "grid", gridTemplateColumns: "1fr 1fr", gap: 4 }, children: ["x", "y", "width", "height"].map(field => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("label", { style: { display: "flex", alignItems: "center", gap: 4, color: "#aaa" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("span", { style: { width: 40, textAlign: "right" }, children: [field, ":"] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("input", { type: "text", defaultValue: debugZone[field], onBlur: e => {
                                        const v = parseInt(e.target.value);
                                        if (!isNaN(v))
                                            updateDebugZone(field, v);
                                    }, onKeyDown: e => {
                                        if (e.key === "Enter") {
                                            const v = parseInt(e.target.value);
                                            if (!isNaN(v))
                                                updateDebugZone(field, v);
                                        }
                                    }, style: {
                                        width: 60,
                                        padding: "1px 4px",
                                        fontSize: 11,
                                        background: "#222",
                                        color: "#fff",
                                        border: "1px solid #444",
                                        borderRadius: 2,
                                    } }, `${field}-${debugZone[field]}`)] }, field))) }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { marginTop: 4, color: "#666", fontSize: 10 }, children: ["Zone: x=", debugZone.x, ", y=", debugZone.y, ", w=", debugZone.width, ", h=", debugZone.height] })] })] }));
}
// Default skill colors - muted monotone palette for dark UI
const DEFAULT_SKILL_COLORS = {
    // Combat skills - warm reds/oranges
    Attack: "#e85d5d",
    Strength: "#5dcc5d",
    Defence: "#5d9ce8",
    Constitution: "#e89a5d",
    Ranged: "#8bc34a",
    Prayer: "#e8d85d",
    Magic: "#5dcce8",
    Necromancy: "#b06dd9",
    // Gathering skills - earthy tones
    Mining: "#a67c52",
    Fishing: "#5db8e8",
    Woodcutting: "#4d7a4d",
    Farming: "#7ba353",
    Hunter: "#9a7640",
    Divination: "#9b7ed9",
    Archaeology: "#c49a6c",
    // Artisan skills - muted warm tones
    Smithing: "#7a7a8a",
    Cooking: "#d98c5c",
    Firemaking: "#d96846",
    Runecrafting: "#d9a85c",
    Crafting: "#c9a37a",
    Fletching: "#5a9a5a",
    Herblore: "#5db35d",
    Construction: "#b87333",
    Invention: "#d9c35c",
    // Support skills - cool tones
    Agility: "#5a8ad9",
    Thieving: "#9a5a9a",
    Slayer: "#6a6a7a",
    Summoning: "#5aa8d9",
    Dungeoneering: "#d96a5a",
};
// LocalStorage key for user color preferences
const SKILL_COLORS_KEY = "xp-tracker-skill-colors";
// Load user color preferences from localStorage
function loadUserSkillColors() {
    try {
        const stored = localStorage.getItem(SKILL_COLORS_KEY);
        return stored ? JSON.parse(stored) : {};
    }
    catch {
        return {};
    }
}
// Save a single skill color to localStorage
function saveSkillColor(skill, color) {
    const colors = loadUserSkillColors();
    colors[skill] = color;
    localStorage.setItem(SKILL_COLORS_KEY, JSON.stringify(colors));
}
// Reset a skill color to default
function resetSkillColor(skill) {
    const colors = loadUserSkillColors();
    delete colors[skill];
    localStorage.setItem(SKILL_COLORS_KEY, JSON.stringify(colors));
}
// Get color for a skill (user preference first, then default)
function getSkillColor(skill) {
    const userColors = loadUserSkillColors();
    return userColors[skill] ?? DEFAULT_SKILL_COLORS[skill] ?? "#5de8a0";
}
// Clickable color indicator with color picker
function SkillColorPicker({ skill, color, onColorChange, }) {
    const inputRef = react__WEBPACK_IMPORTED_MODULE_2___default().useRef(null);
    return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: { position: "relative" }, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: {
                    width: "4px",
                    height: "20px",
                    backgroundColor: color,
                    borderRadius: "2px",
                    marginRight: "8px",
                    cursor: "pointer",
                }, onClick: () => inputRef.current?.click(), title: `Click to change ${skill} color` }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("input", { ref: inputRef, type: "color", value: color, onChange: (e) => onColorChange(skill, e.target.value), onContextMenu: (e) => {
                    e.preventDefault();
                    resetSkillColor(skill);
                    onColorChange(skill, DEFAULT_SKILL_COLORS[skill] ?? "#5de8a0");
                }, style: {
                    position: "absolute",
                    opacity: 0,
                    width: 0,
                    height: 0,
                    pointerEvents: "none",
                } })] }));
}
// ============================================================================
// BOOTSTRAP
// ============================================================================
function start() {
    const api = getApi();
    globalThis.native = api;
    react_dom_client__WEBPACK_IMPORTED_MODULE_3__.createRoot(document.getElementById("app")).render((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(Alt1ApiContext.Provider, { value: api, children: (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)(App, {}) }));
    // F5 to refresh
    window.addEventListener("keydown", (e) => {
        if (e.key === "F5")
            window.location.reload();
    });
}
start();


/***/ },

/***/ "./overlay/ChartRenderer.ts"
/*!**********************************!*\
  !*** ./overlay/ChartRenderer.ts ***!
  \**********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   renderCompactOverlay: () => (/* binding */ renderCompactOverlay),
/* harmony export */   renderExpandedOverlay: () => (/* binding */ renderExpandedOverlay),
/* harmony export */   renderXpRateChart: () => (/* binding */ renderXpRateChart),
/* harmony export */   renderXpStatsPanel: () => (/* binding */ renderXpStatsPanel),
/* harmony export */   renderXpSummary: () => (/* binding */ renderXpSummary)
/* harmony export */ });
/**
 * Chart Renderer - Renders XP overlays to canvas for GL overlay
 * Supports compact view with sparkline and expanded view with skill breakdown
 */
const DEFAULT_CONFIG = {
    width: 320,
    height: 200,
    backgroundColor: "rgba(0, 0, 0, 0.85)",
    textColor: "#ffffff",
    gridColor: "#333333",
    fontFamily: "'Segoe UI', 'Arial', sans-serif",
    fontSize: 14,
    padding: 12,
    showLegend: true,
    title: "XP Tracker",
};
/**
 * Format XP value for display
 */
function formatXp(xp) {
    if (xp >= 1000000)
        return (xp / 1000000).toFixed(1) + "M";
    if (xp >= 1000)
        return (xp / 1000).toFixed(1) + "K";
    return Math.round(xp).toLocaleString();
}
/**
 * Format XP rate - large number with commas
 */
function formatRateLarge(xp) {
    return Math.round(xp).toLocaleString();
}
/**
 * Format XP rate compact
 */
function formatRate(xp) {
    if (xp >= 1000000)
        return (xp / 1000000).toFixed(2) + "M/hr";
    if (xp >= 1000)
        return Math.round(xp / 1000).toLocaleString() + "K/hr";
    return Math.round(xp).toLocaleString() + "/hr";
}
/**
 * Format session time
 */
function formatTime(ms) {
    const h = Math.floor(ms / 3600000);
    const m = Math.floor((ms % 3600000) / 60000);
    const s = Math.floor((ms % 60000) / 1000);
    return `${h}:${m.toString().padStart(2, "0")}:${s.toString().padStart(2, "0")}`;
}
/**
 * Get device pixel ratio
 */
function getDPR() {
    return window.devicePixelRatio || 1;
}
/**
 * Draw rounded rectangle
 */
function roundedRect(ctx, x, y, w, h, r) {
    ctx.beginPath();
    ctx.moveTo(x + r, y);
    ctx.lineTo(x + w - r, y);
    ctx.quadraticCurveTo(x + w, y, x + w, y + r);
    ctx.lineTo(x + w, y + h - r);
    ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h);
    ctx.lineTo(x + r, y + h);
    ctx.quadraticCurveTo(x, y + h, x, y + h - r);
    ctx.lineTo(x, y + r);
    ctx.quadraticCurveTo(x, y, x + r, y);
    ctx.closePath();
}
/**
 * Draw clean line chart with smooth curves (like RS player count chart)
 */
function drawLineChart(ctx, data, x, y, width, height, color, showDots = true, showYAxis = false, fontFamily = "'Segoe UI', Arial, sans-serif", showFill = true) {
    if (data.length < 2)
        return;
    // Enable antialiasing
    ctx.imageSmoothingEnabled = true;
    ctx.imageSmoothingQuality = "high";
    const minVal = Math.min(...data) * 0.9; // Add 10% padding below
    const maxVal = Math.max(...data) * 1.05; // Add 5% padding above
    const range = maxVal - minVal || 1;
    const leftPadding = showYAxis ? 35 : 0;
    const chartWidth = width - leftPadding;
    const chartX = x + leftPadding;
    // Draw Y-axis labels if enabled
    if (showYAxis && height > 40) {
        ctx.fillStyle = "#666";
        ctx.font = `10px ${fontFamily}`;
        ctx.textAlign = "right";
        // Draw 3 labels: top, middle, bottom
        const labels = [maxVal, (maxVal + minVal) / 2, minVal];
        labels.forEach((val, i) => {
            const labelY = y + (i / 2) * height;
            ctx.fillText(formatRate(val).replace("/hr", ""), chartX - 5, labelY + 3);
            // Draw subtle grid line
            ctx.strokeStyle = "#333";
            ctx.lineWidth = 0.5;
            ctx.beginPath();
            ctx.moveTo(chartX, labelY);
            ctx.lineTo(chartX + chartWidth, labelY);
            ctx.stroke();
        });
    }
    // Calculate points
    const points = [];
    for (let i = 0; i < data.length; i++) {
        const px = chartX + (i / (data.length - 1)) * chartWidth;
        const py = y + height - ((data[i] - minVal) / range) * height;
        points.push({ x: px, y: py });
    }
    // Helper to draw smooth curve through points using cardinal spline
    const drawSmoothLine = (close = false) => {
        ctx.beginPath();
        ctx.moveTo(points[0].x, points[0].y);
        if (points.length === 2) {
            // Just two points - draw straight line
            ctx.lineTo(points[1].x, points[1].y);
        }
        else {
            // Use quadratic curves for smoothing
            for (let i = 0; i < points.length - 1; i++) {
                const p0 = points[i];
                const p1 = points[i + 1];
                // Control point is midpoint
                const midX = (p0.x + p1.x) / 2;
                const midY = (p0.y + p1.y) / 2;
                if (i === 0) {
                    // First segment - line to midpoint
                    ctx.lineTo(midX, midY);
                }
                else {
                    // Curve to midpoint using previous point as control
                    ctx.quadraticCurveTo(p0.x, p0.y, midX, midY);
                }
            }
            // Final segment to last point
            const lastPoint = points[points.length - 1];
            const secondLast = points[points.length - 2];
            ctx.quadraticCurveTo(secondLast.x, secondLast.y, lastPoint.x, lastPoint.y);
        }
        if (close) {
            // Close path for fill
            ctx.lineTo(chartX + chartWidth, y + height);
            ctx.lineTo(chartX, y + height);
            ctx.closePath();
        }
    };
    // Draw gradient fill under the line
    if (showFill) {
        drawSmoothLine(true);
        const gradient = ctx.createLinearGradient(0, y, 0, y + height);
        gradient.addColorStop(0, color + "40"); // 25% opacity at top
        gradient.addColorStop(1, color + "08"); // 3% opacity at bottom
        ctx.fillStyle = gradient;
        ctx.fill();
    }
    // Draw the line
    drawSmoothLine(false);
    ctx.strokeStyle = color;
    ctx.lineWidth = 2;
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    ctx.stroke();
    // Draw dots at data points
    if (showDots && data.length <= 30) {
        for (const point of points) {
            // Outer circle (colored)
            ctx.beginPath();
            ctx.arc(point.x, point.y, 4, 0, Math.PI * 2);
            ctx.fillStyle = color;
            ctx.fill();
            // Inner circle (white)
            ctx.beginPath();
            ctx.arc(point.x, point.y, 2, 0, Math.PI * 2);
            ctx.fillStyle = "#fff";
            ctx.fill();
        }
    }
}
/**
 * Draw simple sparkline (for compact view)
 */
function drawSparkline(ctx, data, x, y, width, height, color) {
    drawLineChart(ctx, data, x, y, width, height, color, false, false);
}
/**
 * Render COMPACT overlay - XP/hr with sparkline (like RuneLite)
 */
function renderCompactOverlay(xpPerHour, elapsedMs, sparklineData, primarySkillColor = "#00ff88", config = {}) {
    const width = 220;
    const height = 56;
    const dpr = getDPR();
    const canvas = document.createElement("canvas");
    canvas.width = Math.round(width * dpr);
    canvas.height = Math.round(height * dpr);
    const ctx = canvas.getContext("2d", { alpha: true });
    ctx.scale(dpr, dpr);
    const fontFamily = config.fontFamily ?? DEFAULT_CONFIG.fontFamily;
    const pad = 10;
    // Clear canvas
    ctx.clearRect(0, 0, width, height);
    // Draw filled rounded rectangle background
    roundedRect(ctx, 0, 0, width, height, 8);
    ctx.fillStyle = "rgba(50, 50, 55, 0.95)";
    ctx.fill();
    // Draw border on top
    roundedRect(ctx, 1, 1, width - 2, height - 2, 7);
    ctx.strokeStyle = primarySkillColor;
    ctx.lineWidth = 2;
    ctx.stroke();
    // Sparkline graph - right side (draw first so text overlaps if needed)
    const graphWidth = 70;
    const graphHeight = height - 16;
    const graphX = width - graphWidth - 8;
    const graphY = 8;
    // Graph background
    ctx.fillStyle = "rgba(255, 255, 255, 0.06)";
    roundedRect(ctx, graphX, graphY, graphWidth, graphHeight, 4);
    ctx.fill();
    // Draw sparkline or placeholder
    if (sparklineData.length >= 2) {
        drawSparkline(ctx, sparklineData, graphX + 3, graphY + 3, graphWidth - 6, graphHeight - 6, primarySkillColor);
    }
    else {
        // Draw a flat line placeholder
        ctx.strokeStyle = primarySkillColor + "60";
        ctx.lineWidth = 2;
        ctx.beginPath();
        const midY = graphY + graphHeight / 2;
        ctx.moveTo(graphX + 6, midY);
        ctx.lineTo(graphX + graphWidth - 6, midY);
        ctx.stroke();
    }
    // XP/hr value - large text
    const xpDisplay = xpPerHour > 0 ? formatRateLarge(xpPerHour) : "--";
    ctx.fillStyle = "#ffffff";
    ctx.font = `bold 20px ${fontFamily}`;
    ctx.textAlign = "left";
    ctx.fillText(xpDisplay, pad, 24);
    // "xp/hr" label - same line, smaller
    if (xpPerHour > 0) {
        ctx.fillStyle = primarySkillColor;
        ctx.font = `bold 12px ${fontFamily}`;
        ctx.font = `bold 20px ${fontFamily}`;
        const xpTextWidth = ctx.measureText(xpDisplay).width;
        ctx.font = `bold 12px ${fontFamily}`;
        ctx.fillText("xp/hr", pad + xpTextWidth + 4, 24);
    }
    else {
        ctx.fillStyle = "#888";
        ctx.font = `12px ${fontFamily}`;
        ctx.fillText("waiting...", pad, 24);
    }
    // Session time - bottom left
    ctx.fillStyle = "#888";
    ctx.font = `12px ${fontFamily}`;
    ctx.textAlign = "left";
    ctx.fillText(formatTime(elapsedMs), pad, height - 10);
    return { canvas, width, height };
}
/**
 * Draw multi-line chart with multiple skill series
 */
function drawMultiLineChart(ctx, series, x, y, width, height, fontFamily) {
    if (series.length === 0)
        return;
    // Find all time points and value range
    let minTime = Infinity, maxTime = -Infinity;
    let maxVal = 0;
    for (const s of series) {
        for (const pt of s.data) {
            if (pt.time < minTime)
                minTime = pt.time;
            if (pt.time > maxTime)
                maxTime = pt.time;
            if (pt.value > maxVal)
                maxVal = pt.value;
        }
    }
    if (maxTime <= minTime || maxVal === 0)
        return;
    const timeRange = maxTime - minTime;
    const niceMax = maxVal * 1.1;
    // Draw Y-axis labels
    ctx.fillStyle = "#666";
    ctx.font = `9px ${fontFamily}`;
    ctx.textAlign = "right";
    const yLabels = [niceMax, niceMax / 2, 0];
    for (let i = 0; i < yLabels.length; i++) {
        const labelY = y + (i / 2) * height;
        ctx.fillText(formatRate(yLabels[i]).replace("/hr", ""), x - 4, labelY + 3);
        // Grid line
        ctx.strokeStyle = "#333";
        ctx.lineWidth = 0.5;
        ctx.beginPath();
        ctx.moveTo(x, labelY);
        ctx.lineTo(x + width, labelY);
        ctx.stroke();
    }
    // Draw each series
    for (const s of series) {
        if (s.data.length < 2)
            continue;
        // Calculate points
        const points = [];
        for (const pt of s.data) {
            const px = x + ((pt.time - minTime) / timeRange) * width;
            const py = y + height - (pt.value / niceMax) * height;
            points.push({ x: px, y: py });
        }
        // Draw line
        ctx.beginPath();
        ctx.moveTo(points[0].x, points[0].y);
        for (let i = 1; i < points.length; i++) {
            ctx.lineTo(points[i].x, points[i].y);
        }
        ctx.strokeStyle = s.color;
        ctx.lineWidth = 2;
        ctx.lineCap = "round";
        ctx.lineJoin = "round";
        ctx.stroke();
        // Draw dots if not too many points
        if (points.length <= 20) {
            for (const pt of points) {
                ctx.beginPath();
                ctx.arc(pt.x, pt.y, 3, 0, Math.PI * 2);
                ctx.fillStyle = s.color;
                ctx.fill();
                ctx.beginPath();
                ctx.arc(pt.x, pt.y, 1.5, 0, Math.PI * 2);
                ctx.fillStyle = "#1a1a2e";
                ctx.fill();
            }
        }
    }
    // Draw X-axis time labels
    ctx.fillStyle = "#666";
    ctx.font = `9px ${fontFamily}`;
    ctx.textAlign = "center";
    const numLabels = Math.min(5, Math.floor(width / 50));
    for (let i = 0; i < numLabels; i++) {
        const t = minTime + (i / (numLabels - 1)) * timeRange;
        const labelX = x + (i / (numLabels - 1)) * width;
        const totalSec = Math.floor(t / 1000);
        const min = Math.floor(totalSec / 60);
        const sec = totalSec % 60;
        const label = min > 0 ? `${min}:${sec.toString().padStart(2, '0')}` : `${sec}s`;
        ctx.fillText(label, labelX, y + height + 12);
    }
}
/**
 * Render EXPANDED overlay - Full stats with skill breakdown and multi-line chart
 */
function renderExpandedOverlay(totalXp, xpPerHour, rollingXpPerHour, elapsedMs, skillBreakdown, sparklineData, config = {}, skillRateHistory) {
    // Calculate height based on skills - larger chart area
    const numSkills = Math.min(skillBreakdown.length, 6);
    const hasMultiLineData = skillRateHistory && skillRateHistory.length > 0 && skillRateHistory.some(s => s.data.length >= 2);
    const graphHeight = hasMultiLineData ? 120 : 50; // Larger graph for multi-line
    const baseHeight = 100; // Header + main stats
    const skillRowHeight = 24;
    const height = baseHeight + graphHeight + 20 + numSkills * skillRowHeight;
    const width = hasMultiLineData ? 320 : 260;
    const dpr = getDPR();
    const canvas = document.createElement("canvas");
    canvas.width = Math.round(width * dpr);
    canvas.height = Math.round(height * dpr);
    const ctx = canvas.getContext("2d", { alpha: true });
    ctx.scale(dpr, dpr);
    const fontFamily = config.fontFamily ?? DEFAULT_CONFIG.fontFamily;
    const pad = 8;
    // Draw background
    roundedRect(ctx, 0, 0, width, height, 6);
    ctx.fillStyle = "rgba(0, 0, 0, 0.88)";
    ctx.fill();
    ctx.strokeStyle = "#00d4ff";
    ctx.lineWidth = 2;
    ctx.stroke();
    let y = pad;
    // === HEADER ROW ===
    // XP/hr - Large and prominent on left
    ctx.fillStyle = "#00ff88";
    ctx.font = `bold 20px ${fontFamily}`;
    ctx.textAlign = "left";
    ctx.fillText(formatRateLarge(xpPerHour), pad, y + 18);
    // "xp/hr" label
    ctx.fillStyle = "#00ff88";
    ctx.font = `bold 12px ${fontFamily}`;
    const rateText = ctx.measureText(formatRateLarge(xpPerHour));
    ctx.fillText("xp/hr", pad + rateText.width + 4, y + 18);
    // Session time on right
    ctx.fillStyle = "#aaaaaa";
    ctx.font = `13px ${fontFamily}`;
    ctx.textAlign = "right";
    ctx.fillText(formatTime(elapsedMs), width - pad, y + 18);
    y += 28;
    // === CHART AREA ===
    const chartLeftPad = hasMultiLineData ? 45 : pad; // More space for Y-axis labels
    const graphWidth = width - chartLeftPad - pad;
    const graphY = y;
    const actualGraphHeight = graphHeight - (hasMultiLineData ? 20 : 8); // Room for X-axis labels
    // Graph background
    ctx.fillStyle = "rgba(26, 26, 46, 0.95)";
    roundedRect(ctx, pad, graphY - 2, width - pad * 2, actualGraphHeight + (hasMultiLineData ? 18 : 4), 4);
    ctx.fill();
    // Draw multi-line chart if we have per-skill data
    if (hasMultiLineData && skillRateHistory) {
        drawMultiLineChart(ctx, skillRateHistory, chartLeftPad, graphY, graphWidth, actualGraphHeight, fontFamily);
        // Draw legend below chart
        const legendY = graphY + actualGraphHeight + 2;
        let legendX = chartLeftPad;
        ctx.font = `9px ${fontFamily}`;
        for (const s of skillRateHistory.slice(0, 4)) {
            // Color box
            ctx.fillStyle = s.color;
            ctx.fillRect(legendX, legendY, 8, 8);
            // Label
            ctx.fillStyle = "#888";
            ctx.textAlign = "left";
            const labelWidth = ctx.measureText(s.name).width;
            ctx.fillText(s.name, legendX + 10, legendY + 7);
            legendX += 10 + labelWidth + 12;
            if (legendX > width - 50)
                break;
        }
    }
    else if (sparklineData.length >= 2) {
        // Fallback to sparkline
        drawSparkline(ctx, sparklineData, chartLeftPad, graphY, graphWidth, actualGraphHeight, "#00ff88");
    }
    else {
        // No data yet message
        ctx.fillStyle = "#555";
        ctx.font = `11px ${fontFamily}`;
        ctx.textAlign = "center";
        ctx.fillText("Collecting data...", width / 2, graphY + actualGraphHeight / 2 + 4);
    }
    y += graphHeight;
    // === STATS ROW ===
    // Total XP on left
    ctx.fillStyle = "#888888";
    ctx.font = `11px ${fontFamily}`;
    ctx.textAlign = "left";
    ctx.fillText("Total:", pad, y + 4);
    ctx.fillStyle = "#ffffff";
    ctx.font = `bold 13px ${fontFamily}`;
    ctx.fillText(formatXp(totalXp), pad + 35, y + 4);
    // 5min rate on right
    if (rollingXpPerHour > 0) {
        ctx.fillStyle = "#888888";
        ctx.font = `11px ${fontFamily}`;
        ctx.textAlign = "right";
        ctx.fillText("5min:", width - pad - 60, y + 4);
        ctx.fillStyle = "#ffcc00";
        ctx.font = `bold 12px ${fontFamily}`;
        ctx.fillText(formatRate(rollingXpPerHour), width - pad, y + 4);
    }
    y += 16;
    // === DIVIDER ===
    if (numSkills > 0) {
        ctx.strokeStyle = "#333333";
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.moveTo(pad, y);
        ctx.lineTo(width - pad, y);
        ctx.stroke();
        y += 8;
    }
    // === SKILL BREAKDOWN ===
    const topSkills = skillBreakdown.slice(0, 6);
    for (const skill of topSkills) {
        // Color indicator
        ctx.fillStyle = skill.color;
        roundedRect(ctx, pad, y + 3, 3, 14, 1);
        ctx.fill();
        // Skill name
        ctx.fillStyle = "#cccccc";
        ctx.font = `12px ${fontFamily}`;
        ctx.textAlign = "left";
        ctx.fillText(skill.name, pad + 10, y + 14);
        // XP gained
        ctx.fillStyle = "#00ff88";
        ctx.font = `bold 12px ${fontFamily}`;
        ctx.textAlign = "center";
        ctx.fillText(formatXp(skill.xp), width / 2 + 10, y + 14);
        // XP/hr for this skill
        if (skill.xpPerHour > 0) {
            ctx.fillStyle = "#888888";
            ctx.font = `11px ${fontFamily}`;
            ctx.textAlign = "right";
            ctx.fillText(formatRate(skill.xpPerHour), width - pad, y + 14);
        }
        y += skillRowHeight;
    }
    return { canvas, width, height };
}
/**
 * Render XP stats panel - wrapper for backwards compatibility
 * Uses expanded view by default
 */
function renderXpStatsPanel(totalXp, xpPerHour, rollingXpPerHour, elapsedMs, skillBreakdown, config = {}, sparklineData = []) {
    return renderExpandedOverlay(totalXp, xpPerHour, rollingXpPerHour, elapsedMs, skillBreakdown, sparklineData, config);
}
/**
 * Render XP rate chart - backwards compat
 */
function renderXpRateChart(series, config = {}) {
    const totalXp = series.reduce((sum, s) => {
        const lastVal = s.data[s.data.length - 1]?.value ?? 0;
        return sum + lastVal;
    }, 0);
    const skillBreakdown = series.map(s => {
        const lastRate = s.data[s.data.length - 1]?.value ?? 0;
        return {
            name: s.name,
            xp: 0,
            xpPerHour: lastRate,
            color: s.color,
        };
    }).filter(s => s.xpPerHour > 0);
    return renderExpandedOverlay(0, totalXp, 0, 0, skillBreakdown, [], config);
}
/**
 * Render XP summary - backwards compat
 */
function renderXpSummary(totalXp, xpPerHour, rollingXpPerHour, elapsedMs, skillBreakdown, config = {}, sparklineData = []) {
    const skillsWithRate = skillBreakdown.map(s => ({
        ...s,
        xpPerHour: elapsedMs > 0 ? (s.xp / elapsedMs) * 3600000 : 0,
    }));
    return renderExpandedOverlay(totalXp, xpPerHour, rollingXpPerHour, elapsedMs, skillsWithRate, sparklineData, config);
}


/***/ },

/***/ "./overlay/DebugZoneOverlay.ts"
/*!*************************************!*\
  !*** ./overlay/DebugZoneOverlay.ts ***!
  \*************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   DebugZoneOverlay: () => (/* binding */ DebugZoneOverlay)
/* harmony export */ });
/* harmony import */ var _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../util/patchrs_napi */ "./util/patchrs_napi.ts");
/* harmony import */ var _XpChartOverlay__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./XpChartOverlay */ "./overlay/XpChartOverlay.ts");
/**
 * DebugZoneOverlay - Draws a debug rectangle on the game screen
 * Shows the XP detection zone with a colored border and semi-transparent fill.
 */


// OpenGL constants
const GL_FLOAT = 0x1406;
const GL_FLOAT_VEC2 = 0x8b50;
const GL_FLOAT_VEC4 = 0x8b52;
// Vertex shader - screen space quad
const vertShader = `
  #version 330 core
  layout (location = 0) in vec2 aPos;

  uniform vec2 uScreenSize;
  uniform vec2 uPosition;
  uniform vec2 uSize;
  uniform float uFlipY;

  out vec2 vLocalPos;

  void main() {
    vec2 scaledPos = aPos * uSize;
    vec2 screenPos = scaledPos + uPosition;
    vec2 ndc = (screenPos / uScreenSize) * 2.0 - 1.0;
    if (uFlipY > 0.5) {
      ndc.y = -ndc.y;
    }
    gl_Position = vec4(ndc, 0.0, 1.0);
    vLocalPos = aPos * uSize; // pixel position within the quad
  }
`;
// Fragment shader - bordered rectangle with semi-transparent fill
const fragShader = `
  #version 330 core
  in vec2 vLocalPos;

  uniform vec2 uSize;
  uniform vec4 uBorderColor;
  uniform vec4 uFillColor;

  out vec4 FragColor;

  void main() {
    float borderWidth = 2.0;

    // Check if we're on the border
    bool onBorder = vLocalPos.x < borderWidth || vLocalPos.x > uSize.x - borderWidth
                 || vLocalPos.y < borderWidth || vLocalPos.y > uSize.y - borderWidth;

    if (onBorder) {
      FragColor = uBorderColor;
    } else {
      FragColor = uFillColor;
    }
  }
`;
// Uniform buffer layout:
// uScreenSize: vec2, offset 0 (8 bytes)
// uPosition: vec2, offset 8 (8 bytes)
// uSize: vec2, offset 16 (8 bytes)
// uFlipY: float, offset 24 (4 bytes)
// padding: 4 bytes at offset 28
// uBorderColor: vec4, offset 32 (16 bytes)
// uFillColor: vec4, offset 48 (16 bytes)
// Total: 64 bytes
const UNIFORM_BUFFER_SIZE = 64;
class DebugZoneOverlay {
    constructor() {
        this.program = null;
        this.vertexArray = null;
        this.overlayHandle = null;
        this.zone = { x: 400, y: 30, width: 600, height: 170 };
        this.visible = false;
    }
    createProgram() {
        return _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createProgram(vertShader, fragShader, [
            { location: 0, name: "aPos", type: GL_FLOAT, length: 2 },
        ], [
            { name: "uScreenSize", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 0, snapshotSize: 8 },
            { name: "uPosition", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 8, snapshotSize: 8 },
            { name: "uSize", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 16, snapshotSize: 8 },
            { name: "uFlipY", length: 1, type: GL_FLOAT, snapshotOffset: 24, snapshotSize: 4 },
            // 4 bytes padding at 28
            { name: "uBorderColor", length: 1, type: GL_FLOAT_VEC4, snapshotOffset: 32, snapshotSize: 16 },
            { name: "uFillColor", length: 1, type: GL_FLOAT_VEC4, snapshotOffset: 48, snapshotSize: 16 },
        ]);
    }
    createVertexArray() {
        const vertices = new Float32Array([
            0, 0, // Top-left
            1, 0, // Top-right
            1, 1, // Bottom-right
            0, 1, // Bottom-left
        ]);
        const indices = new Uint16Array([0, 3, 2, 0, 2, 1]);
        return _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createVertexArray(new Uint8Array(indices.buffer), [{
                location: 0,
                buffer: new Uint8Array(vertices.buffer),
                enabled: true,
                normalized: false,
                offset: 0,
                scalartype: GL_FLOAT,
                stride: 8,
                vectorlength: 2,
            }]);
    }
    buildUniformBuffer() {
        const buf = new ArrayBuffer(UNIFORM_BUFFER_SIZE);
        const view = new DataView(buf);
        const scaleState = (0,_XpChartOverlay__WEBPACK_IMPORTED_MODULE_1__.getUIScaleState)();
        const screenWidth = scaleState.uiWidth || scaleState.screenWidth || 1920;
        const screenHeight = scaleState.uiHeight || scaleState.screenHeight || 1080;
        const flipY = scaleState.isScaled ? 0.0 : 1.0;
        // uScreenSize
        view.setFloat32(0, screenWidth, true);
        view.setFloat32(4, screenHeight, true);
        // uPosition
        view.setFloat32(8, this.zone.x, true);
        view.setFloat32(12, this.zone.y, true);
        // uSize
        view.setFloat32(16, this.zone.width, true);
        view.setFloat32(20, this.zone.height, true);
        // uFlipY
        view.setFloat32(24, flipY, true);
        // padding at 28
        // uBorderColor (bright green, 90% alpha)
        view.setFloat32(32, 0.0, true); // R
        view.setFloat32(36, 1.0, true); // G
        view.setFloat32(40, 0.0, true); // B
        view.setFloat32(44, 0.9, true); // A
        // uFillColor (green tint, 10% alpha)
        view.setFloat32(48, 0.0, true); // R
        view.setFloat32(52, 1.0, true); // G
        view.setFloat32(56, 0.0, true); // B
        view.setFloat32(60, 0.1, true); // A
        return buf;
    }
    async show(zone) {
        if (zone)
            this.zone = { ...zone };
        this.stopOverlay();
        if (!this.program) {
            this.program = this.createProgram();
        }
        if (!this.vertexArray) {
            this.vertexArray = this.createVertexArray();
        }
        const uniformBuffer = this.buildUniformBuffer();
        try {
            this.overlayHandle = _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.beginOverlay({}, this.program, this.vertexArray, {
                uniformSources: [],
                uniformBuffer: new Uint8Array(uniformBuffer),
                renderMode: "triangles",
                trigger: "frameend",
                alphaBlend: true,
            });
            this.visible = true;
            console.log(`[DebugZone] Showing zone at (${this.zone.x}, ${this.zone.y}) ${this.zone.width}x${this.zone.height}`);
        }
        catch (e) {
            console.error("[DebugZone] Failed to create overlay:", e);
        }
    }
    async updateZone(zone) {
        this.zone = { ...zone };
        if (this.visible && this.overlayHandle) {
            // Update uniforms in place
            const uniformBuffer = this.buildUniformBuffer();
            try {
                this.overlayHandle.setUniformState(new Uint8Array(uniformBuffer));
            }
            catch {
                // If setUniformState fails, recreate
                await this.show(zone);
            }
        }
    }
    hide() {
        this.stopOverlay();
        this.visible = false;
    }
    isShowing() {
        return this.visible;
    }
    getZone() {
        return { ...this.zone };
    }
    stopOverlay() {
        if (this.overlayHandle) {
            try {
                this.overlayHandle.stop();
            }
            catch {
                // ignore
            }
            this.overlayHandle = null;
        }
    }
    dispose() {
        this.hide();
        this.program = null;
        this.vertexArray = null;
    }
}


/***/ },

/***/ "./overlay/OverlayPositionEditor.tsx"
/*!*******************************************!*\
  !*** ./overlay/OverlayPositionEditor.tsx ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react/jsx-runtime */ "../node_modules/react/jsx-runtime.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! react */ "../node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_1__);

/**
 * OverlayPositionEditor - Visual drag-to-position editor for the XP overlay
 *
 * Displays a preview of the overlay position on a miniature game screen representation.
 * Users can drag the overlay box to reposition it with snap-to-edge functionality.
 */

// Snap distance threshold in screen pixels
const SNAP_THRESHOLD = 15;
// Common game resolutions
const COMMON_RESOLUTIONS = [
    { label: "1080p", width: 1920, height: 1080 },
    { label: "1440p", width: 2560, height: 1440 },
    { label: "4K", width: 3840, height: 2160 },
    { label: "720p", width: 1280, height: 720 },
    { label: "900p", width: 1600, height: 900 },
];
const OverlayPositionEditor = ({ positionX, positionY, onPositionChange, screenWidth: rawScreenWidth = 1920, screenHeight: rawScreenHeight = 1080, overlayWidth: rawOverlayWidth = 280, overlayHeight: rawOverlayHeight = 200, isVisible = false, }) => {
    const containerRef = (0,react__WEBPACK_IMPORTED_MODULE_1__.useRef)(null);
    const [isDragging, setIsDragging] = (0,react__WEBPACK_IMPORTED_MODULE_1__.useState)(false);
    const [dragOffset, setDragOffset] = (0,react__WEBPACK_IMPORTED_MODULE_1__.useState)({ x: 0, y: 0 });
    const [snapEnabled, setSnapEnabled] = (0,react__WEBPACK_IMPORTED_MODULE_1__.useState)(true);
    const [activeSnap, setActiveSnap] = (0,react__WEBPACK_IMPORTED_MODULE_1__.useState)({ x: null, y: null });
    // Sanitize inputs - ensure valid screen dimensions
    const screenWidth = rawScreenWidth > 0 ? rawScreenWidth : 1920;
    const screenHeight = rawScreenHeight > 0 ? rawScreenHeight : 1080;
    const overlayWidth = rawOverlayWidth > 0 ? rawOverlayWidth : 200;
    const overlayHeight = rawOverlayHeight > 0 ? rawOverlayHeight : 100;
    // Detect if screen size seems wrong (e.g., 0x0 or very small)
    const screenSizeWarning = rawScreenWidth < 800 || rawScreenHeight < 600;
    // Calculate aspect ratio and preview dimensions
    const aspectRatio = screenWidth / screenHeight;
    const previewWidth = 320;
    const previewHeight = Math.round(previewWidth / aspectRatio);
    // Scale factors
    const scaleX = previewWidth / screenWidth;
    const scaleY = previewHeight / screenHeight;
    // Scaled overlay dimensions
    const scaledOverlayWidth = overlayWidth * scaleX;
    const scaledOverlayHeight = overlayHeight * scaleY;
    // Generate snap points based on screen dimensions and overlay size
    const snapPoints = (0,react__WEBPACK_IMPORTED_MODULE_1__.useMemo)(() => {
        const points = [];
        // Edge snap points (X axis - left/right edges)
        points.push({ position: 10, type: "edge", axis: "x", label: "Left" });
        points.push({ position: screenWidth - overlayWidth - 10, type: "edge", axis: "x", label: "Right" });
        // Edge snap points (Y axis - top/bottom edges)
        points.push({ position: 10, type: "edge", axis: "y", label: "Top" });
        points.push({ position: screenHeight - overlayHeight - 10, type: "edge", axis: "y", label: "Bottom" });
        // Center snap points
        points.push({ position: (screenWidth - overlayWidth) / 2, type: "center", axis: "x", label: "Center" });
        points.push({ position: (screenHeight - overlayHeight) / 2, type: "center", axis: "y", label: "Center" });
        // Quarter snap points
        points.push({ position: screenWidth * 0.25 - overlayWidth / 2, type: "quarter", axis: "x", label: "25%" });
        points.push({ position: screenWidth * 0.75 - overlayWidth / 2, type: "quarter", axis: "x", label: "75%" });
        points.push({ position: screenHeight * 0.25 - overlayHeight / 2, type: "quarter", axis: "y", label: "25%" });
        points.push({ position: screenHeight * 0.75 - overlayHeight / 2, type: "quarter", axis: "y", label: "75%" });
        return points;
    }, [screenWidth, screenHeight, overlayWidth, overlayHeight]);
    // Find nearest snap point within threshold
    const findSnap = (0,react__WEBPACK_IMPORTED_MODULE_1__.useCallback)((value, axis) => {
        if (!snapEnabled)
            return null;
        const axisPoints = snapPoints.filter((p) => p.axis === axis);
        let nearestSnap = null;
        let nearestDistance = SNAP_THRESHOLD;
        for (const point of axisPoints) {
            const distance = Math.abs(value - point.position);
            if (distance < nearestDistance) {
                nearestDistance = distance;
                nearestSnap = point;
            }
        }
        return nearestSnap;
    }, [snapEnabled, snapPoints]);
    // Scaled position
    const scaledX = positionX * scaleX;
    const scaledY = positionY * scaleY;
    const handleMouseDown = (0,react__WEBPACK_IMPORTED_MODULE_1__.useCallback)((e) => {
        e.preventDefault();
        const rect = e.target.getBoundingClientRect();
        setDragOffset({
            x: e.clientX - rect.left,
            y: e.clientY - rect.top,
        });
        setIsDragging(true);
    }, []);
    const handleMouseMove = (0,react__WEBPACK_IMPORTED_MODULE_1__.useCallback)((e) => {
        if (!isDragging || !containerRef.current)
            return;
        const containerRect = containerRef.current.getBoundingClientRect();
        // Calculate new position in preview coordinates
        let newScaledX = e.clientX - containerRect.left - dragOffset.x;
        let newScaledY = e.clientY - containerRect.top - dragOffset.y;
        // Clamp to preview bounds
        newScaledX = Math.max(0, Math.min(newScaledX, previewWidth - scaledOverlayWidth));
        newScaledY = Math.max(0, Math.min(newScaledY, previewHeight - scaledOverlayHeight));
        // Convert back to screen coordinates (no rounding for smooth movement)
        let newX = newScaledX / scaleX;
        let newY = newScaledY / scaleY;
        // Apply snap detection
        const snapX = findSnap(newX, "x");
        const snapY = findSnap(newY, "y");
        if (snapX) {
            newX = snapX.position;
        }
        if (snapY) {
            newY = snapY.position;
        }
        // Update active snap state for visual feedback
        setActiveSnap({ x: snapX, y: snapY });
        onPositionChange(newX, newY);
    }, [isDragging, dragOffset, previewWidth, previewHeight, scaledOverlayWidth, scaledOverlayHeight, scaleX, scaleY, onPositionChange, findSnap]);
    const handleMouseUp = (0,react__WEBPACK_IMPORTED_MODULE_1__.useCallback)(() => {
        setIsDragging(false);
        setActiveSnap({ x: null, y: null });
    }, []);
    // Add/remove global mouse listeners for dragging
    (0,react__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
        if (isDragging) {
            window.addEventListener("mousemove", handleMouseMove);
            window.addEventListener("mouseup", handleMouseUp);
        }
        return () => {
            window.removeEventListener("mousemove", handleMouseMove);
            window.removeEventListener("mouseup", handleMouseUp);
        };
    }, [isDragging, handleMouseMove, handleMouseUp]);
    // Preset positions
    const presets = [
        { label: "TL", title: "Top Left", x: 10, y: 10 },
        { label: "TR", title: "Top Right", x: screenWidth - overlayWidth - 10, y: 10 },
        { label: "BL", title: "Bottom Left", x: 10, y: screenHeight - overlayHeight - 10 },
        { label: "BR", title: "Bottom Right", x: screenWidth - overlayWidth - 10, y: screenHeight - overlayHeight - 10 },
        { label: "C", title: "Center", x: (screenWidth - overlayWidth) / 2, y: (screenHeight - overlayHeight) / 2 },
    ];
    // Snap line color based on type
    const getSnapLineColor = (type) => {
        switch (type) {
            case "edge":
                return "rgba(100, 200, 255, 0.8)";
            case "center":
                return "rgba(255, 200, 100, 0.8)";
            case "quarter":
                return "rgba(150, 255, 150, 0.6)";
        }
    };
    const styles = {
        container: {
            display: "flex",
            flexDirection: "column",
            gap: "8px",
        },
        controls: {
            display: "flex",
            alignItems: "center",
            gap: "8px",
            fontSize: "11px",
        },
        snapToggle: {
            display: "flex",
            alignItems: "center",
            gap: "4px",
            color: "#888",
            cursor: "pointer",
        },
        checkbox: {
            cursor: "pointer",
        },
        resolution: {
            marginLeft: "auto",
            color: "#666",
        },
        preview: {
            width: previewWidth,
            height: previewHeight,
            position: "relative",
            backgroundColor: "#0a0a1a",
            border: "1px solid #333",
            borderRadius: "6px",
            overflow: "hidden",
            cursor: isDragging ? "grabbing" : "default",
            userSelect: "none",
        },
        overlayBox: {
            position: "absolute",
            left: scaledX,
            top: scaledY,
            width: scaledOverlayWidth,
            height: scaledOverlayHeight,
            backgroundColor: "rgba(0, 0, 0, 0.9)",
            border: "2px solid #00d4ff",
            borderRadius: "4px",
            cursor: isDragging ? "grabbing" : "grab",
            display: "flex",
            flexDirection: "column",
            padding: "4px",
            boxShadow: isDragging
                ? "0 0 15px rgba(0, 212, 255, 0.6)"
                : "0 2px 8px rgba(0, 0, 0, 0.5)",
        },
        overlayTitle: {
            color: "#00d4ff",
            fontSize: "7px",
            fontWeight: "bold",
        },
        overlayContent: {
            color: "#888",
            fontSize: "6px",
            marginTop: "2px",
        },
        positionIndicator: {
            position: "absolute",
            bottom: "4px",
            right: "8px",
            fontSize: "10px",
            backgroundColor: "rgba(0, 0, 0, 0.8)",
            color: "#666",
            padding: "2px 6px",
            borderRadius: "3px",
        },
        snapIndicator: {
            position: "absolute",
            top: "4px",
            left: "8px",
            fontSize: "10px",
            backgroundColor: "rgba(100, 200, 255, 0.9)",
            color: "#000",
            padding: "2px 6px",
            borderRadius: "3px",
            fontWeight: "bold",
        },
        presets: {
            display: "flex",
            gap: "4px",
            justifyContent: "space-between",
        },
        presetButton: {
            flex: 1,
            padding: "6px 8px",
            fontSize: "11px",
            backgroundColor: "#1a1a3a",
            color: "#00d4ff",
            border: "1px solid #333",
            borderRadius: "4px",
            cursor: "pointer",
        },
    };
    return ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.container, children: [screenSizeWarning && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: {
                    backgroundColor: "rgba(255, 150, 50, 0.2)",
                    border: "1px solid #ffaa00",
                    borderRadius: "4px",
                    padding: "6px 8px",
                    marginBottom: "8px",
                    fontSize: "11px",
                    color: "#ffaa00",
                }, children: "Screen size not detected. Using default 1920x1080. Show the overlay on game first." })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.controls, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("label", { style: styles.snapToggle, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("input", { type: "checkbox", checked: snapEnabled, onChange: (e) => setSnapEnabled(e.target.checked), style: styles.checkbox }), "Snap"] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("span", { style: {
                            ...styles.resolution,
                            color: screenSizeWarning ? "#ffaa00" : "#666",
                        }, children: [screenWidth, "x", screenHeight] })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { ref: containerRef, style: styles.preview, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: {
                            position: "absolute",
                            inset: 0,
                            background: "linear-gradient(135deg, #0a0a1a 0%, #12122a 100%)",
                        } }), (activeSnap.x || activeSnap.y) && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("svg", { style: { position: "absolute", inset: 0, pointerEvents: "none" }, width: previewWidth, height: previewHeight, children: [activeSnap.x && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("line", { x1: (activeSnap.x.position + overlayWidth / 2) * scaleX, y1: 0, x2: (activeSnap.x.position + overlayWidth / 2) * scaleX, y2: previewHeight, stroke: getSnapLineColor(activeSnap.x.type), strokeWidth: 2 })), activeSnap.y && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("line", { x1: 0, y1: (activeSnap.y.position + overlayHeight / 2) * scaleY, x2: previewWidth, y2: (activeSnap.y.position + overlayHeight / 2) * scaleY, stroke: getSnapLineColor(activeSnap.y.type), strokeWidth: 2 }))] })), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { onMouseDown: handleMouseDown, style: styles.overlayBox, children: [(0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.overlayTitle, children: "XP Tracker" }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("span", { style: styles.overlayContent, children: isVisible ? "Overlay visible" : "Drag to position" })] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.positionIndicator, children: [Math.round(positionX), ", ", Math.round(positionY)] }), (activeSnap.x || activeSnap.y) && ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsxs)("div", { style: styles.snapIndicator, children: ["Snap: ", [activeSnap.x?.label, activeSnap.y?.label].filter(Boolean).join(" + ")] }))] }), (0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("div", { style: styles.presets, children: presets.map((preset) => ((0,react_jsx_runtime__WEBPACK_IMPORTED_MODULE_0__.jsx)("button", { style: styles.presetButton, title: preset.title, onClick: () => onPositionChange(Math.round(preset.x), Math.round(preset.y)), children: preset.label }, preset.label))) })] }));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (OverlayPositionEditor);


/***/ },

/***/ "./overlay/XpChartOverlay.ts"
/*!***********************************!*\
  !*** ./overlay/XpChartOverlay.ts ***!
  \***********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   XpChartOverlay: () => (/* binding */ XpChartOverlay),
/* harmony export */   getUIScaleInfo: () => (/* binding */ getUIScaleInfo),
/* harmony export */   getUIScaleState: () => (/* binding */ getUIScaleState),
/* harmony export */   getXpChartOverlay: () => (/* binding */ getXpChartOverlay),
/* harmony export */   setUIScaleState: () => (/* binding */ setUIScaleState)
/* harmony export */ });
/* harmony import */ var _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../util/patchrs_napi */ "./util/patchrs_napi.ts");
/* harmony import */ var _render_renderprogram__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../render/renderprogram */ "./render/renderprogram.ts");
/**
 * XP Chart Overlay - Renders XP charts as moveable GL overlays
 * Supports 4K scaling and resolution-aware position persistence
 */


// OpenGL constants
const GL_FLOAT = 0x1406;
const GL_FLOAT_VEC2 = 0x8b50;
const GL_FLOAT_VEC4 = 0x8b52;
const GL_SAMPLER_2D = 0x8b5e;
const GL_INT = 0x1404;
// Max data points for chart (fixed size for shader uniform array)
const MAX_DATA_POINTS = 64;
// Position storage key prefix
const POSITION_STORAGE_KEY_PREFIX = "xpChartOverlay:position:";
// Vertex shader for chart quad
const vertShader = `
  #version 330 core
  layout (location = 0) in vec2 aPos;
  layout (location = 1) in vec2 aUV;

  uniform vec2 uScreenSize;
  uniform vec2 uPosition;
  uniform vec2 uSize;
  uniform float uFlipY;

  out vec2 vUV;
  out vec2 vTextUV;
  out vec2 vPixelPos;

  void main() {
    vec2 scaledPos = aPos * uSize;
    vec2 screenPos = scaledPos + uPosition;
    vec2 ndc = (screenPos / uScreenSize) * 2.0 - 1.0;
    if (uFlipY > 0.5) {
      ndc.y = -ndc.y;
    }
    gl_Position = vec4(ndc, 0.0, 1.0);
    vUV = aUV;
    // For text texture: flip V when in 4K mode (uFlipY = 0) to correct orientation
    vTextUV = vec2(aUV.x, uFlipY > 0.5 ? aUV.y : 1.0 - aUV.y);
    // For chart content: flip Y pixel position in 4K mode so chart renders right-side up
    vec2 pixelPos = aPos * uSize;
    vPixelPos = vec2(pixelPos.x, uFlipY > 0.5 ? pixelPos.y : uSize.y - pixelPos.y);
  }
`;
// Fragment shader - draws chart + text texture overlay
const fragShader = `
  #version 330 core
  in vec2 vUV;
  in vec2 vTextUV;
  in vec2 vPixelPos;

  uniform vec2 uSize;           // Overlay size in pixels
  uniform vec4 uBgColor;        // Background color
  uniform vec4 uLineColor;      // Line/border color
  uniform vec4 uGridColor;      // Grid line color
  uniform float uDataPoints[${MAX_DATA_POINTS}];  // Normalized data values (0-1)
  uniform int uDataCount;       // Actual number of data points
  uniform float uCornerRadius;  // Corner radius in pixels
  uniform sampler2D uTextTex;   // Text overlay texture

  out vec4 FragColor;

  // Rounded box SDF
  float roundedBoxSDF(vec2 p, vec2 b, float r) {
    vec2 q = abs(p) - b + r;
    return min(max(q.x, q.y), 0.0) + length(max(q, 0.0)) - r;
  }

  // Line segment distance
  float lineSegmentDist(vec2 p, vec2 a, vec2 b) {
    vec2 pa = p - a;
    vec2 ba = b - a;
    float h = clamp(dot(pa, ba) / dot(ba, ba), 0.0, 1.0);
    return length(pa - ba * h);
  }

  void main() {
    vec2 pixelSize = uSize;
    vec2 center = pixelSize * 0.5;
    vec2 halfSize = center;

    // Chart area margins (bottom for text info)
    float marginLeft = 6.0;
    float marginBottom = 24.0;  // Space for XP/hr and time text
    float marginTop = 6.0;
    float marginRight = 6.0;

    // Chart area size
    vec2 chartSize = vec2(pixelSize.x - marginLeft - marginRight, pixelSize.y - marginTop - marginBottom);

    // Position relative to center for rounded box
    vec2 posFromCenter = vPixelPos - center;

    // Rounded rectangle background
    float boxDist = roundedBoxSDF(posFromCenter, halfSize - 1.0, uCornerRadius);
    float boxMask = 1.0 - smoothstep(-1.0, 1.0, boxDist);

    // Border
    float borderDist = abs(boxDist) - 1.5;
    float borderMask = 1.0 - smoothstep(-1.0, 1.0, borderDist);

    // Start with background
    vec3 color = uBgColor.rgb;
    float alpha = uBgColor.a * boxMask;

    // Chart position: X from left margin, Y flipped (0 at bottom of chart)
    vec2 chartPos = vec2(
      vPixelPos.x - marginLeft,
      (pixelSize.y - marginBottom) - vPixelPos.y  // Flip Y so 0 is at bottom
    );

    bool inChart = chartPos.x >= 0.0 && chartPos.x <= chartSize.x &&
                   chartPos.y >= 0.0 && chartPos.y <= chartSize.y;

    // Draw grid lines
    if (inChart) {
      // Horizontal grid lines (3 lines)
      for (int i = 0; i <= 2; i++) {
        float gridY = chartSize.y * float(i) / 2.0;
        float gridDist = abs(chartPos.y - gridY);
        if (gridDist < 0.5) {
          color = mix(color, uGridColor.rgb, 0.3);
        }
      }

      // Vertical grid lines
      int numVertLines = max(uDataCount - 1, 4);
      for (int i = 0; i <= numVertLines; i++) {
        float gridX = chartSize.x * float(i) / float(numVertLines);
        float gridDist = abs(chartPos.x - gridX);
        if (gridDist < 0.5) {
          color = mix(color, uGridColor.rgb, 0.2);
        }
      }
    }

    // Draw the data line
    if (uDataCount >= 2) {
      float minDist = 1000.0;

      for (int i = 0; i < ${MAX_DATA_POINTS} - 1; i++) {
        if (i >= uDataCount - 1) break;

        float x0 = chartSize.x * float(i) / float(uDataCount - 1);
        float y0 = chartSize.y * uDataPoints[i];
        float x1 = chartSize.x * float(i + 1) / float(uDataCount - 1);
        float y1 = chartSize.y * uDataPoints[i + 1];

        float d = lineSegmentDist(chartPos, vec2(x0, y0), vec2(x1, y1));
        minDist = min(minDist, d);
      }

      // Line with subtle glow
      float lineWidth = 2.0;
      float lineMask = 1.0 - smoothstep(lineWidth - 0.5, lineWidth + 0.5, minDist);
      float glowMask = 1.0 - smoothstep(lineWidth, lineWidth + 6.0, minDist);

      color = mix(color, uLineColor.rgb, glowMask * 0.15);
      color = mix(color, uLineColor.rgb, lineMask);
    }

    // Apply border
    color = mix(color, uLineColor.rgb, borderMask * boxMask);

    // Sample text texture (vTextUV has correct orientation for both 1080p and 4K)
    vec4 textSample = texture(uTextTex, vTextUV);

    // Alpha blend text over chart
    color = mix(color, textSample.rgb, textSample.a);
    float finalAlpha = alpha * boxMask + textSample.a * (1.0 - alpha * boxMask);

    FragColor = vec4(color, max(alpha * boxMask, textSample.a));
  }
`;
// Simple texture-only fragment shader for expanded mode (renders pre-drawn canvas)
const fragShaderTextureOnly = `
  #version 330 core
  in vec2 vUV;
  in vec2 vTextUV;
  in vec2 vPixelPos;

  uniform vec2 uSize;
  uniform float uCornerRadius;
  uniform sampler2D uTextTex;   // Pre-rendered canvas texture

  out vec4 FragColor;

  // Rounded box SDF
  float roundedBoxSDF(vec2 p, vec2 b, float r) {
    vec2 q = abs(p) - b + r;
    return min(max(q.x, q.y), 0.0) + length(max(q, 0.0)) - r;
  }

  void main() {
    vec2 center = uSize * 0.5;
    vec2 posFromCenter = vPixelPos - center;

    // Rounded rectangle mask
    float boxDist = roundedBoxSDF(posFromCenter, center - 1.0, uCornerRadius);
    float boxMask = 1.0 - smoothstep(-1.0, 1.0, boxDist);

    // Sample the pre-rendered canvas texture
    vec4 texColor = texture(uTextTex, vTextUV);

    // Apply rounded corners
    FragColor = vec4(texColor.rgb, texColor.a * boxMask);
  }
`;
/**
 * Format number with K/M suffix
 */
function formatXpRate(xpPerHour) {
    if (xpPerHour >= 1000000) {
        return (xpPerHour / 1000000).toFixed(1) + "M";
    }
    else if (xpPerHour >= 1000) {
        return (xpPerHour / 1000).toFixed(1) + "K";
    }
    return Math.round(xpPerHour).toString();
}
/**
 * Format elapsed time as HH:MM:SS or MM:SS
 */
function formatElapsedTime(elapsedMs) {
    const totalSeconds = Math.floor(elapsedMs / 1000);
    const hours = Math.floor(totalSeconds / 3600);
    const minutes = Math.floor((totalSeconds % 3600) / 60);
    const seconds = totalSeconds % 60;
    if (hours > 0) {
        return `${hours}:${minutes.toString().padStart(2, "0")}:${seconds.toString().padStart(2, "0")}`;
    }
    return `${minutes}:${seconds.toString().padStart(2, "0")}`;
}
/**
 * Create text overlay texture as ImageData
 */
function createTextOverlayImageData(width, height, xpPerHour, elapsedMs, lineColor) {
    // Create offscreen canvas
    const canvas = document.createElement("canvas");
    canvas.width = width;
    canvas.height = height;
    const ctx = canvas.getContext("2d");
    // Clear with transparent
    ctx.clearRect(0, 0, width, height);
    // Convert line color to brighter CSS (boost by 30%)
    const r = Math.min(255, Math.round(lineColor[0] * 255 * 1.3 + 50));
    const g = Math.min(255, Math.round(lineColor[1] * 255 * 1.3 + 50));
    const b = Math.min(255, Math.round(lineColor[2] * 255 * 1.3 + 50));
    const colorStr = `rgb(${r}, ${g}, ${b})`;
    // Draw XP/hr rate at bottom left
    ctx.font = "bold 13px 'Segoe UI', Arial, sans-serif";
    ctx.fillStyle = colorStr;
    ctx.textBaseline = "bottom";
    ctx.textAlign = "left";
    ctx.fillText(formatXpRate(xpPerHour) + "/hr", 8, height - 6);
    // Draw elapsed time at bottom right
    ctx.font = "12px 'Segoe UI', Arial, sans-serif";
    ctx.fillStyle = "rgba(255, 255, 255, 1.0)";
    ctx.textAlign = "right";
    ctx.fillText(formatElapsedTime(elapsedMs), width - 8, height - 6);
    return ctx.getImageData(0, 0, width, height);
}
// Default UI scale state
let uiScaleState = {
    isScaled: false,
    uiWidth: 1920,
    uiHeight: 1080,
    screenWidth: 1920,
    screenHeight: 1080,
    scaleX: 1,
    scaleY: 1,
    scalingTextureId: 0,
};
/**
 * Get current UI scale info
 */
function getUIScaleInfo() {
    return uiScaleState;
}
/**
 * Get current UI scale state (alias for getUIScaleInfo)
 */
function getUIScaleState() {
    return uiScaleState;
}
/**
 * Set UI scale state from external source
 */
function setUIScaleState(state) {
    uiScaleState = { ...uiScaleState, ...state };
}
/**
 * XP Chart Overlay class
 * Renders charts as textured GL overlays that can be positioned anywhere on screen
 */
class XpChartOverlay {
    constructor() {
        this.overlayHandle = null;
        this.position = { x: 50, y: 50 };
        this.size = { width: 300, height: 180 };
        this.uiSize = null;
        this.uiFramebufferInfo = null;
        this.isVisible = false;
        this.textureId = 0;
        this.program = null;
        this.programTextureOnly = null;
        this.vertexArray = null;
        this.lastRenderResult = null;
        this.lastChartData = null;
        this.textTexture = null;
        // Operation serialization to prevent race conditions
        this.pendingOperation = null;
        this.operationSeq = 0;
        this.position = this.loadPosition();
        this.updateUISize();
    }
    /**
     * Stop any existing overlay synchronously - call before creating new one
     */
    stopOverlaySync() {
        if (this.overlayHandle) {
            try {
                this.overlayHandle.stop();
            }
            catch (e) {
                console.warn("[XpChartOverlay] Error stopping overlay:", e);
            }
            this.overlayHandle = null;
        }
    }
    /**
     * Update UI size from current state
     */
    updateUISize() {
        this.uiSize = {
            width: uiScaleState.uiWidth,
            height: uiScaleState.uiHeight,
        };
    }
    /**
     * Get position storage key for current resolution
     */
    getPositionStorageKey() {
        const width = this.uiSize?.width ?? 1920;
        const height = this.uiSize?.height ?? 1080;
        return `${POSITION_STORAGE_KEY_PREFIX}${width}x${height}`;
    }
    /**
     * Load position from localStorage
     */
    loadPosition() {
        try {
            const key = this.getPositionStorageKey();
            const saved = localStorage.getItem(key);
            if (saved) {
                return JSON.parse(saved);
            }
        }
        catch (e) {
            console.warn("[XpChartOverlay] Failed to load position:", e);
        }
        return { x: 50, y: 50 };
    }
    /**
     * Save position to localStorage
     */
    savePosition() {
        try {
            const key = this.getPositionStorageKey();
            localStorage.setItem(key, JSON.stringify(this.position));
        }
        catch (e) {
            console.warn("[XpChartOverlay] Failed to save position:", e);
        }
    }
    /**
     * Set overlay position
     */
    setPosition(x, y) {
        // Clamp to valid bounds
        const maxX = (this.uiSize?.width ?? 1920) - this.size.width;
        const maxY = (this.uiSize?.height ?? 1080) - this.size.height;
        this.position = {
            x: Math.max(0, Math.min(x, maxX)),
            y: Math.max(0, Math.min(y, maxY)),
        };
        this.savePosition();
        // Recreate overlay at new position if visible
        if (this.isVisible && this.lastRenderResult) {
            // Hide and re-show to ensure clean state
            this.show(this.lastRenderResult).catch(e => {
                console.warn("[XpChartOverlay] Failed to update position:", e);
            });
        }
    }
    /**
     * Get current position
     */
    getPosition() {
        return { ...this.position };
    }
    /**
     * Get overlay size
     */
    getSize() {
        return { ...this.size };
    }
    /**
     * Get UI bounds
     */
    getUIBounds() {
        return {
            width: this.uiSize?.width ?? 1920,
            height: this.uiSize?.height ?? 1080,
        };
    }
    /**
     * Update position live without recreating overlay
     */
    updatePositionLive() {
        if (!this.overlayHandle || !this.isVisible)
            return;
        try {
            const uniformState = this.overlayHandle.getUniformState();
            const view = new DataView(uniformState.buffer, uniformState.byteOffset, uniformState.byteLength);
            // uPosition is at offset 8 (after uScreenSize which is 2 floats = 8 bytes)
            if (uiScaleState.isScaled && this.uiFramebufferInfo) {
                // 4K mode: use UI coordinates with Y flip (GL Y=0 at bottom)
                const glPositionY = this.uiFramebufferInfo.height - this.position.y - this.size.height;
                view.setFloat32(8, this.position.x, true);
                view.setFloat32(12, glPositionY, true);
            }
            else {
                // 1080p mode: direct screen coordinates
                view.setFloat32(8, this.position.x, true);
                view.setFloat32(12, this.position.y, true);
            }
            this.overlayHandle.setUniformState(uniformState);
        }
        catch (e) {
            console.warn("[XpChartOverlay] Live position update failed:", e);
        }
    }
    /**
     * Find UI framebuffer from render data
     */
    async findUIFramebuffer() {
        try {
            const screenRenders = await _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.recordRenderCalls({
                maxframes: 1,
                features: ["texturesnapshot"],
                framebufferId: 0,
            });
            // Find Lanczos scaler program
            let scalingTextureId = 0;
            for (const render of screenRenders) {
                if (!render.program)
                    continue;
                const progMeta = (0,_render_renderprogram__WEBPACK_IMPORTED_MODULE_1__.getProgramMeta)(render.program);
                if (progMeta.isUiScaler) {
                    const textureData = render.samplers || render.textures || {};
                    const sampler = Object.values(textureData)[0];
                    if (sampler) {
                        scalingTextureId = sampler.texid;
                        break;
                    }
                }
            }
            if (scalingTextureId > 0) {
                const uiRenders = await _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.recordRenderCalls({
                    maxframes: 1,
                    features: [],
                    framebufferTexture: scalingTextureId,
                });
                const uiRender = uiRenders.find(r => r.viewport);
                if (uiRender?.viewport) {
                    return {
                        framebufferId: uiRender.framebufferId,
                        width: uiRender.viewport.width,
                        height: uiRender.viewport.height,
                    };
                }
            }
            // Fallback: find UI program in screen renders
            for (const render of screenRenders) {
                if (!render.program)
                    continue;
                const progMeta = (0,_render_renderprogram__WEBPACK_IMPORTED_MODULE_1__.getProgramMeta)(render.program);
                if (progMeta.isUi && render.viewport) {
                    return {
                        framebufferId: render.framebufferId,
                        width: render.viewport.width,
                        height: render.viewport.height,
                    };
                }
            }
            // Final fallback
            const viewport = screenRenders.find(r => r.viewport)?.viewport;
            if (viewport) {
                return {
                    framebufferId: 0,
                    width: viewport.width,
                    height: viewport.height,
                };
            }
            return null;
        }
        catch (e) {
            console.warn("[XpChartOverlay] Failed to find UI framebuffer:", e);
            return null;
        }
    }
    /**
     * Create shader program for pure GL chart rendering
     * Uniform buffer layout:
     * - uScreenSize: vec2, offset 0
     * - uPosition: vec2, offset 8
     * - uSize: vec2, offset 16
     * - uFlipY: float, offset 24
     * - uCornerRadius: float, offset 28
     * - uBgColor: vec4, offset 32
     * - uLineColor: vec4, offset 48
     * - uGridColor: vec4, offset 64
     * - uDataCount: int, offset 80
     * - padding: 12 bytes, offset 84 (align to 16 for array)
     * - uDataPoints: float[64], offset 96
     * Total: 352 bytes
     */
    createProgram() {
        return _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createProgram(vertShader, fragShader, [
            { location: 0, name: "aPos", type: GL_FLOAT, length: 2 },
            { location: 1, name: "aUV", type: GL_FLOAT, length: 2 },
        ], [
            { name: "uScreenSize", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 0, snapshotSize: 8 },
            { name: "uPosition", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 8, snapshotSize: 8 },
            { name: "uSize", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 16, snapshotSize: 8 },
            { name: "uFlipY", length: 1, type: GL_FLOAT, snapshotOffset: 24, snapshotSize: 4 },
            { name: "uCornerRadius", length: 1, type: GL_FLOAT, snapshotOffset: 28, snapshotSize: 4 },
            { name: "uBgColor", length: 1, type: GL_FLOAT_VEC4, snapshotOffset: 32, snapshotSize: 16 },
            { name: "uLineColor", length: 1, type: GL_FLOAT_VEC4, snapshotOffset: 48, snapshotSize: 16 },
            { name: "uGridColor", length: 1, type: GL_FLOAT_VEC4, snapshotOffset: 64, snapshotSize: 16 },
            { name: "uDataCount", length: 1, type: GL_INT, snapshotOffset: 80, snapshotSize: 4 },
            // Padding at 84-95 (12 bytes) for 16-byte alignment
            { name: "uDataPoints", length: MAX_DATA_POINTS, type: GL_FLOAT, snapshotOffset: 96, snapshotSize: MAX_DATA_POINTS * 4 },
        ]);
    }
    /**
     * Create texture-only program for expanded mode (renders pre-drawn canvas)
     * Uniform buffer layout:
     * - uScreenSize: vec2, offset 0
     * - uPosition: vec2, offset 8
     * - uSize: vec2, offset 16
     * - uFlipY: float, offset 24
     * - uCornerRadius: float, offset 28
     * Total: 32 bytes
     */
    createProgramTextureOnly() {
        return _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createProgram(vertShader, fragShaderTextureOnly, [
            { location: 0, name: "aPos", type: GL_FLOAT, length: 2 },
            { location: 1, name: "aUV", type: GL_FLOAT, length: 2 },
        ], [
            { name: "uScreenSize", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 0, snapshotSize: 8 },
            { name: "uPosition", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 8, snapshotSize: 8 },
            { name: "uSize", length: 1, type: GL_FLOAT_VEC2, snapshotOffset: 16, snapshotSize: 8 },
            { name: "uFlipY", length: 1, type: GL_FLOAT, snapshotOffset: 24, snapshotSize: 4 },
            { name: "uCornerRadius", length: 1, type: GL_FLOAT, snapshotOffset: 28, snapshotSize: 4 },
        ]);
    }
    /**
     * Create vertex array for textured quad
     */
    createVertexArray() {
        // Unit quad with UV coordinates
        const vertices = new Float32Array([
            // Position  UV
            0, 0, 0, 0, // Top-left
            1, 0, 1, 0, // Top-right
            1, 1, 1, 1, // Bottom-right
            0, 1, 0, 1, // Bottom-left
        ]);
        // CCW winding
        const indices = new Uint16Array([
            0, 3, 2,
            0, 2, 1,
        ]);
        const posBuffer = new Uint8Array(vertices.buffer);
        return _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createVertexArray(new Uint8Array(indices.buffer), [
            {
                location: 0,
                buffer: posBuffer,
                enabled: true,
                normalized: false,
                offset: 0,
                scalartype: GL_FLOAT,
                stride: 16,
                vectorlength: 2,
            },
            {
                location: 1,
                buffer: posBuffer,
                enabled: true,
                normalized: false,
                offset: 8,
                scalartype: GL_FLOAT,
                stride: 16,
                vectorlength: 2,
            },
        ]);
    }
    /**
     * Build uniform buffer for chart shader
     */
    buildUniformBuffer(chartData) {
        const uniformBuffer = new ArrayBuffer(XpChartOverlay.UNIFORM_BUFFER_SIZE);
        const view = new DataView(uniformBuffer);
        const isScaled = uiScaleState.isScaled;
        const screenWidth = this.uiFramebufferInfo?.width ?? uiScaleState.screenWidth;
        const screenHeight = this.uiFramebufferInfo?.height ?? uiScaleState.screenHeight;
        // Use all data points (up to MAX_DATA_POINTS for shader array limit)
        // Data is already downsampled to ~60 points in index.tsx to show full session history
        const dataPoints = chartData.dataPoints.slice(0, MAX_DATA_POINTS);
        // Normalize data points to 0-1 range
        const minVal = Math.min(...dataPoints) * 0.9;
        const maxVal = Math.max(...dataPoints) * 1.1;
        const range = maxVal - minVal || 1;
        const normalizedData = dataPoints.map(v => (v - minVal) / range);
        if (isScaled && this.uiFramebufferInfo) {
            // 4K mode: render to UI framebuffer in UI coordinates (not scaled)
            // The UI framebuffer is at UI resolution (e.g., 1920x1080), not screen resolution
            // Position Y needs to be flipped for GL (Y=0 at bottom)
            const glPositionY = this.uiFramebufferInfo.height - this.position.y - this.size.height;
            view.setFloat32(0, this.uiFramebufferInfo.width, true); // uScreenSize.x (UI resolution)
            view.setFloat32(4, this.uiFramebufferInfo.height, true); // uScreenSize.y (UI resolution)
            view.setFloat32(8, this.position.x, true); // uPosition.x (UI coords)
            view.setFloat32(12, glPositionY, true); // uPosition.y (GL Y-flipped)
            view.setFloat32(16, this.size.width, true); // uSize.x (UI size)
            view.setFloat32(20, this.size.height, true); // uSize.y (UI size)
            view.setFloat32(24, 0.0, true); // uFlipY (no flip for UI fb)
        }
        else {
            // 1080p mode: use direct coordinates
            view.setFloat32(0, screenWidth, true);
            view.setFloat32(4, screenHeight, true);
            view.setFloat32(8, this.position.x, true);
            view.setFloat32(12, this.position.y, true);
            view.setFloat32(16, this.size.width, true);
            view.setFloat32(20, this.size.height, true);
            view.setFloat32(24, 1.0, true); // uFlipY
        }
        // uCornerRadius
        view.setFloat32(28, 8.0, true);
        // uBgColor - dark gray
        view.setFloat32(32, 30 / 255, true); // R
        view.setFloat32(36, 30 / 255, true); // G
        view.setFloat32(40, 35 / 255, true); // B
        view.setFloat32(44, 0.95, true); // A
        // uLineColor - from chart data
        view.setFloat32(48, chartData.lineColor[0], true);
        view.setFloat32(52, chartData.lineColor[1], true);
        view.setFloat32(56, chartData.lineColor[2], true);
        view.setFloat32(60, chartData.lineColor[3], true);
        // uGridColor - subtle gray
        view.setFloat32(64, 0.3, true);
        view.setFloat32(68, 0.3, true);
        view.setFloat32(72, 0.3, true);
        view.setFloat32(76, 0.4, true);
        // uDataCount
        view.setInt32(80, normalizedData.length, true);
        // Padding at 84-95
        // uDataPoints array at offset 96
        for (let i = 0; i < normalizedData.length && i < MAX_DATA_POINTS; i++) {
            view.setFloat32(96 + i * 4, normalizedData[i], true);
        }
        return uniformBuffer;
    }
    /**
     * Build uniform buffer for texture-only shader (for expanded canvas rendering)
     */
    buildTextureUniformBuffer() {
        const uniformBuffer = new ArrayBuffer(XpChartOverlay.TEXTURE_UNIFORM_BUFFER_SIZE);
        const view = new DataView(uniformBuffer);
        const isScaled = uiScaleState.isScaled;
        const screenWidth = this.uiFramebufferInfo?.width ?? uiScaleState.screenWidth;
        const screenHeight = this.uiFramebufferInfo?.height ?? uiScaleState.screenHeight;
        if (isScaled && this.uiFramebufferInfo) {
            // 4K mode: render to UI framebuffer in UI coordinates
            const glPositionY = this.uiFramebufferInfo.height - this.position.y - this.size.height;
            view.setFloat32(0, this.uiFramebufferInfo.width, true); // uScreenSize.x
            view.setFloat32(4, this.uiFramebufferInfo.height, true); // uScreenSize.y
            view.setFloat32(8, this.position.x, true); // uPosition.x
            view.setFloat32(12, glPositionY, true); // uPosition.y
            view.setFloat32(16, this.size.width, true); // uSize.x
            view.setFloat32(20, this.size.height, true); // uSize.y
            view.setFloat32(24, 0.0, true); // uFlipY
        }
        else {
            // 1080p mode: direct screen coordinates
            view.setFloat32(0, screenWidth, true);
            view.setFloat32(4, screenHeight, true);
            view.setFloat32(8, this.position.x, true);
            view.setFloat32(12, this.position.y, true);
            view.setFloat32(16, this.size.width, true);
            view.setFloat32(20, this.size.height, true);
            view.setFloat32(24, 1.0, true); // uFlipY
        }
        // uCornerRadius
        view.setFloat32(28, 6.0, true);
        return uniformBuffer;
    }
    /**
     * Show overlay with chart data (pure GL rendering)
     */
    async showChart(chartData) {
        const mySeq = ++this.operationSeq;
        this.lastChartData = chartData;
        this.size = { width: chartData.width, height: chartData.height };
        this.stopOverlaySync();
        this.isVisible = false;
        // Find UI framebuffer (async)
        if (!this.uiFramebufferInfo) {
            this.uiFramebufferInfo = await this.findUIFramebuffer();
            if (mySeq !== this.operationSeq)
                return;
        }
        // Create vertex array if needed
        if (!this.vertexArray) {
            this.vertexArray = this.createVertexArray();
        }
        const isScaled = uiScaleState.isScaled;
        const useUIFramebuffer = isScaled && this.uiFramebufferInfo && this.uiFramebufferInfo.framebufferId > 0;
        // Check if we have pre-rendered image data (expanded mode with multi-line chart)
        if (chartData.expandedImageData) {
            // Use texture-only program for pre-rendered canvas
            if (!this.programTextureOnly) {
                this.programTextureOnly = this.createProgramTextureOnly();
            }
            const uniformBuffer = this.buildTextureUniformBuffer();
            this.textTexture = _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createTexture(chartData.expandedImageData);
            if (mySeq !== this.operationSeq)
                return;
            this.stopOverlaySync();
            try {
                const filter = useUIFramebuffer
                    ? { framebufferId: this.uiFramebufferInfo.framebufferId }
                    : {};
                this.overlayHandle = _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.beginOverlay(filter, this.programTextureOnly, this.vertexArray, {
                    uniformSources: [],
                    uniformBuffer: new Uint8Array(uniformBuffer),
                    renderMode: "triangles",
                    trigger: "frameend",
                    alphaBlend: true,
                    samplers: { 0: this.textTexture },
                });
                this.isVisible = true;
                console.log(`[XpChartOverlay] Showing expanded overlay (texture mode) at (${this.position.x}, ${this.position.y}), size ${this.size.width}x${this.size.height}`);
            }
            catch (e) {
                console.error("[XpChartOverlay] Failed to create expanded overlay:", e);
            }
        }
        else {
            // Use shader-based chart program for compact mode
            if (!this.program) {
                this.program = this.createProgram();
            }
            const uniformBuffer = this.buildUniformBuffer(chartData);
            // Create text overlay texture at overlay resolution
            const textImageData = createTextOverlayImageData(chartData.width, chartData.height, chartData.xpPerHour ?? 0, chartData.elapsedMs ?? 0, chartData.lineColor);
            this.textTexture = _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createTexture(textImageData);
            if (mySeq !== this.operationSeq)
                return;
            this.stopOverlaySync();
            try {
                const filter = useUIFramebuffer
                    ? { framebufferId: this.uiFramebufferInfo.framebufferId }
                    : {};
                this.overlayHandle = _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.beginOverlay(filter, this.program, this.vertexArray, {
                    uniformSources: [],
                    uniformBuffer: new Uint8Array(uniformBuffer),
                    renderMode: "triangles",
                    trigger: "frameend",
                    alphaBlend: true,
                    samplers: { 0: this.textTexture },
                });
                this.isVisible = true;
                console.log(`[XpChartOverlay] Showing GL chart at (${this.position.x}, ${this.position.y}), size ${this.size.width}x${this.size.height}, isScaled=${uiScaleState.isScaled}`);
            }
            catch (e) {
                console.error("[XpChartOverlay] Failed to create overlay:", e);
            }
        }
    }
    /**
     * Legacy show method - converts canvas to chart data
     * @deprecated Use showChart() instead
     */
    async show(renderResult) {
        // For backwards compatibility, create a simple chart with placeholder data
        await this.showChart({
            dataPoints: [0.5], // Single point placeholder
            lineColor: [0, 0.83, 1, 1], // Cyan (#00d4ff)
            width: renderResult.width,
            height: renderResult.height,
        });
    }
    /**
     * Hide overlay
     */
    hide() {
        // Increment sequence to cancel any pending show operations
        ++this.operationSeq;
        this.stopOverlaySync();
        this.isVisible = false;
    }
    /**
     * Update chart with new data - swaps overlays to minimize flicker
     */
    async updateChart(chartData) {
        this.lastChartData = chartData;
        // If not visible or no handle, just show
        if (!this.isVisible || !this.overlayHandle) {
            await this.showChart(chartData);
            return;
        }
        // Size changed - need full recreate
        if (chartData.width !== this.size.width || chartData.height !== this.size.height) {
            await this.showChart(chartData);
            return;
        }
        const isScaled = uiScaleState.isScaled;
        const useUIFramebuffer = isScaled && this.uiFramebufferInfo && this.uiFramebufferInfo.framebufferId > 0;
        // Select program and build uniform buffer based on mode
        let uniformBuffer;
        let program;
        if (chartData.expandedImageData) {
            // Texture-only mode for expanded view
            if (!this.programTextureOnly) {
                this.programTextureOnly = this.createProgramTextureOnly();
            }
            uniformBuffer = this.buildTextureUniformBuffer();
            program = this.programTextureOnly;
            this.textTexture = _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createTexture(chartData.expandedImageData);
        }
        else {
            // Shader-based chart mode for compact view
            if (!this.program) {
                this.program = this.createProgram();
            }
            uniformBuffer = this.buildUniformBuffer(chartData);
            program = this.program;
            // Create text overlay texture
            const textImageData = createTextOverlayImageData(chartData.width, chartData.height, chartData.xpPerHour ?? 0, chartData.elapsedMs ?? 0, chartData.lineColor);
            this.textTexture = _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.createTexture(textImageData);
        }
        try {
            const filter = useUIFramebuffer
                ? { framebufferId: this.uiFramebufferInfo.framebufferId }
                : {};
            const newHandle = _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_0__.native.beginOverlay(filter, program, this.vertexArray, {
                uniformSources: [],
                uniformBuffer: new Uint8Array(uniformBuffer),
                renderMode: "triangles",
                trigger: "frameend",
                alphaBlend: true,
                samplers: { 0: this.textTexture },
            });
            const oldHandle = this.overlayHandle;
            this.overlayHandle = newHandle;
            if (oldHandle) {
                try {
                    oldHandle.stop();
                }
                catch (e) {
                    // Ignore
                }
            }
        }
        catch (e) {
            console.warn("[XpChartOverlay] Chart update failed:", e);
            await this.showChart(chartData);
        }
    }
    /**
     * Legacy update method
     * @deprecated Use updateChart() instead
     */
    async update(renderResult) {
        await this.show(renderResult);
    }
    /**
     * Check if overlay is visible
     */
    isShowing() {
        return this.isVisible;
    }
    /**
     * Handle resolution change
     */
    handleResolutionChange(info) {
        this.uiSize = { width: info.uiWidth, height: info.uiHeight };
        this.uiFramebufferInfo = null;
        // Load position for new resolution
        this.position = this.loadPosition();
        // Clamp position to valid bounds
        const maxX = Math.max(0, info.uiWidth - this.size.width);
        const maxY = Math.max(0, info.uiHeight - this.size.height);
        this.position = {
            x: Math.max(0, Math.min(this.position.x, maxX)),
            y: Math.max(0, Math.min(this.position.y, maxY)),
        };
    }
    /**
     * Dispose overlay and cleanup
     */
    dispose() {
        ++this.operationSeq; // Cancel any pending operations
        this.stopOverlaySync();
        this.isVisible = false;
        this.program = null;
        this.vertexArray = null;
        this.lastRenderResult = null;
    }
}
// Uniform buffer size for chart shader
XpChartOverlay.UNIFORM_BUFFER_SIZE = 96 + MAX_DATA_POINTS * 4; // 352 bytes
// Uniform buffer size for texture-only shader (simpler layout)
XpChartOverlay.TEXTURE_UNIFORM_BUFFER_SIZE = 32; // uScreenSize(8) + uPosition(8) + uSize(8) + uFlipY(4) + uCornerRadius(4)
// Global singleton
let globalOverlay = null;
function getXpChartOverlay() {
    if (!globalOverlay) {
        globalOverlay = new XpChartOverlay();
    }
    return globalOverlay;
}


/***/ },

/***/ "./overlay/index.ts"
/*!**************************!*\
  !*** ./overlay/index.ts ***!
  \**************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   XpChartOverlay: () => (/* reexport safe */ _XpChartOverlay__WEBPACK_IMPORTED_MODULE_0__.XpChartOverlay),
/* harmony export */   getUIScaleInfo: () => (/* reexport safe */ _XpChartOverlay__WEBPACK_IMPORTED_MODULE_0__.getUIScaleInfo),
/* harmony export */   getXpChartOverlay: () => (/* reexport safe */ _XpChartOverlay__WEBPACK_IMPORTED_MODULE_0__.getXpChartOverlay),
/* harmony export */   renderXpRateChart: () => (/* reexport safe */ _ChartRenderer__WEBPACK_IMPORTED_MODULE_1__.renderXpRateChart),
/* harmony export */   renderXpSummary: () => (/* reexport safe */ _ChartRenderer__WEBPACK_IMPORTED_MODULE_1__.renderXpSummary),
/* harmony export */   setUIScaleState: () => (/* reexport safe */ _XpChartOverlay__WEBPACK_IMPORTED_MODULE_0__.setUIScaleState),
/* harmony export */   useXpChartOverlay: () => (/* reexport safe */ _useXpChartOverlay__WEBPACK_IMPORTED_MODULE_2__.useXpChartOverlay)
/* harmony export */ });
/* harmony import */ var _XpChartOverlay__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./XpChartOverlay */ "./overlay/XpChartOverlay.ts");
/* harmony import */ var _ChartRenderer__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./ChartRenderer */ "./overlay/ChartRenderer.ts");
/* harmony import */ var _useXpChartOverlay__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./useXpChartOverlay */ "./overlay/useXpChartOverlay.ts");
/**
 * XP Chart Overlay exports
 */





/***/ },

/***/ "./overlay/useXpChartOverlay.ts"
/*!**************************************!*\
  !*** ./overlay/useXpChartOverlay.ts ***!
  \**************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   useXpChartOverlay: () => (/* binding */ useXpChartOverlay)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "../node_modules/react/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(react__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var _XpChartOverlay__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./XpChartOverlay */ "./overlay/XpChartOverlay.ts");
/* harmony import */ var _ChartRenderer__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./ChartRenderer */ "./overlay/ChartRenderer.ts");
/* harmony import */ var _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../util/patchrs_napi */ "./util/patchrs_napi.ts");
/**
 * React hook for XP Chart Overlay with compact/expanded modes
 */




// Convert hex color to RGBA tuple (0-1 range)
function hexToRgba(hex) {
    const result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
    if (result) {
        return [
            parseInt(result[1], 16) / 255,
            parseInt(result[2], 16) / 255,
            parseInt(result[3], 16) / 255,
            1.0
        ];
    }
    return [0, 0.83, 1, 1]; // Default cyan (#00d4ff)
}
/**
 * Get ImageData from canvas at device pixel ratio
 */
function getCanvasImageData(canvas, width, height) {
    const ctx = canvas.getContext("2d");
    if (!ctx) {
        return new ImageData(width, height);
    }
    // Get at canvas resolution (which may be scaled by DPR)
    return ctx.getImageData(0, 0, canvas.width, canvas.height);
}
/**
 * Hook for managing XP chart overlay with compact/expanded modes
 */
function useXpChartOverlay(options = {}) {
    const { enabled = true, defaultExpanded = false, } = options;
    const overlayRef = react__WEBPACK_IMPORTED_MODULE_0___default().useRef(null);
    const [isAvailable, setIsAvailable] = react__WEBPACK_IMPORTED_MODULE_0___default().useState(false);
    const [isVisible, setIsVisible] = react__WEBPACK_IMPORTED_MODULE_0___default().useState(false);
    const [isExpanded, setIsExpanded] = react__WEBPACK_IMPORTED_MODULE_0___default().useState(defaultExpanded);
    const [position, setPositionState] = react__WEBPACK_IMPORTED_MODULE_0___default().useState({ x: 50, y: 50 });
    const [size, setSize] = react__WEBPACK_IMPORTED_MODULE_0___default().useState({ width: 200, height: 48 });
    const [uiBounds, setUIBounds] = react__WEBPACK_IMPORTED_MODULE_0___default().useState({ width: 1920, height: 1080 });
    // Drag state
    const [isDragging, setIsDragging] = react__WEBPACK_IMPORTED_MODULE_0___default().useState(false);
    const dragStartRef = react__WEBPACK_IMPORTED_MODULE_0___default().useRef({ x: 0, y: 0, posX: 0, posY: 0 });
    // Last chart data for updates
    const lastDataRef = react__WEBPACK_IMPORTED_MODULE_0___default().useRef(null);
    // Detect UI scale from game
    const detectUIScale = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback(async () => {
        try {
            if (!_util_patchrs_napi__WEBPACK_IMPORTED_MODULE_3__.native)
                return;
            const frames = await _util_patchrs_napi__WEBPACK_IMPORTED_MODULE_3__.native.recordRenderCalls({
                maxframes: 1,
                features: ["texturesnapshot"],
                framebufferId: 0,
            });
            const viewport = frames.find(f => f.viewport)?.viewport;
            if (viewport) {
                const isScaled = viewport.width > 2560;
                (0,_XpChartOverlay__WEBPACK_IMPORTED_MODULE_1__.setUIScaleState)({
                    isScaled,
                    uiWidth: isScaled ? 1920 : viewport.width,
                    uiHeight: isScaled ? 1080 : viewport.height,
                    screenWidth: viewport.width,
                    screenHeight: viewport.height,
                    scaleX: isScaled ? viewport.width / 1920 : 1,
                    scaleY: isScaled ? viewport.height / 1080 : 1,
                    scalingTextureId: 0,
                });
                setUIBounds({
                    width: isScaled ? 1920 : viewport.width,
                    height: isScaled ? 1080 : viewport.height,
                });
                if (overlayRef.current) {
                    overlayRef.current.handleResolutionChange((0,_XpChartOverlay__WEBPACK_IMPORTED_MODULE_1__.getUIScaleInfo)());
                }
            }
        }
        catch (e) {
            console.warn("[useXpChartOverlay] Failed to detect UI scale:", e);
        }
    }, []);
    // Initialize overlay
    react__WEBPACK_IMPORTED_MODULE_0___default().useEffect(() => {
        if (!enabled)
            return;
        const initOverlay = async () => {
            try {
                if (!_util_patchrs_napi__WEBPACK_IMPORTED_MODULE_3__.native) {
                    console.warn("[useXpChartOverlay] Native addon not available");
                    return;
                }
                overlayRef.current = (0,_XpChartOverlay__WEBPACK_IMPORTED_MODULE_1__.getXpChartOverlay)();
                setIsAvailable(true);
                const pos = overlayRef.current.getPosition();
                setPositionState(pos);
                const sz = overlayRef.current.getSize();
                setSize(sz);
                const bounds = overlayRef.current.getUIBounds();
                setUIBounds(bounds);
                await detectUIScale();
            }
            catch (e) {
                console.error("[useXpChartOverlay] Failed to initialize:", e);
            }
        };
        initOverlay();
        return () => {
            if (overlayRef.current) {
                overlayRef.current.dispose();
            }
        };
    }, [enabled, detectUIScale]);
    // Render the overlay based on current mode
    const renderOverlay = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback((data, expanded) => {
        if (expanded) {
            // Expanded view with skill breakdown
            const skillsWithRate = data.skillBreakdown.map(s => ({
                ...s,
                xpPerHour: data.elapsedMs > 0 ? (s.xp / data.elapsedMs) * 3600000 : 0,
            }));
            return (0,_ChartRenderer__WEBPACK_IMPORTED_MODULE_2__.renderExpandedOverlay)(data.totalXp, data.xpPerHour, data.rollingXpPerHour, data.elapsedMs, skillsWithRate, data.sparklineData);
        }
        else {
            // Compact view
            return (0,_ChartRenderer__WEBPACK_IMPORTED_MODULE_2__.renderCompactOverlay)(data.xpPerHour, data.elapsedMs, data.sparklineData, data.primarySkillColor ?? "#00d4ff");
        }
    }, []);
    // Show overlay - uses pure GL rendering (compact) or canvas texture (expanded)
    const show = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback(() => {
        if (!overlayRef.current)
            return;
        const data = lastDataRef.current;
        // If expanded with skill rate history, use canvas-rendered multi-line chart
        if (isExpanded && data?.skillRateHistory && data.skillRateHistory.length > 0) {
            // Build skill breakdown with xpPerHour for the expanded overlay
            const skillsWithRate = data.skillBreakdown.map(s => ({
                ...s,
                xpPerHour: data.elapsedMs > 0 ? (s.xp / data.elapsedMs) * 3600000 : 0,
            }));
            // Render canvas-based multi-line chart
            const { canvas, width, height } = (0,_ChartRenderer__WEBPACK_IMPORTED_MODULE_2__.renderExpandedOverlay)(data.totalXp, data.xpPerHour, data.rollingXpPerHour, data.elapsedMs, skillsWithRate, data.sparklineData, {}, data.skillRateHistory);
            // Get ImageData from rendered canvas
            const expandedImageData = getCanvasImageData(canvas, width, height);
            const chartData = {
                dataPoints: data.sparklineData.length > 0 ? data.sparklineData : [data.xpPerHour || 0],
                lineColor: hexToRgba(data.primarySkillColor ?? "#00d4ff"),
                width: canvas.width, // Use canvas resolution (may be scaled by DPR)
                height: canvas.height,
                xpPerHour: data.xpPerHour ?? 0,
                elapsedMs: data.elapsedMs ?? 0,
                expandedImageData,
            };
            overlayRef.current.showChart(chartData).catch(e => {
                console.error("[useXpChartOverlay] Failed to show expanded overlay:", e);
            });
            setIsVisible(true);
            setSize({ width, height });
        }
        else {
            // Compact mode: use shader-based sparkline
            const chartWidth = isExpanded ? 320 : 200;
            const chartHeight = isExpanded ? 180 : 126;
            const chartData = {
                dataPoints: data?.sparklineData?.length ? data.sparklineData : [data?.xpPerHour || 0],
                lineColor: hexToRgba(data?.primarySkillColor ?? "#00d4ff"),
                width: chartWidth,
                height: chartHeight,
                xpPerHour: data?.xpPerHour ?? 0,
                elapsedMs: data?.elapsedMs ?? 0,
            };
            overlayRef.current.showChart(chartData).catch(e => {
                console.error("[useXpChartOverlay] Failed to show overlay:", e);
            });
            setIsVisible(true);
            setSize({ width: chartData.width, height: chartData.height });
        }
    }, [isExpanded]);
    // Hide overlay
    const hide = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback(() => {
        if (overlayRef.current) {
            overlayRef.current.hide();
        }
        setIsVisible(false);
    }, []);
    // Toggle overlay visibility
    const toggle = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback(() => {
        if (isVisible) {
            hide();
        }
        else {
            show();
        }
    }, [isVisible, show, hide]);
    // Toggle expanded/compact mode
    const toggleExpanded = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback(() => {
        const newExpanded = !isExpanded;
        setIsExpanded(newExpanded);
        // Re-render overlay with new mode if visible
        if (isVisible && overlayRef.current && lastDataRef.current) {
            const data = lastDataRef.current;
            // If expanding with skill rate history, use canvas-rendered multi-line chart
            if (newExpanded && data.skillRateHistory && data.skillRateHistory.length > 0) {
                const skillsWithRate = data.skillBreakdown.map(s => ({
                    ...s,
                    xpPerHour: data.elapsedMs > 0 ? (s.xp / data.elapsedMs) * 3600000 : 0,
                }));
                const { canvas, width, height } = (0,_ChartRenderer__WEBPACK_IMPORTED_MODULE_2__.renderExpandedOverlay)(data.totalXp, data.xpPerHour, data.rollingXpPerHour, data.elapsedMs, skillsWithRate, data.sparklineData, {}, data.skillRateHistory);
                const expandedImageData = getCanvasImageData(canvas, width, height);
                const chartData = {
                    dataPoints: data.sparklineData.length > 0 ? data.sparklineData : [data.xpPerHour || 0],
                    lineColor: hexToRgba(data.primarySkillColor ?? "#00d4ff"),
                    width: canvas.width,
                    height: canvas.height,
                    xpPerHour: data.xpPerHour ?? 0,
                    elapsedMs: data.elapsedMs ?? 0,
                    expandedImageData,
                };
                overlayRef.current.showChart(chartData).catch(e => {
                    console.error("[useXpChartOverlay] Failed to update to expanded mode:", e);
                });
                setSize({ width, height });
            }
            else {
                // Compact mode: shader-based sparkline
                const chartWidth = newExpanded ? 320 : 200;
                const chartHeight = newExpanded ? 180 : 126;
                const chartData = {
                    dataPoints: data.sparklineData.length > 0 ? data.sparklineData : [data.xpPerHour || 0],
                    lineColor: hexToRgba(data.primarySkillColor ?? "#00d4ff"),
                    width: chartWidth,
                    height: chartHeight,
                    xpPerHour: data.xpPerHour ?? 0,
                    elapsedMs: data.elapsedMs ?? 0,
                };
                overlayRef.current.showChart(chartData).catch(e => {
                    console.error("[useXpChartOverlay] Failed to update overlay mode:", e);
                });
                setSize({ width: chartData.width, height: chartData.height });
            }
        }
    }, [isExpanded, isVisible]);
    // Set position
    const setPosition = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback((x, y) => {
        if (overlayRef.current) {
            overlayRef.current.setPosition(x, y);
            setPositionState({ x, y });
        }
    }, []);
    // Update chart data - uses pure GL rendering (compact) or canvas texture (expanded)
    const updateChart = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback((data) => {
        lastDataRef.current = data;
        if (!isVisible || !overlayRef.current)
            return;
        // If expanded with skill rate history, use canvas-rendered multi-line chart
        if (isExpanded && data.skillRateHistory && data.skillRateHistory.length > 0) {
            const skillsWithRate = data.skillBreakdown.map(s => ({
                ...s,
                xpPerHour: data.elapsedMs > 0 ? (s.xp / data.elapsedMs) * 3600000 : 0,
            }));
            const { canvas, width, height } = (0,_ChartRenderer__WEBPACK_IMPORTED_MODULE_2__.renderExpandedOverlay)(data.totalXp, data.xpPerHour, data.rollingXpPerHour, data.elapsedMs, skillsWithRate, data.sparklineData, {}, data.skillRateHistory);
            const expandedImageData = getCanvasImageData(canvas, width, height);
            const chartData = {
                dataPoints: data.sparklineData.length > 0 ? data.sparklineData : [data.xpPerHour || 0],
                lineColor: hexToRgba(data.primarySkillColor ?? "#00d4ff"),
                width: canvas.width,
                height: canvas.height,
                xpPerHour: data.xpPerHour ?? 0,
                elapsedMs: data.elapsedMs ?? 0,
                expandedImageData,
            };
            overlayRef.current.updateChart(chartData).catch(e => {
                console.error("[useXpChartOverlay] Failed to update expanded overlay:", e);
            });
            setSize(prev => {
                if (prev.width === width && prev.height === height) {
                    return prev;
                }
                return { width, height };
            });
        }
        else {
            // Compact mode: shader-based sparkline
            const chartWidth = isExpanded ? 320 : 200;
            const chartHeight = isExpanded ? 180 : 126;
            const chartData = {
                dataPoints: data.sparklineData.length > 0 ? data.sparklineData : [data.xpPerHour || 0],
                lineColor: hexToRgba(data.primarySkillColor ?? "#00d4ff"),
                width: chartWidth,
                height: chartHeight,
                xpPerHour: data.xpPerHour ?? 0,
                elapsedMs: data.elapsedMs ?? 0,
            };
            overlayRef.current.updateChart(chartData).catch(e => {
                console.error("[useXpChartOverlay] Failed to update overlay:", e);
            });
            setSize(prev => {
                if (prev.width === chartData.width && prev.height === chartData.height) {
                    return prev;
                }
                return { width: chartData.width, height: chartData.height };
            });
        }
    }, [isVisible, isExpanded]);
    // Drag handling
    const startDrag = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback((clientX, clientY) => {
        setIsDragging(true);
        dragStartRef.current = {
            x: clientX,
            y: clientY,
            posX: position.x,
            posY: position.y,
        };
    }, [position]);
    const updateDrag = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback((clientX, clientY) => {
        if (!isDragging)
            return;
        const deltaX = clientX - dragStartRef.current.x;
        const deltaY = clientY - dragStartRef.current.y;
        const newX = dragStartRef.current.posX + deltaX;
        const newY = dragStartRef.current.posY + deltaY;
        setPosition(newX, newY);
    }, [isDragging, setPosition]);
    const endDrag = react__WEBPACK_IMPORTED_MODULE_0___default().useCallback(() => {
        setIsDragging(false);
    }, []);
    return {
        isAvailable,
        isVisible,
        isExpanded,
        position,
        size,
        uiBounds,
        show,
        hide,
        toggle,
        toggleExpanded,
        setPosition,
        updateChart,
        isDragging,
        startDrag,
        updateDrag,
        endDrag,
    };
}


/***/ },

/***/ "./reader/xpReader.ts"
/*!****************************!*\
  !*** ./reader/xpReader.ts ***!
  \****************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   SKILL_SPRITES: () => (/* binding */ SKILL_SPRITES),
/* harmony export */   SkillSpriteCache: () => (/* binding */ SkillSpriteCache),
/* harmony export */   UI_SPRITES: () => (/* binding */ UI_SPRITES),
/* harmony export */   XP_DROP_PATTERN: () => (/* binding */ XP_DROP_PATTERN),
/* harmony export */   XpDetector: () => (/* binding */ XpDetector),
/* harmony export */   XpDropType: () => (/* binding */ XpDropType),
/* harmony export */   analyzeBrightness: () => (/* binding */ analyzeBrightness),
/* harmony export */   captureSprite: () => (/* binding */ captureSprite),
/* harmony export */   captureSpriteWithBrightness: () => (/* binding */ captureSpriteWithBrightness),
/* harmony export */   getSkillLabel: () => (/* binding */ getSkillLabel),
/* harmony export */   isBrightnessValidForXpDrop: () => (/* binding */ isBrightnessValidForXpDrop),
/* harmony export */   skillSpriteCache: () => (/* binding */ skillSpriteCache)
/* harmony export */ });
/* harmony import */ var _reflect2d_uiparser__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../reflect2d/uiparser */ "./reflect2d/uiparser.ts");
/* harmony import */ var _reflect2d_reflect2d__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../reflect2d/reflect2d */ "./reflect2d/reflect2d.ts");
/* harmony import */ var _reflect2d_spritecache__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../reflect2d/spritecache */ "./reflect2d/spritecache.ts");
/* harmony import */ var immer__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! immer */ "../node_modules/immer/dist/immer.mjs");
/* harmony import */ var _render_renderprogram__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../render/renderprogram */ "./render/renderprogram.ts");
/* harmony import */ var _data_xpSources_json__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../data/xpSources.json */ "./data/xpSources.json");





// ============================================================================
// SPRITE ID CONSTANTS - All RS3 skill sprites
// ============================================================================
const SKILL_SPRITES = {
    // Combat Skills (9)
    attack: { id: 197, label: "Attack" },
    strength: { id: 198, label: "Strength" },
    defence: { id: 199, label: "Defence" },
    ranged: { id: 200, label: "Ranged" },
    prayer: { id: 201, label: "Prayer" },
    magic: { id: 202, label: "Magic" },
    constitution: { id: 203, label: "Constitution" },
    summoning: { id: 222, label: "Summoning" },
    necromancy: { id: 30934, label: "Necromancy" },
    // Gathering Skills (7)
    mining: { id: 209, label: "Mining" },
    fishing: { id: 211, label: "Fishing" },
    woodcutting: { id: 214, label: "Woodcutting" },
    farming: { id: 217, label: "Farming" },
    hunter: { id: 220, label: "Hunter" },
    divination: { id: 9170, label: "Divination" },
    archaeology: { id: 10782, label: "Archaeology" },
    // Artisan Skills (8)
    smithing: { id: 210, label: "Smithing" },
    cooking: { id: 212, label: "Cooking" },
    firemaking: { id: 213, label: "Firemaking" },
    runecrafting: { id: 215, label: "Runecrafting" },
    crafting: { id: 207, label: "Crafting" },
    fletching: { id: 208, label: "Fletching" },
    herblore: { id: 205, label: "Herblore" },
    construction: { id: 221, label: "Construction" },
    // Support Skills (4)
    agility: { id: 204, label: "Agility" },
    thieving: { id: 206, label: "Thieving" },
    slayer: { id: 216, label: "Slayer" },
    dungeoneering: { id: 3028, label: "Dungeoneering" },
    // Elite Skills (1)
    invention: { id: 26541, label: "Invention" },
};
// Special UI sprites
const UI_SPRITES = {
    xpDrop: { id: 3795 }, // XP drop indicator
    statBackground: { id: 9278 }, // Stats panel background
};
// ============================================================================
// LOOKUP MAPS
// ============================================================================
const SKILL_ID_TO_INFO = new Map(Object.values(SKILL_SPRITES).map(s => [s.id, { id: s.id, label: s.label }]));
const ALL_SKILL_IDS = Object.values(SKILL_SPRITES).map(s => s.id);
// Combat skills that can appear together in shared XP drops (all 9 combat skills)
const COMBAT_SKILL_IDS = new Set([
    SKILL_SPRITES.attack.id,
    SKILL_SPRITES.strength.id,
    SKILL_SPRITES.defence.id,
    SKILL_SPRITES.ranged.id,
    SKILL_SPRITES.prayer.id,
    SKILL_SPRITES.magic.id,
    SKILL_SPRITES.constitution.id,
    SKILL_SPRITES.summoning.id,
    SKILL_SPRITES.necromancy.id,
]);
// Primary attack skills that receive 2/3 of combat XP
// These are the main damage-dealing skills (excludes Prayer, Summoning, Constitution)
const PRIMARY_ATTACK_SKILL_IDS = new Set([
    SKILL_SPRITES.attack.id,
    SKILL_SPRITES.strength.id,
    SKILL_SPRITES.defence.id,
    SKILL_SPRITES.ranged.id,
    SKILL_SPRITES.magic.id,
    SKILL_SPRITES.necromancy.id,
]);
// Skills that are ALWAYS visible in the combat skills menu UI
// These should be IGNORED when looking for XP drop indicators because they're
// part of the skills interface, not the XP drop itself
// NOTE: Constitution is NOT in this list because it's needed for combat XP splits
// We rely on render order + distance filtering to avoid menu sprites
const MENU_SKILL_IDS = new Set([
    SKILL_SPRITES.attack.id,
    SKILL_SPRITES.strength.id,
    SKILL_SPRITES.defence.id,
    SKILL_SPRITES.necromancy.id,
]);
// Maximum horizontal distance (pixels) between XP text and skill indicator
// Skill sprites must be close to the XP text to be considered part of the drop
const MAX_INDICATOR_DISTANCE = 120;
// Maximum render order distance - only consider skill sprites within this many
// elements of the XP drop sprite in the render order. This prevents picking up
// skill sprites from other UI components (skills menu, buff bar, etc.)
const MAX_RENDER_ORDER_DISTANCE = 20;
class SkillSpriteCache {
    constructor() {
        this.cache = new Map();
    }
    /**
     * Capture a sprite from a RenderRect and return as data URL
     */
    captureSprite(sprite) {
        try {
            const imgData = (0,_reflect2d_reflect2d__WEBPACK_IMPORTED_MODULE_1__.safeCapture)(sprite.basetex, sprite.x, sprite.y, sprite.width, sprite.height);
            if (!imgData)
                return null;
            const canvas = document.createElement("canvas");
            canvas.width = sprite.width;
            canvas.height = sprite.height;
            const ctx = canvas.getContext("2d");
            if (!ctx)
                return null;
            ctx.putImageData(imgData, 0, 0);
            return canvas.toDataURL();
        }
        catch {
            return null;
        }
    }
    /**
     * Cache a skill sprite from a detected RenderRect
     */
    cacheFromRenderRect(element) {
        const skillId = element.sprite.known?.id;
        if (skillId === undefined)
            return null;
        const skillInfo = SKILL_ID_TO_INFO.get(skillId);
        if (!skillInfo)
            return null;
        // Already cached? Return existing
        if (this.cache.has(skillId)) {
            return this.cache.get(skillId);
        }
        // Capture the sprite
        const dataUrl = this.captureSprite(element.sprite);
        if (!dataUrl)
            return null;
        const cached = {
            skillId,
            label: skillInfo.label,
            dataUrl,
            width: element.width,
            height: element.height,
            capturedAt: Date.now(),
        };
        this.cache.set(skillId, cached);
        console.log(`[Sprite Cache] Cached ${skillInfo.label} sprite (${element.width}x${element.height})`);
        return cached;
    }
    /**
     * Get a cached sprite by skill ID
     */
    get(skillId) {
        return this.cache.get(skillId);
    }
    /**
     * Get all cached sprites
     */
    getAll() {
        return Array.from(this.cache.values());
    }
    /**
     * Check if a skill sprite is cached
     */
    has(skillId) {
        return this.cache.has(skillId);
    }
    /**
     * Get cache size
     */
    get size() {
        return this.cache.size;
    }
    /**
     * Clear the cache
     */
    clear() {
        this.cache.clear();
    }
}
// Global sprite cache instance
const skillSpriteCache = new SkillSpriteCache();
/** Parse raw XP drop characters into structured components */
function assembleXpText(chars) {
    const raw = chars.join("");
    let i = 0;
    const n = raw.length;
    let sign = null;
    if (i < n && (raw[i] === "+" || raw[i] === "-")) {
        sign = raw[i];
        i += 1;
    }
    while (i < n && (raw[i] === " " || raw[i] === "\u00A0"))
        i += 1;
    const numStart = i;
    let hasDigits = false;
    while (i < n && raw[i] >= "0" && raw[i] <= "9") {
        hasDigits = true;
        i += 1;
    }
    if (i < n && raw[i] === ".") {
        const dotPos = i;
        i += 1;
        let postDigits = 0;
        while (i < n && raw[i] >= "0" && raw[i] <= "9") {
            postDigits += 1;
            hasDigits = true;
            i += 1;
        }
        if (postDigits === 0 && !hasDigits)
            i = dotPos;
    }
    const value = hasDigits ? raw.slice(numStart, i) : null;
    while (i < n && (raw[i] === " " || raw[i] === "\u00A0"))
        i += 1;
    const unitStart = i;
    while (i < n && ((raw[i] >= "A" && raw[i] <= "Z") || (raw[i] >= "a" && raw[i] <= "z")))
        i += 1;
    let unit = i > unitStart ? raw.slice(unitStart, i) : null;
    if (unit) {
        const lower = unit.toLowerCase();
        if (lower === "xp" || lower === "k" || lower === "m") {
            unit = lower;
        }
        else {
            i = unitStart;
            unit = null;
        }
    }
    while (i < n && (raw[i] === " " || raw[i] === "\u00A0"))
        i += 1;
    const label = i < n ? raw.slice(i).replace(/\s+/g, " ").trim() : null;
    const parts = [];
    if (sign)
        parts.push(sign, " ");
    if (value)
        parts.push(value);
    if (unit)
        parts.push(" ", unit);
    if (label)
        parts.push(" ", label);
    const text = parts.join("").replace(/\s+/g, " ").trim();
    return { sign, value, unit, label, text };
}
/**
 * Edge detector for XP drops - emits only on FIRST frame of a new drop.
 * Handles RS3's frame-to-frame jitter with linger frames.
 */
class XpEdgeDetector {
    constructor() {
        this.phase = "idle";
        this.lastSignature = null;
        this.lastEmitMs = 0;
        this.lingerFramesLeft = 0;
        this.burstCount = 0;
        // Tuning constants (from reference)
        this.LINGER_FRAMES = 3;
        this.MAX_BURST = 1; // emit once per drop
        this.MIN_MS_BETWEEN_SAME = 550; // ~1 game tick
    }
    /** Build signature from XP text - used for dedup */
    signature(xpText) {
        const a = assembleXpText(xpText.split(""));
        return [a.sign ?? "", a.value ?? "", a.label ?? ""].join("|");
    }
    /**
     * Process a frame. Returns true if this is a NEW drop that should be emitted.
     * Call with null when no XP drop is detected in a frame.
     */
    processFrame(xpText) {
        const now = typeof performance !== "undefined" ? performance.now() : Date.now();
        if (xpText === null) {
            // No match: count as gap frame
            if (this.phase === "seen") {
                if (this.lingerFramesLeft > 0) {
                    this.lingerFramesLeft--;
                }
                else {
                    this.phase = "idle";
                }
            }
            return false;
        }
        const sig = this.signature(xpText);
        // New drop or different signature: emit
        if (this.phase === "idle" || (this.phase === "seen" && sig !== this.lastSignature)) {
            this.phase = "seen";
            this.lastSignature = sig;
            this.lingerFramesLeft = this.LINGER_FRAMES;
            this.lastEmitMs = now;
            this.burstCount = 1;
            return true;
        }
        // Same signature continuing
        if (this.phase === "seen" && sig === this.lastSignature) {
            this.lingerFramesLeft = this.LINGER_FRAMES; // refresh linger
            const canEmit = this.burstCount < this.MAX_BURST;
            const timeOk = now - this.lastEmitMs >= this.MIN_MS_BETWEEN_SAME;
            if (canEmit && timeOk) {
                this.lastEmitMs = now;
                this.burstCount++;
                return true;
            }
            return false; // suppress duplicate
        }
        // Fallback: treat as new
        this.phase = "seen";
        this.lastSignature = sig;
        this.lingerFramesLeft = this.LINGER_FRAMES;
        this.lastEmitMs = now;
        this.burstCount = 1;
        return true;
    }
    /** Reset the detector state */
    reset() {
        this.phase = "idle";
        this.lastSignature = null;
        this.lastEmitMs = 0;
        this.lingerFramesLeft = 0;
        this.burstCount = 0;
    }
}
// ============================================================================
// XP DROP PATTERN - Using UIParser pattern matching
// ============================================================================
const XP_DROP_PATTERN = [
    { id: UI_SPRITES.xpDrop.id },
    { string: "", ref: "xpText" },
    { repeat: [0, 10] },
];
// ============================================================================
// XP DROP TYPES - Different sources have different distribution rules
// ============================================================================
var XpDropType;
(function (XpDropType) {
    /** Single skill XP drop (skilling, quest reward, etc.) - 100% to one skill */
    XpDropType["SINGLE_SKILL"] = "single";
    /** Combat XP split - Constitution gets 1/3, other combat skills split 2/3 */
    XpDropType["COMBAT_SPLIT"] = "combat";
    /** Celebration lamp - Main skill gets full XP, all others get 5% */
    XpDropType["CELEBRATION_LAMP"] = "celebration";
    /** All-skills lamp (Djinn, etc.) - Equal XP to all skills shown */
    XpDropType["ALL_SKILLS_LAMP"] = "all_skills";
    /** Unknown multi-skill drop - split evenly as fallback */
    XpDropType["UNKNOWN_MULTI"] = "unknown_multi";
})(XpDropType || (XpDropType = {}));
// ============================================================================
// XP SOURCE IDENTIFICATION (loaded from JSON to avoid circular deps)
// ============================================================================

// Build lookup map from JSON data
const XP_SOURCE_MAP = new Map();
for (const src of _data_xpSources_json__WEBPACK_IMPORTED_MODULE_5__.sources) {
    const key = `${Math.round(src.xp * 10)}_${src.skillId}`;
    if (!XP_SOURCE_MAP.has(key))
        XP_SOURCE_MAP.set(key, []);
    XP_SOURCE_MAP.get(key).push(src);
}
function identifySource(xpValue, skillId) {
    const key = `${Math.round(xpValue * 10)}_${skillId}`;
    const matches = XP_SOURCE_MAP.get(key);
    return matches && matches.length > 0 ? matches[0].name : null;
}
/**
 * Check if XP value matches a known Prayer source (bones/ashes).
 * Returns the source name and XP if found, null otherwise.
 * Allows tolerance for rounding (e.g., 4.5 XP bones can show as 4 or 5).
 */
function findMatchingPrayerSource(xpValue) {
    const prayerSources = _data_xpSources_json__WEBPACK_IMPORTED_MODULE_5__.sources
        .filter(s => s.skillId === SKILL_SPRITES.prayer.id);
    // Check for near-match (within 1 XP tolerance for rounding)
    // RS3 can round 4.5 to either 4 or 5
    let bestMatch = null;
    for (const src of prayerSources) {
        const diff = Math.abs(src.xp - xpValue);
        if (diff < 1.0 && (!bestMatch || diff < bestMatch.diff)) {
            bestMatch = { name: src.name, xp: src.xp, diff };
        }
    }
    return bestMatch ? { name: bestMatch.name, xp: bestMatch.xp } : null;
}
class XpDetector {
    constructor(atlas) {
        this._hasLoggedDebug = false;
        // Deduplication: Track recent XP drops by value AND position to prevent counting
        // the same drop multiple times as it animates upward, while still allowing
        // rapid kills with the same XP value at different positions.
        this.recentDrops = new Map();
        this.DEDUP_WINDOW_MS = 1500; // ~2.5 ticks
        this.DEDUP_POSITION_THRESHOLD = 50; // pixels - drops within this distance are considered same
        // Pending drops: XP drops detected without skills, waiting for skill association
        // Key is XP value (stringified), value is pending drop info
        this.pendingDrops = new Map();
        // How long to wait for skills before giving up (ms) - about 3 game ticks
        this.PENDING_TIMEOUT_MS = 1800;
        // XP drop zone tracking - focus detection on known XP drop area
        this.xpDropZone = null;
        // How long before zone expires and we scan everything again (10 seconds)
        this.ZONE_EXPIRE_MS = 10000;
        // Margin around detected drops to define the zone
        this.ZONE_MARGIN = 150;
        // Static text rejection: tracks text seen in previous frame
        // Key: "x_y" (rounded to nearest 5px), Value: text content
        this.previousScanTexts = new Map();
        // Delta tracking for XP counter positions: persists across frames (NOT cleared each frame)
        // When the same position shows a different numeric value, the XP gain is the delta.
        // Key: "x_y" (rounded to nearest 15px), Value: last XP numeric value
        this.positionXpValues = new Map();
        // Typical delta tracking: stores exponential moving average of deltas per position
        // Used to detect anomalous deltas from position quantization boundary crossings
        // Key: "x_y" (rounded to nearest 15px), Value: typical delta value
        this.positionTypicalDelta = new Map();
        // Stable delta tracking: locks onto the consistent delta for each position
        // Snaps deltas within ±1 of locked value to eliminate alternation from fractional XP
        // (RS3 tracks .5 XP internally → text parsing loses it for numbers >1000 → alternating N/N+1)
        this.positionStableDelta = new Map();
        // Frame counter for TextScan - first 2 frames are "learning period" to record
        // counter starting values without emitting them as false positives
        this.textScanFrameCount = 0;
        // Fallback rejection cache: throttle repeated rejection logs
        // Key: "${skillLabel}:${roundedX}:${roundedY}:${text}", Value: timestamp
        this.fallbackRejectCache = new Map();
        // User-configured detection zone - when set, TextScan only processes text within this zone
        this.userDetectionZone = null;
        // Edge detector for robust dedup (ported from reference)
        this.edgeDetector = new XpEdgeDetector();
        // Runtime font learning: XP drop text uses an 8x12 font not in the hash database
        // We learn character hashes by cross-referencing with delta tracking results
        this.learnedFontSheet = null;
        this.learnedFontChars = new Map();
        // Confidence tracking: need to see same hash→char pair multiple times before registering
        this.fontCharConfidence = new Map();
        this.FONT_LEARN_CONFIDENCE = 2;
        this.fontSheetState = {
            sheetwidth: 0, sheetheight: 0, sheethash: 0, spriteid: -2,
            characters: [], unknownchars: [],
        };
        this.atlas = atlas;
        this.loadLearnedFont();
    }
    /** Load previously learned font characters from JSON file or localStorage */
    loadLearnedFont() {
        // Try loading from JSON file first (via IPC), fall back to localStorage
        try {
            const api = window.appWindowApi;
            if (api?.readAppData) {
                api.readAppData('xp-buddy', 'learned-font.json').then((raw) => {
                    if (raw) {
                        const count = this.loadFontFromJson(raw);
                        if (count > 0) {
                            console.log(`[XP Font] Loaded ${count} learned characters from JSON file`);
                            return;
                        }
                    }
                    // Fall back to localStorage
                    this.loadFontFromLocalStorage();
                }).catch(() => {
                    this.loadFontFromLocalStorage();
                });
                return;
            }
        }
        catch (e) { /* fall through */ }
        // No IPC available, use localStorage directly
        this.loadFontFromLocalStorage();
    }
    /** Parse font sheet JSON and register all characters */
    loadFontFromJson(raw) {
        try {
            const data = JSON.parse(raw);
            // Standard font sheet format
            if (data.spriteid !== undefined && Array.isArray(data.characters)) {
                // Load into immer state
                this.fontSheetState = (0,immer__WEBPACK_IMPORTED_MODULE_3__.produce)(this.fontSheetState, draft => {
                    Object.assign(draft, data);
                });
                let count = 0;
                for (const entry of data.characters) {
                    if (!entry.chr || !entry.hash)
                        continue;
                    this.registerLearnedFontChar(entry.hash, entry.chr, entry.width ?? 0, entry.height ?? 0);
                    count++;
                }
                return count;
            }
            // Legacy format
            if (data.version === 1 && data.chars) {
                let count = 0;
                for (const [hashStr, entry] of Object.entries(data.chars)) {
                    const hash = Number(hashStr);
                    if (isNaN(hash) || !entry.char)
                        continue;
                    this.registerLearnedFontChar(hash, entry.char, entry.width ?? 0, entry.height ?? 0);
                    count++;
                }
                if (count > 0)
                    this.saveLearnedFont(); // Migrate to new format
                return count;
            }
        }
        catch (e) {
            console.warn('[XP Font] Failed to parse font data:', e);
        }
        return 0;
    }
    /** Load from localStorage (fallback) */
    loadFontFromLocalStorage() {
        try {
            const raw = localStorage.getItem(XpDetector.FONT_STORAGE_KEY);
            if (!raw)
                return;
            const count = this.loadFontFromJson(raw);
            if (count > 0) {
                console.log(`[XP Font] Loaded ${count} learned characters from localStorage`);
            }
        }
        catch (e) {
            console.warn('[XP Font] Failed to load from localStorage:', e);
        }
    }
    /** Save learned font characters using immer for immutable state, persisting to JSON file */
    saveLearnedFont() {
        // Use immer to produce the next font sheet state immutably
        this.fontSheetState = (0,immer__WEBPACK_IMPORTED_MODULE_3__.produce)(this.fontSheetState, draft => {
            draft.characters = [];
            for (const [hash, entry] of this.learnedFontChars) {
                draft.characters.push({
                    chr: entry.char,
                    charcode: entry.char.charCodeAt(0),
                    x: 0, y: 0,
                    width: entry.width,
                    height: entry.height,
                    hash,
                    bearingy: 0,
                });
            }
            draft.characters.sort((a, b) => a.charcode - b.charcode);
        });
        const json = JSON.stringify(this.fontSheetState, null, 2);
        // Save to localStorage as fallback
        try {
            localStorage.setItem(XpDetector.FONT_STORAGE_KEY, json);
        }
        catch (e) { /* silent */ }
        // Save to actual JSON file via IPC
        try {
            const api = window.appWindowApi;
            if (api?.writeAppData) {
                api.writeAppData('xp-buddy', 'learned-font.json', json);
            }
        }
        catch (e) {
            console.warn('[XP Font] Failed to write font JSON file:', e);
        }
    }
    /** Log a detected XP drop to localStorage and JSON file for history tracking */
    logXpDrop(result) {
        try {
            const raw = localStorage.getItem(XpDetector.XP_LOG_KEY);
            const data = raw
                ? JSON.parse(raw)
                : { version: 1, drops: [] };
            if (data.version !== 1)
                return;
            const entry = {
                timestamp: result.timestamp,
                xp: result.xpValue,
                text: result.xpText,
                skill: result.skill?.label ?? null,
                skills: result.skills.map(s => s.label),
                type: result.dropType,
                source: result.likelySource,
                skillXp: result.skillXp.map(s => ({ skill: s.skill.label, xp: s.xp, primary: s.isPrimary })),
            };
            data.drops.push(entry);
            // Cap the log to prevent bloat
            if (data.drops.length > XpDetector.XP_LOG_MAX_ENTRIES) {
                data.drops = data.drops.slice(-XpDetector.XP_LOG_MAX_ENTRIES);
            }
            const json = JSON.stringify(data);
            localStorage.setItem(XpDetector.XP_LOG_KEY, json);
            // Also write to JSON file
            try {
                const api = window.appWindowApi;
                if (api?.writeAppData) {
                    api.writeAppData('xp-buddy', 'xp-log.json', json);
                }
            }
            catch (e) { /* silent */ }
        }
        catch (e) {
            // Silent fail - logging shouldn't break detection
        }
    }
    /**
     * Learn XP drop font characters at runtime.
     * RS3's XP drop text uses a small font (3-8px wide, 4-9px tall) not in the sprite hash database.
     * When delta tracking gives us a confirmed XP value, we cross-reference with
     * unknown ~8x12 elements visible on screen to learn hash→character mappings.
     * Once learned, readFont() can read the XP drop text directly.
     */
    learnXpFontFromDelta(elements, knownXpValue) {
        // Collect ALL font-sized elements (known AND unknown) so we can match
        // mixed rows where some chars are already learned and others aren't.
        // Previous approach only collected unknowns, which failed for rows like
        // "+1866" where "+","8","6","6" are learned but "1" is not — the unknown
        // group had length 1, never matching candidate "1866" (length 4).
        const fontSized = elements.filter(el => el.width >= 3 && el.width <= 10 &&
            el.height >= 4 && el.height <= 12);
        if (fontSized.length < 2)
            return;
        // Deduplicate anti-alias doubles: RS3 renders each character twice
        // at ~1px offset with different colors. Keep only the first of each pair.
        const deduped = [];
        const sortedByPos = [...fontSized].sort((a, b) => a.x - b.x || a.y - b.y);
        for (const el of sortedByPos) {
            const last = deduped[deduped.length - 1];
            if (last &&
                Math.abs(el.x - last.x) <= 2 &&
                Math.abs(el.y - last.y) <= 2 &&
                Math.abs(el.width - last.width) <= 1 &&
                Math.abs(el.height - last.height) <= 1) {
                continue; // Anti-alias duplicate, skip
            }
            deduped.push(el);
        }
        // Group by Y position (tolerance 4px) to find text rows
        const yGroups = new Map();
        for (const el of deduped) {
            const yKey = Math.round(el.y / 4) * 4;
            if (!yGroups.has(yKey))
                yGroups.set(yKey, []);
            yGroups.get(yKey).push(el);
        }
        // Generate candidate strings from the known XP value
        const candidates = [];
        // Most common XP drop formats (floating text)
        if (knownXpValue % 1 !== 0) {
            // Has decimal: "6.3", "+6.3"
            const dec = knownXpValue.toFixed(1);
            candidates.push(dec, "+" + dec);
        }
        const intStr = Math.round(knownXpValue).toString();
        candidates.push(intStr, "+" + intStr);
        // Also try with "xp" suffix
        if (knownXpValue % 1 !== 0) {
            candidates.push(knownXpValue.toFixed(1) + "xp");
        }
        candidates.push(intStr + "xp");
        for (const [, group] of yGroups) {
            if (group.length < 2)
                continue;
            const sorted = group.sort((a, b) => a.x - b.x);
            // Verify consistent spacing (text-like row)
            let isTextLike = true;
            for (let i = 1; i < sorted.length; i++) {
                const gap = sorted[i].x - (sorted[i - 1].x + sorted[i - 1].width);
                if (gap < -3 || gap > 12) {
                    isTextLike = false;
                    break;
                }
            }
            if (!isTextLike)
                continue;
            // Try each candidate against this row
            for (const candidate of candidates) {
                if (sorted.length !== candidate.length)
                    continue;
                // Check consistency: known chars (both from sprite DB and learned)
                // must match the candidate. At least one must be unknown (learnable).
                let consistent = true;
                let hasNewChar = false;
                for (let i = 0; i < sorted.length; i++) {
                    const el = sorted[i];
                    const hash = el.sprite.pixelhash;
                    const learned = this.learnedFontChars.get(hash);
                    const knownFontChr = el.sprite.known?.fontchr;
                    if (learned) {
                        // Already learned font-2 char — must match candidate
                        if (learned.char !== candidate[i]) {
                            consistent = false;
                            break;
                        }
                    }
                    else if (knownFontChr) {
                        // Known from sprite DB (font -1, etc.) — must match candidate
                        if (knownFontChr.chr !== candidate[i]) {
                            consistent = false;
                            break;
                        }
                    }
                    else if (el.sprite.known && !el.sprite.known.fontchr) {
                        // Known sprite but not a font char (icon, etc.) — reject
                        consistent = false;
                        break;
                    }
                    else {
                        // Unknown — can learn this char
                        hasNewChar = true;
                    }
                }
                if (!consistent || !hasNewChar)
                    continue;
                // Update confidence for each unknown hash→char pair
                for (let i = 0; i < sorted.length; i++) {
                    const hash = sorted[i].sprite.pixelhash;
                    const char = candidate[i];
                    // Skip already known chars
                    if (this.learnedFontChars.has(hash))
                        continue;
                    if (sorted[i].sprite.known)
                        continue;
                    const conf = this.fontCharConfidence.get(hash);
                    if (conf && conf.char === char) {
                        conf.count++;
                        if (conf.count >= this.FONT_LEARN_CONFIDENCE) {
                            this.registerLearnedFontChar(hash, char, sorted[i].width, sorted[i].height);
                        }
                    }
                    else if (!conf || conf.char !== char) {
                        this.fontCharConfidence.set(hash, { char, count: 1 });
                    }
                }
                break; // Found a match, stop trying other candidates
            }
        }
    }
    /**
     * Register a learned font character in the SpriteCache.
     * After registration, getUIState() will recognize this hash on future frames,
     * and readFont() will be able to read the character.
     */
    registerLearnedFontChar(hash, char, width, height) {
        // Create font sheet on first use (sprite ID -2 for XP drop font)
        if (!this.learnedFontSheet) {
            this.learnedFontSheet = new _reflect2d_spritecache__WEBPACK_IMPORTED_MODULE_2__.KnownSpriteSheet(-2, 0, 0, 0);
        }
        const charcode = char.charCodeAt(0);
        const info = this.learnedFontSheet.addCharSprite(charcode, 0, 0, width, height, hash);
        this.atlas.spriteCache.addSprite(info);
        this.learnedFontChars.set(hash, { char, width, height });
        this.saveLearnedFont();
        console.log(`[XP Font] Registered '${char}' (hash:${hash}, ${width}x${height}) - ${this.learnedFontChars.size} total chars`);
    }
    /** Set a user-defined detection zone. TextScan will only process text within this rectangle. */
    setDetectionZone(zone) {
        this.userDetectionZone = zone;
    }
    /**
     * Update the XP drop zone based on a detected drop position.
     * XP drops all start from a single spot, so we just need a fixed-size zone
     * centered on that spot (not an expanding zone).
     */
    updateDropZone(x, y, now) {
        if (!this.xpDropZone) {
            // First drop - create fixed zone around it
            this.xpDropZone = {
                minX: x - this.ZONE_MARGIN,
                maxX: x + this.ZONE_MARGIN,
                minY: y - this.ZONE_MARGIN,
                maxY: y + this.ZONE_MARGIN,
                lastSeen: now,
            };
            console.log(`[XP Zone] Created at (${x}, ${y}) ± ${this.ZONE_MARGIN}px`);
        }
        else {
            // Just update the last seen time, don't expand the zone
            this.xpDropZone.lastSeen = now;
        }
    }
    /**
     * Check if zone is still valid or should be reset
     */
    isZoneValid(now) {
        if (!this.xpDropZone)
            return false;
        return now - this.xpDropZone.lastSeen < this.ZONE_EXPIRE_MS;
    }
    /**
     * Filter elements to only those in or near the XP drop zone
     */
    filterToZone(elements, now) {
        if (!this.isZoneValid(now)) {
            // Zone expired or doesn't exist - return all elements
            return elements;
        }
        const zone = this.xpDropZone;
        return elements.filter(el => {
            // Always include XP drop sprites and skill sprites regardless of position
            const id = el.sprite.known?.id;
            if (id === UI_SPRITES.xpDrop.id || (id !== undefined && ALL_SKILL_IDS.includes(id))) {
                return true;
            }
            // For other elements (like text), filter by zone
            return el.x >= zone.minX && el.x <= zone.maxX &&
                el.y >= zone.minY && el.y <= zone.maxY;
        });
    }
    /**
     * Clean up old entries from the dedup cache
     */
    cleanupRecentDrops(now) {
        for (const [sig, entry] of this.recentDrops) {
            if (now - entry.timestamp > this.DEDUP_WINDOW_MS) {
                this.recentDrops.delete(sig);
            }
        }
    }
    /**
     * Check if this drop was recently seen at a similar position (deduplication).
     * Same XP value at a different position is NOT a duplicate (rapid kills).
     */
    isDuplicate(signature, now, x, y) {
        const entry = this.recentDrops.get(signature);
        if (!entry)
            return false;
        if (now - entry.timestamp > this.DEDUP_WINDOW_MS)
            return false;
        // Check if position is similar (same drop animating)
        const dist = Math.abs(x - entry.x) + Math.abs(y - entry.y);
        if (dist < this.DEDUP_POSITION_THRESHOLD) {
            return true; // Same value at similar position = duplicate
        }
        // Same value but different position = new drop (rapid kills)
        return false;
    }
    /**
     * Record a drop as seen at a specific position
     */
    recordDrop(signature, now, x, y) {
        this.recentDrops.set(signature, { timestamp: now, x, y });
    }
    /**
     * Clean up expired pending drops and return any that timed out
     */
    cleanupPendingDrops(now) {
        const expired = [];
        for (const [sig, pending] of this.pendingDrops) {
            if (now - pending.timestamp > this.PENDING_TIMEOUT_MS) {
                expired.push(pending);
                this.pendingDrops.delete(sig);
            }
        }
        return expired;
    }
    /**
     * Detect XP drops from render data.
     * Returns an array of detected drops (may be multiple if pending drops resolved).
     */
    detectAll(renders) {
        const results = [];
        const now = Date.now();
        // Clean up old dedup entries
        this.cleanupRecentDrops(now);
        // Clean up expired pending drops - DON'T emit them
        // They'll be re-detected with skills once the dedup window also expires
        // This prevents double-counting (expired pending + later detection with skills)
        const expiredPending = this.cleanupPendingDrops(now);
        for (const pending of expiredPending) {
            console.log(`[XP Pending Expired] ${pending.xpValue} XP expired, will re-detect with skills`);
        }
        // Diagnostic: Count renders by program type to detect if XP drops are filtered by isUi
        if (!this._hasLoggedDebug) {
            let uiCount = 0, scalerCount = 0, meshCount = 0, particleCount = 0, floorCount = 0, otherCount = 0;
            for (const render of renders) {
                const meta = (0,_render_renderprogram__WEBPACK_IMPORTED_MODULE_4__.getProgramMeta)(render.program);
                if (meta.isUi)
                    uiCount++;
                else if (meta.isUiScaler)
                    scalerCount++;
                else if (meta.isMainMesh)
                    meshCount++;
                else if (meta.isParticles)
                    particleCount++;
                else if (meta.isFloor)
                    floorCount++;
                else
                    otherCount++;
            }
            console.log(`[XP Pipeline] ${renders.length} renders - UI:${uiCount}, Scaler:${scalerCount}, Mesh:${meshCount}, Particles:${particleCount}, Floor:${floorCount}, Other:${otherCount}`);
        }
        const state = (0,_reflect2d_reflect2d__WEBPACK_IMPORTED_MODULE_1__.getUIState)(renders, this.atlas);
        // Log render stats (helps diagnose if XP drops are missing from UI pipeline)
        if (!this._hasLoggedDebug) {
            console.log(`[XP Debug] Processing ${renders.length} render invocations, got ${state.elements.length} UI elements`);
        }
        // Debug: Log element counts (less frequently)
        const knownSprites = state.elements.filter(el => el.sprite.known);
        const xpDropSprites = state.elements.filter(el => el.sprite.known?.id === UI_SPRITES.xpDrop.id);
        const fontChars = state.elements.filter(el => el.sprite.known?.fontchr);
        if (state.elements.length > 0 && !this._hasLoggedDebug) {
            console.log(`[XP Debug] Elements: ${state.elements.length}, Known: ${knownSprites.length}, XP Drop sprites (3795): ${xpDropSprites.length}, Font chars: ${fontChars.length}`);
            // Log ALL unique known sprite IDs
            if (knownSprites.length > 0) {
                const allIds = [...new Set(knownSprites.map(el => el.sprite.known?.id))];
                console.log(`[XP Debug] ALL unique known sprite IDs (${allIds.length}): ${allIds.join(", ")}`);
            }
            // Log elements in the XP drop zone (upper portion of screen, y < 300)
            const xpZoneElements = state.elements.filter(el => el.y < 300);
            const xpZoneKnown = xpZoneElements.filter(el => el.sprite.known);
            const xpZoneUnknown = xpZoneElements.filter(el => !el.sprite.known);
            const xpZoneFonts = xpZoneElements.filter(el => el.sprite.known?.fontchr);
            console.log(`[XP Debug] XP Zone (y<300): ${xpZoneElements.length} elements, ${xpZoneKnown.length} known, ${xpZoneUnknown.length} unknown, ${xpZoneFonts.length} font chars`);
            if (xpZoneKnown.length > 0) {
                const zoneInfo = xpZoneKnown.map(el => {
                    const id = el.sprite.known.id;
                    const fc = el.sprite.known.fontchr;
                    const label = fc ? `font:'${fc.chr}'` : `ID:${id}`;
                    return `${label}@(${Math.round(el.x)},${Math.round(el.y)})`;
                });
                console.log(`[XP Debug] XP Zone known sprites: ${zoneInfo.join(", ")}`);
            }
            if (xpZoneUnknown.length > 0) {
                const unknownInfo = xpZoneUnknown.slice(0, 20).map(el => `hash:${el.sprite.pixelhash}@(${Math.round(el.x)},${Math.round(el.y)},${Math.round(el.width)}x${Math.round(el.height)})`);
                console.log(`[XP Debug] XP Zone UNKNOWN sprites (first 20): ${unknownInfo.join(", ")}`);
            }
            // Check for unknown sprites that could be the XP drop indicator (sprite 3795 with updated hash)
            const potentialXpIndicators = xpZoneUnknown.filter(el => el.width >= 10 && el.width <= 40 && el.height >= 10 && el.height <= 40);
            if (potentialXpIndicators.length > 0) {
                const indicatorInfo = potentialXpIndicators.map(el => `hash:${el.sprite.pixelhash} @(${Math.round(el.x)},${Math.round(el.y)}) ${Math.round(el.width)}x${Math.round(el.height)}`);
                console.log(`[XP Debug] POTENTIAL XP indicators (icon-sized unknowns in XP zone): ${indicatorInfo.join(" | ")}`);
            }
            // Log ALL skill sprites detected on screen
            const allSkillSprites = state.elements.filter(el => {
                const id = el.sprite.known?.id;
                return id !== undefined && ALL_SKILL_IDS.includes(id);
            });
            if (allSkillSprites.length > 0) {
                const skillInfo = allSkillSprites.map(el => {
                    const id = el.sprite.known.id;
                    const label = SKILL_ID_TO_INFO.get(id)?.label ?? `ID:${id}`;
                    return `${label}@(${el.x},${el.y})`;
                });
                console.log(`[XP Debug] ALL skill sprites on screen: ${skillInfo.join(", ")}`);
            }
            // TEXT SCANNER: Read ALL text strings on screen for debugging
            const textScanner = new _reflect2d_uiparser__WEBPACK_IMPORTED_MODULE_0__.UIRenderParser(state.elements);
            const allTexts = [];
            while (textScanner.index < textScanner.end) {
                const el = textScanner.peek();
                if (!el)
                    break;
                if (el.sprite.known?.fontchr) {
                    const font = textScanner.readFont();
                    if (font.text.trim().length > 0) {
                        allTexts.push({ text: font.text.trim(), x: font.x, y: font.y, color: font.color });
                    }
                }
                else {
                    textScanner.skip(1);
                }
            }
            if (allTexts.length > 0) {
                // Show texts that PASS XP validation (potential XP drops)
                const xpCandidates = allTexts.filter(t => {
                    if (t.text.includes("/") || t.text.includes("%"))
                        return false;
                    const val = this.parseXpValue(t.text);
                    return val >= 1 && val <= 10000000;
                });
                if (xpCandidates.length > 0) {
                    const candidateInfo = xpCandidates.map(t => {
                        const c = t.color ? `rgb(${t.color[0]},${t.color[1]},${t.color[2]})` : '?';
                        const val = this.parseXpValue(t.text);
                        return `"${t.text}"=${val}XP @(${Math.round(t.x)},${Math.round(t.y)}) ${c}`;
                    });
                    console.log(`[XP Debug] XP CANDIDATES (${xpCandidates.length}): ${candidateInfo.join(" | ")}`);
                }
                else {
                    console.log(`[XP Debug] No XP candidates found in ${allTexts.length} text strings on screen`);
                }
                // Also log all text briefly for debugging (reduced noise)
                const textSummary = allTexts.slice(0, 15).map(t => `"${t.text}"@(${Math.round(t.x)},${Math.round(t.y)})`);
                console.log(`[XP Debug] Text sample (${allTexts.length} total): ${textSummary.join(" | ")}`);
            }
            this._hasLoggedDebug = true;
            // Reset after 5 seconds to log again
            setTimeout(() => { this._hasLoggedDebug = false; }, 30000);
        }
        // Filter elements at valid Y positions (not faded drops)
        const allElements = state.elements.filter(el => {
            if (el.sprite.known?.id === UI_SPRITES.xpDrop.id) {
                return el.y >= 15;
            }
            return true;
        });
        // Apply zone filtering to reduce processing if we have a known XP drop area
        const elements = this.filterToZone(allElements, now);
        // Log zone stats occasionally
        if (this._hasLoggedDebug && this.xpDropZone && this.isZoneValid(now)) {
            const reduction = ((1 - elements.length / allElements.length) * 100).toFixed(0);
            console.log(`[XP Zone] Filtered ${allElements.length} → ${elements.length} elements (${reduction}% reduction)`);
        }
        // Try to resolve pending drops by looking for skills NEAR the pending drop's position
        for (const [sig, pending] of this.pendingDrops) {
            // Look for skill sprites near this specific pending drop, not anywhere on screen
            const nearbySkills = this.findSkillsNearPosition(elements, pending.approxX, pending.approxY);
            if (nearbySkills.length > 0) {
                // Found skills near this drop! Resolve it
                this.pendingDrops.delete(sig);
                const dropType = this.classifyDropType(nearbySkills);
                const skill = nearbySkills.length === 1 ? nearbySkills[0] : null;
                const skillXp = this.calculateSkillXp(pending.xpValue, dropType, skill, nearbySkills);
                let likelySource = null;
                if (dropType === XpDropType.SINGLE_SKILL && skill) {
                    likelySource = identifySource(pending.xpValue, skill.id);
                }
                // Update zone with pending drop's original position
                this.updateDropZone(pending.approxX, pending.approxY, now);
                const xpBreakdown = skillXp.map(s => `${s.skill.label}:${s.xp}${s.isPrimary ? "*" : ""}`).join(", ");
                const sourceInfo = likelySource ? ` [${likelySource}]` : "";
                console.log(`[XP Pending Resolved] ${pending.xpValue} XP at (${pending.approxX}, ${pending.approxY}) - ${dropType} (${xpBreakdown})${sourceInfo}`);
                // Get cached sprites for the skills
                const skillSpriteImages = nearbySkills
                    .map(s => skillSpriteCache.get(s.id))
                    .filter((s) => s !== undefined);
                results.push({
                    xpValue: pending.xpValue,
                    xpText: pending.xpText,
                    skill,
                    skillSprite: null,
                    dropType,
                    skills: nearbySkills,
                    skillXp,
                    skillSpriteImages,
                    timestamp: pending.timestamp,
                    likelySource,
                });
            }
        }
        // PRIMARY: Text-based detection with delta tracking
        // This is the most reliable method - reads cumulative XP counters and emits deltas
        const textResults = this.detectByTextScan(elements, now);
        if (textResults.length > 0) {
            results.push(...textResults);
            return results;
        }
        // SECONDARY: Pattern match using sprite 3795 (when hash is up-to-date)
        const parser = new _reflect2d_uiparser__WEBPACK_IMPORTED_MODULE_0__.UIRenderParser(elements);
        const match = parser.searchUIPattern(XP_DROP_PATTERN);
        if (!match) {
            this.edgeDetector.processFrame(null);
            // TERTIARY: Fallback - skill sprites outside panel + nearby text
            const fallbackResults = this.detectFallback(elements, now);
            results.push(...fallbackResults);
            return results;
        }
        // Parse XP text
        const xpText = match.xpText.text.trim();
        const xpValue = this.parseXpValue(xpText);
        if (!this._hasLoggedDebug) {
            console.log(`[XP Pattern] Found pattern, text="${xpText}", parsed=${xpValue}`);
        }
        if (xpValue < 1) {
            console.log(`[XP Pattern] Rejected: invalid XP text or value < 1 (text="${xpText}")`);
            return results;
        }
        // Brightness validation
        const textElement = match.xpText.match;
        if (textElement) {
            try {
                const { x, y, width, height } = textElement.sprite;
                const imgData = (0,_reflect2d_reflect2d__WEBPACK_IMPORTED_MODULE_1__.safeCapture)(textElement.sprite.basetex, x, y, width, height);
                if (imgData) {
                    const brightness = analyzeBrightness(imgData);
                    // Log brightness for debugging
                    if (!this._hasLoggedDebug) {
                        console.log(`[XP Brightness] avg=${brightness.avgBrightness.toFixed(1)}, ` +
                            `max=${brightness.maxBrightness.toFixed(1)}, ` +
                            `ratio=${(brightness.brightPixelRatio * 100).toFixed(1)}%, ` +
                            `alpha=${brightness.avgAlpha.toFixed(1)}, ` +
                            `visible=${brightness.visiblePixels}/${brightness.totalPixels}`);
                    }
                    // Validate brightness matches XP drop characteristics
                    if (!isBrightnessValidForXpDrop(brightness)) {
                        console.log(`[XP Brightness] Rejected: doesn't match XP drop characteristics`);
                        return results;
                    }
                }
            }
            catch (e) {
                console.log(`[XP Brightness] Capture failed: ${e}`);
            }
        }
        // Edge detection: only emit on first frame of a new drop
        if (!this.edgeDetector.processFrame(xpText)) {
            return results;
        }
        // Position vars for zone tracking and TextScan dedup compatibility
        const signature = `${xpValue}`;
        const dropX = textElement?.x ?? 0;
        const dropY = textElement?.y ?? 0;
        // Find the index of the XP text element in the render order
        // Only consider elements rendered near the XP drop (like Chatbox_Reader does)
        const xpTextIndex = elements.findIndex(el => el === match.xpText.match);
        const startIndex = Math.max(0, xpTextIndex - MAX_RENDER_ORDER_DISTANCE);
        const endIndex = Math.min(elements.length, xpTextIndex + MAX_RENDER_ORDER_DISTANCE);
        const nearbyElements = elements.slice(startIndex, endIndex);
        console.log(`[XP Render Order] XP text at index ${xpTextIndex}, searching indices ${startIndex}-${endIndex} (${nearbyElements.length} elements)`);
        // Find associated skill sprites (only from nearby render elements)
        const { skill, skillSprite, skills } = this.findAssociatedSkill(nearbyElements, match.xpText.match);
        // Classify the drop type based on skills present
        const dropType = this.classifyDropType(skills);
        // If no skills found, add to pending and wait for next frame
        if (skills.length === 0) {
            const pendingSig = `${xpValue}`;
            if (!this.pendingDrops.has(pendingSig)) {
                const approxX = textElement?.x ?? 0;
                const approxY = textElement?.y ?? 0;
                console.log(`[XP Pending] ${xpValue} XP added to pending at (${approxX}, ${approxY}), waiting for skills...`);
                this.pendingDrops.set(pendingSig, {
                    xpValue,
                    xpText,
                    timestamp: now,
                    approxX,
                    approxY,
                });
                this.updateDropZone(approxX, approxY, now);
                this.recordDrop(signature, now, approxX, approxY);
            }
            return results;
        }
        // Record this drop in dedup
        this.recordDrop(signature, now, dropX, dropY);
        // Calculate per-skill XP attribution based on drop type
        const skillXp = this.calculateSkillXp(xpValue, dropType, skill, skills);
        // Identify source only for single-skill drops
        let likelySource = null;
        if (dropType === XpDropType.SINGLE_SKILL && skill) {
            likelySource = identifySource(xpValue, skill.id);
        }
        // Update the XP drop zone based on this detection's position
        if (textElement) {
            this.updateDropZone(textElement.x, textElement.y, now);
        }
        // Debug: Log successful detection with position
        const pos = textElement ? `(${textElement.x}, ${textElement.y})` : "(unknown)";
        const xpBreakdown = skillXp.map(s => `${s.skill.label}:${s.xp}${s.isPrimary ? "*" : ""}`).join(", ");
        const sourceInfo = likelySource ? ` [${likelySource}]` : "";
        console.log(`[XP Detected] ${xpValue} XP at ${pos} - ${dropType} (${xpBreakdown})${sourceInfo}`);
        // Cache skill sprites from this detection and get cached images
        const skillSpriteImages = [];
        for (const s of skills) {
            // Try to get from cache first
            let cached = skillSpriteCache.get(s.id);
            // If not cached and we have a skillSprite for this skill, cache it
            if (!cached && skillSprite && skillSprite.sprite.known?.id === s.id) {
                cached = skillSpriteCache.cacheFromRenderRect(skillSprite) ?? undefined;
            }
            if (cached) {
                skillSpriteImages.push(cached);
            }
        }
        results.push({
            xpValue,
            xpText,
            skill,
            skillSprite,
            dropType,
            skills,
            skillXp,
            skillSpriteImages,
            timestamp: now,
            likelySource,
        });
        return results;
    }
    /**
     * Text-based XP detection: scans ALL text on screen for XP values.
     * Does not rely on sprite 3795. Finds XP text outside known UI panels
     * and associates with nearby skill sprites.
     */
    detectByTextScan(elements, now) {
        const results = [];
        // Step 1: Find skill panel bounding box
        const skillSprites = [];
        for (let i = 0; i < elements.length; i++) {
            const id = elements[i].sprite.known?.id;
            if (id !== undefined && ALL_SKILL_IDS.includes(id)) {
                skillSprites.push({ el: elements[i], index: i });
            }
        }
        let panelBox = null;
        if (skillSprites.length >= 5) {
            for (const seed of skillSprites) {
                const cluster = skillSprites.filter(s => Math.abs(s.el.x - seed.el.x) < 300 && Math.abs(s.el.y - seed.el.y) < 300);
                if (!panelBox || cluster.length > 0) {
                    const xs = cluster.map(s => s.el.x);
                    const ys = cluster.map(s => s.el.y);
                    const box = {
                        minX: Math.min(...xs) - 20,
                        maxX: Math.max(...xs) + 60,
                        minY: Math.min(...ys) - 120,
                        maxY: Math.max(...ys) + 40,
                    };
                    if (!panelBox || cluster.length > (panelBox._count ?? 0)) {
                        box._count = cluster.length;
                        panelBox = box;
                    }
                }
            }
        }
        // Step 2: Scan all text strings from render data
        const parser = new _reflect2d_uiparser__WEBPACK_IMPORTED_MODULE_0__.UIRenderParser(elements);
        const textRuns = [];
        while (parser.index < parser.end) {
            const el = parser.peek();
            if (!el)
                break;
            if (el.sprite.known?.fontchr) {
                const font = parser.readFont();
                if (font.text.trim().length > 0) {
                    textRuns.push({
                        text: font.text.trim(),
                        x: font.x, y: font.y,
                        width: font.width, height: font.height,
                        startIndex: font.startindex, endIndex: font.endindex,
                        color: font.color,
                    });
                }
            }
            else {
                parser.skip(1);
            }
        }
        // Debug: log unknown (unrecognized) elements that could be XP drop font characters
        // These are elements rendered as UI but whose pixel hashes don't match any known font
        // Only log on first frame to avoid spam
        if (this.textScanFrameCount === 0) {
            const unknownEls = elements.filter(el => !el.sprite.known);
            const smallUnknowns = unknownEls.filter(el => el.width > 0 && el.width < 30 && el.height > 0 && el.height < 30);
            if (smallUnknowns.length > 0) {
                // Group by Y position (tolerance of 5px) to find text-like rows
                const yGroups = new Map();
                for (const el of smallUnknowns) {
                    const yKey = Math.round(el.y / 5) * 5;
                    if (!yGroups.has(yKey))
                        yGroups.set(yKey, []);
                    yGroups.get(yKey).push(el);
                }
                // Log rows with 3+ elements (likely text characters)
                const textRows = [...yGroups.entries()].filter(([, els]) => els.length >= 3);
                if (textRows.length > 0) {
                    for (const [yKey, els] of textRows) {
                        const sorted = els.sort((a, b) => a.x - b.x);
                        const info = sorted.slice(0, 15).map(el => `${Math.round(el.x)},${Math.round(el.y)} ${Math.round(el.width)}x${Math.round(el.height)} h:${el.sprite.pixelhash} c:[${el.color[0]},${el.color[1]},${el.color[2]}]`).join(" | ");
                        console.log(`[TextScan][UnknownRow] y≈${yKey} (${els.length} chars): ${info}`);
                    }
                }
            }
        }
        // Debug: log all text runs found when zone is active
        if (this.userDetectionZone && textRuns.length > 0) {
            const z = this.userDetectionZone;
            const inZone = textRuns.filter(r => r.x >= z.x && r.x <= z.x + z.width && r.y >= z.y && r.y <= z.y + z.height);
            if (inZone.length > 0) {
                console.log(`[TextScan][Zone] ${inZone.length} text runs in zone:`, inZone.map(r => `"${r.text}" @(${Math.round(r.x)},${Math.round(r.y)}) color=${r.color ? `[${r.color[0]},${r.color[1]},${r.color[2]}]` : 'null'}`));
            }
        }
        // Merge adjacent text runs that were split by unrecognized characters (like ".")
        // readFont() stops at unknown elements, so "6.3xp" might become ["6", "3xp"]
        // Merging reconnects them with inferred separators, but ONLY if the runs are
        // adjacent in the render stream (preventing false merges of unrelated numbers)
        if (textRuns.length > 1) {
            const mergedRuns = [];
            let current = { ...textRuns[0] };
            for (let i = 1; i < textRuns.length; i++) {
                const next = textRuns[i];
                const sameLineY = Math.abs(next.y - current.y) < 8;
                const gapX = next.x - (current.x + current.width);
                const indexGap = next.startIndex - current.endIndex;
                // Only merge if runs are adjacent in render stream (at most 3 elements apart)
                // AND spatially close. This prevents merging "6" from XP counter with "3" from
                // a nearby level display just because they're on the same line.
                if (sameLineY && gapX >= 0 && gapX < 15 && indexGap >= 0 && indexGap <= 3) {
                    // If gap is 2-10px, likely a period or small character was skipped
                    const separator = (gapX >= 2 && gapX <= 10) ? "." : "";
                    current = {
                        ...current,
                        text: current.text + separator + next.text,
                        width: (next.x + next.width) - current.x,
                        height: Math.max(current.height, next.height),
                        endIndex: next.endIndex,
                    };
                }
                else {
                    mergedRuns.push(current);
                    current = { ...next };
                }
            }
            mergedRuns.push(current);
            // Log merges that happened (only if merging changed something)
            if (mergedRuns.length < textRuns.length) {
                console.log(`[TextScan] Merged ${textRuns.length} text runs → ${mergedRuns.length} (reconnected split text)`);
            }
            // Replace textRuns contents with merged version
            textRuns.length = 0;
            textRuns.push(...mergedRuns);
        }
        // Step 3: Filter for XP-like text outside known UI panels
        for (const run of textRuns) {
            // Skip text inside skill panel
            if (panelBox && run.x >= panelBox.minX && run.x <= panelBox.maxX &&
                run.y >= panelBox.minY && run.y <= panelBox.maxY) {
                continue;
            }
            // If user has set a detection zone, only process text within it
            if (this.userDetectionZone) {
                const z = this.userDetectionZone;
                if (run.x < z.x || run.x > z.x + z.width || run.y < z.y || run.y > z.y + z.height)
                    continue;
            }
            // Skip text at very top of screen (y < 30) - total XP counter area
            if (run.y < 30)
                continue;
            // Color filter - only when NO detection zone is active
            // When zone is active, the zone constraint prevents false positives and
            // we need to accept faded/dimmed colors from XP drops animating upward.
            // Skip color filter for text starting with "+" — these are floating XP drops
            // that can be any color (white, green, red shadow, etc.)
            if (!this.userDetectionZone && run.color && !run.text.trim().startsWith('+')) {
                const [r, g, b] = run.color;
                const isWhite = r > 200 && g > 200 && b > 200;
                const isYellow = r > 180 && g > 130 && b < 100;
                const isGreen = r < 150 && g > 180 && b < 100;
                const isOrange = r > 200 && g > 80 && b < 80;
                if (!isWhite && !isYellow && !isGreen && !isOrange)
                    continue;
            }
            // Skip static UI text - text at the same position as previous frame is not an XP drop
            // XP drops appear suddenly and animate upward; static UI text stays put
            const posKey = `${Math.round(run.x / 15) * 15}_${Math.round(run.y / 15) * 15}`;
            if (this.previousScanTexts.get(posKey) === run.text) {
                if (this.userDetectionZone)
                    console.log(`[TextScan][Zone] SKIP static: "${run.text}" @(${Math.round(run.x)},${Math.round(run.y)})`);
                continue;
            }
            // Check if text looks like an XP value
            let xpValue = this.parseXpValue(run.text);
            if (xpValue < 1) {
                if (this.userDetectionZone)
                    console.log(`[TextScan][Zone] SKIP not-xp: "${run.text}" @(${Math.round(run.x)},${Math.round(run.y)})`);
                continue;
            }
            // Floating XP drops start with "+" (e.g., "+6", "+75", "+8").
            // They animate upward, constantly changing position. Delta tracking (designed
            // for fixed-position cumulative counters) would always treat them as "first
            // time at this position" and skip them. Bypass delta tracking for these.
            const isFloatingDrop = run.text.trim().charAt(0) === '+';
            // Delta tracking for XP counter positions
            // RS3 XP tracker counters show cumulative totals at fixed screen positions.
            // When a counter value changes (e.g., 2512 → 2637), the actual XP gain is
            // the delta (125), not the absolute value (2637).
            let isDeltaTracked = false;
            const prevXpAtPos = this.positionXpValues.get(posKey);
            if (prevXpAtPos !== undefined && !isFloatingDrop) {
                // Known position - use delta instead of absolute value
                const rawDelta = xpValue - prevXpAtPos;
                let delta = Math.round(rawDelta); // RS3 tracks fractional XP internally but displays integers
                this.positionXpValues.set(posKey, xpValue);
                if (delta <= 0) {
                    // Counter reset, no change, or decrease - skip
                    continue;
                }
                // Sanity check: if we have a typical delta for this position and the new
                // delta is more than 5x larger, it's likely a position quantization error
                // or a stale value from a different counter. Re-seed and skip.
                const typicalDelta = this.positionTypicalDelta.get(posKey);
                if (typicalDelta !== undefined && delta > typicalDelta * 5 && delta > 500) {
                    console.log(`[TextScan] SKIP anomalous delta: ${delta} (typical=${typicalDelta}) @(${Math.round(run.x)},${Math.round(run.y)}) - re-seeding position`);
                    // Don't emit, just keep the updated position value for next frame
                    continue;
                }
                // Update typical delta using exponential moving average
                if (typicalDelta === undefined) {
                    this.positionTypicalDelta.set(posKey, delta);
                }
                else {
                    // Blend: 70% old typical, 30% new value
                    this.positionTypicalDelta.set(posKey, typicalDelta * 0.7 + delta * 0.3);
                }
                // Snap stabilization: RS3 tracks fractional XP (e.g., 67.5 per action).
                // When counter < 1000, text correctly reads "67.5" → delta = 68 (rounded).
                // When counter >= 1000, text loses ".5" (comma splits text) → delta alternates 67/68.
                // Lock onto the first consistent delta and snap ±1 variations to it.
                const stable = this.positionStableDelta.get(posKey);
                if (stable) {
                    if (delta === stable.value) {
                        stable.count++;
                        stable.snapCount = 0; // Natural match resets snap counter
                    }
                    else if (Math.abs(delta - stable.value) <= 1 && stable.count >= 3) {
                        // Within ±1 of locked value - snap to stable
                        stable.snapCount++;
                        if (stable.snapCount > 10) {
                            // Too many consecutive snaps without natural match - true delta changed
                            this.positionStableDelta.set(posKey, { value: delta, count: 1, snapCount: 0 });
                        }
                        else {
                            delta = stable.value;
                        }
                    }
                    else if (Math.abs(delta - stable.value) > 1) {
                        // Significantly different (e.g., frame miss double or new activity)
                        this.positionStableDelta.set(posKey, { value: delta, count: 1, snapCount: 0 });
                    }
                }
                else {
                    this.positionStableDelta.set(posKey, { value: delta, count: 1, snapCount: 0 });
                }
                console.log(`[TextScan] Delta: "${run.text}" abs=${xpValue} prev=${prevXpAtPos} delta=${delta} @(${Math.round(run.x)},${Math.round(run.y)})`);
                xpValue = delta;
                isDeltaTracked = true;
                // Use confirmed delta value to learn the XP drop font characters
                this.learnXpFontFromDelta(elements, delta);
            }
            else {
                // First time seeing this position.
                if (isFloatingDrop) {
                    // Floating XP drops ("+75 xp", "+8") animate upward - every frame is a
                    // "new" position. Let them through for direct emission. Don't seed delta
                    // tracking to avoid polluting counter history.
                }
                else {
                    // Record the value for delta tracking on next frame.
                    // NEVER emit absolute values from new positions - bank/inventory numbers would
                    // cause false positives. Only emit when the value CHANGES (delta > 0).
                    this.positionXpValues.set(posKey, xpValue);
                    continue;
                }
            }
            // Cap at 10M - no single XP drop exceeds this in RS3
            if (xpValue > 10000000)
                continue;
            // Dedup check - delta-tracked values use position-based signatures with short window
            // (the delta computation already prevents duplicates - same counter value = delta 0 = skipped).
            // Non-delta values use value-only dedup with wider window (XP drops animate upward).
            const signature = isDeltaTracked ? `delta:${posKey}:${xpValue}` : `textscan:${xpValue}`;
            const dedupWindow = isDeltaTracked ? 400 : 2000;
            const existingDrop = this.recentDrops.get(signature);
            if (existingDrop && (now - existingDrop.timestamp) < dedupWindow) {
                if (this.userDetectionZone)
                    console.log(`[TextScan][Zone] SKIP dedup: "${run.text}" = ${xpValue}xp`);
                continue;
            }
            // Look for nearby skill sprites (outside panel)
            const nearbySkills = this.findSkillsNearPosition(elements, run.x, run.y);
            // Accept XP text without nearby skill sprite if:
            // 1. It's a floating drop ("+" prefix - strong RS3 XP drop indicator), OR
            // 2. Detection zone is active (zone constraint prevents false positives), OR
            // 3. Text is white/near-white (RS3 XP drops are often white)
            if (nearbySkills.length === 0) {
                const isWhiteText = run.color && run.color[0] > 200 && run.color[1] > 200 && run.color[2] > 200;
                if (!isFloatingDrop && !this.userDetectionZone && !isWhiteText)
                    continue;
                console.log(`[TextScan] ACCEPT without skill sprite: "${run.text}" = ${xpValue}xp @(${Math.round(run.x)},${Math.round(run.y)}) color=${run.color ? `rgb(${run.color[0]},${run.color[1]},${run.color[2]})` : '?'}${isFloatingDrop ? ' [floating]' : ''}`);
            }
            const dropType = this.classifyDropType(nearbySkills);
            const skill = nearbySkills.length === 1 ? nearbySkills[0] : null;
            const skillXp = this.calculateSkillXp(xpValue, dropType, skill, nearbySkills);
            console.log(`[XP TextScan] Detected "${run.text}" = ${xpValue} XP at (${Math.round(run.x)},${Math.round(run.y)}) color=${run.color ? `rgb(${run.color[0]},${run.color[1]},${run.color[2]})` : '?'} skills=[${nearbySkills.map(s => s.label).join(',')}]`);
            this.recordDrop(signature, now, run.x, run.y);
            const skillSpriteImages = nearbySkills
                .map(s => skillSpriteCache.get(s.id))
                .filter((s) => s !== undefined);
            results.push({
                xpValue,
                xpText: run.text,
                skill,
                skillSprite: null,
                dropType,
                skills: nearbySkills,
                skillXp,
                skillSpriteImages,
                timestamp: now,
                likelySource: null,
            });
        }
        // Update previous frame text positions for static text rejection
        this.previousScanTexts.clear();
        for (const run of textRuns) {
            const posKey = `${Math.round(run.x / 15) * 15}_${Math.round(run.y / 15) * 15}`;
            this.previousScanTexts.set(posKey, run.text);
        }
        this.textScanFrameCount++;
        return results;
    }
    /**
     * Backward-compatible detect method - returns first result or null
     */
    detect(renders) {
        const results = this.detectAll(renders);
        for (const result of results) {
            this.logXpDrop(result);
        }
        return results.length > 0 ? results[0] : null;
    }
    /**
     * Fallback XP detection: find skill sprites outside the skill panel
     * and read nearby text as XP values.
     * Used when sprite 3795 (XP drop indicator) is not present.
     */
    detectFallback(elements, now) {
        const results = [];
        // Step 1: Find all skill sprites and determine the skill panel region
        const skillSprites = [];
        for (let i = 0; i < elements.length; i++) {
            const id = elements[i].sprite.known?.id;
            if (id !== undefined && ALL_SKILL_IDS.includes(id)) {
                skillSprites.push({ el: elements[i], index: i });
            }
        }
        if (skillSprites.length < 2)
            return results;
        // Step 2: Detect the skill panel bounding box
        // The skill panel has many skill sprites clustered together in a grid
        // Use a simple approach: find the bounding box that contains the most skills
        // within a reasonable area (300x300 pixels)
        let bestCluster = null;
        for (const seed of skillSprites) {
            const clusterSprites = skillSprites.filter(s => Math.abs(s.el.x - seed.el.x) < 300 && Math.abs(s.el.y - seed.el.y) < 300);
            if (!bestCluster || clusterSprites.length > bestCluster.count) {
                const xs = clusterSprites.map(s => s.el.x);
                const ys = clusterSprites.map(s => s.el.y);
                bestCluster = {
                    minX: Math.min(...xs) - 20,
                    maxX: Math.max(...xs) + 40,
                    minY: Math.min(...ys) - 20,
                    maxY: Math.max(...ys) + 40,
                    count: clusterSprites.length,
                };
            }
        }
        if (!bestCluster || bestCluster.count < 5)
            return results;
        // Step 3: Find skill sprites OUTSIDE the skill panel
        const outlierSkills = skillSprites.filter(s => s.el.x < bestCluster.minX || s.el.x > bestCluster.maxX ||
            s.el.y < bestCluster.minY || s.el.y > bestCluster.maxY);
        if (outlierSkills.length === 0)
            return results;
        // Only log fallback panel info once per 10 seconds
        const fallbackPanelKey = `panel:${outlierSkills.length}:${outlierSkills.map(s => `${Math.round(s.el.x / 10) * 10}`).join(',')}`;
        if (!this.fallbackRejectCache.has(fallbackPanelKey) || (now - this.fallbackRejectCache.get(fallbackPanelKey)) > 10000) {
            console.log(`[XP Fallback] Skill panel at (${bestCluster.minX},${bestCluster.minY})-(${bestCluster.maxX},${bestCluster.maxY}) with ${bestCluster.count} skills`);
            console.log(`[XP Fallback] Found ${outlierSkills.length} outlier skill sprites: ${outlierSkills.map(s => {
                const label = SKILL_ID_TO_INFO.get(s.el.sprite.known.id)?.label ?? 'Unknown';
                return `${label}@(${Math.round(s.el.x)},${Math.round(s.el.y)})`;
            }).join(', ')}`);
            this.fallbackRejectCache.set(fallbackPanelKey, now);
        }
        // Step 4: For each outlier skill sprite, try to read nearby text
        for (const outlier of outlierSkills) {
            const skillId = outlier.el.sprite.known.id;
            const skillInfo = SKILL_ID_TO_INFO.get(skillId);
            if (!skillInfo)
                continue;
            // Look for font characters near this skill sprite in the render order
            // Check elements AFTER the skill sprite (XP text usually renders after the icon)
            const searchStart = Math.max(0, outlier.index - 5);
            const searchEnd = Math.min(elements.length, outlier.index + MAX_RENDER_ORDER_DISTANCE);
            let xpText = "";
            let textElement = null;
            for (let i = outlier.index + 1; i < searchEnd; i++) {
                const el = elements[i];
                if (el.sprite.known?.fontchr) {
                    // Found font character - use UIRenderParser to read the full text
                    const textParser = new _reflect2d_uiparser__WEBPACK_IMPORTED_MODULE_0__.UIRenderParser(elements, i, searchEnd);
                    const fontResult = textParser.readFont();
                    if (fontResult.text.trim().length > 0) {
                        xpText = fontResult.text.trim();
                        textElement = elements[fontResult.startindex];
                        break;
                    }
                }
                // If we hit another non-font known sprite that's not a skill, stop looking
                if (el.sprite.known && !el.sprite.known.fontchr && !ALL_SKILL_IDS.includes(el.sprite.known.id)) {
                    break;
                }
            }
            // Also check BEFORE the skill sprite (text might render before icon)
            if (!xpText) {
                for (let i = outlier.index - 1; i >= searchStart; i--) {
                    const el = elements[i];
                    if (el.sprite.known?.fontchr) {
                        // Find the start of this text run
                        let textStart = i;
                        while (textStart > 0 && elements[textStart - 1].sprite.known?.fontchr) {
                            textStart--;
                        }
                        const textParser = new _reflect2d_uiparser__WEBPACK_IMPORTED_MODULE_0__.UIRenderParser(elements, textStart, outlier.index);
                        const fontResult = textParser.readFont();
                        if (fontResult.text.trim().length > 0) {
                            xpText = fontResult.text.trim();
                            textElement = elements[fontResult.startindex];
                            break;
                        }
                    }
                    if (el.sprite.known && !el.sprite.known.fontchr && !ALL_SKILL_IDS.includes(el.sprite.known.id)) {
                        break;
                    }
                }
            }
            if (!xpText) {
                continue;
            }
            // Parse XP value
            const xpValue = this.parseXpValue(xpText);
            if (xpValue < 1) {
                const rejectKey = `${skillInfo.label}:${Math.round(outlier.el.x / 10) * 10}:${Math.round(outlier.el.y / 10) * 10}:${xpText.replace(/[0-9.]/g, '')}`;
                if (!this.fallbackRejectCache.has(rejectKey) || (now - this.fallbackRejectCache.get(rejectKey)) > 10000) {
                    console.log(`[XP Fallback] Rejected: "${xpText}" near ${skillInfo.label} (not a valid XP value)`);
                    this.fallbackRejectCache.set(rejectKey, now);
                }
                continue;
            }
            // Dedup check
            const signature = `${xpValue}`;
            const dropX = textElement?.x ?? outlier.el.x;
            const dropY = textElement?.y ?? outlier.el.y;
            if (this.isDuplicate(signature, now, dropX, dropY)) {
                continue;
            }
            console.log(`[XP Fallback] Detected ${xpValue} XP for ${skillInfo.label}`);
            // Record and emit
            this.recordDrop(signature, now, dropX, dropY);
            this.updateDropZone(dropX, dropY, now);
            // Cache the skill sprite
            skillSpriteCache.cacheFromRenderRect(outlier.el);
            const spriteImage = skillSpriteCache.get(skillId);
            const skillSpriteImages = spriteImage ? [spriteImage] : [];
            const skillXp = [{ skill: skillInfo, xp: xpValue, isPrimary: true }];
            results.push({
                xpValue,
                xpText,
                skill: skillInfo,
                skillSprite: outlier.el,
                dropType: XpDropType.SINGLE_SKILL,
                skills: [skillInfo],
                skillXp,
                skillSpriteImages,
                timestamp: now,
                likelySource: null,
            });
        }
        return results;
    }
    /**
     * Find skill sprites near a specific position (for pending drop resolution)
     * Filters out menu skills (always visible in UI) and uses MAX_INDICATOR_DISTANCE
     */
    findSkillsNearPosition(elements, x, y) {
        // Filter skill sprites that are:
        // 1. Not menu skills (always visible in skills interface)
        // 2. Close to the XP text position
        const nearbySkillSprites = elements.filter(el => {
            const id = el.sprite.known?.id;
            if (id === undefined || !ALL_SKILL_IDS.includes(id))
                return false;
            // Skip menu skills - they're always visible and not XP indicators
            if (MENU_SKILL_IDS.has(id))
                return false;
            // Check if sprite is close to the position
            const horizontalDist = Math.abs(el.x - x);
            if (horizontalDist > MAX_INDICATOR_DISTANCE)
                return false;
            const verticalDist = Math.abs(el.y - y);
            if (verticalDist > 50)
                return false;
            return true;
        });
        // Deduplicate by skill ID
        const uniqueSkillIds = new Set();
        const skills = [];
        for (const sprite of nearbySkillSprites) {
            const id = sprite.sprite.known.id;
            if (!uniqueSkillIds.has(id)) {
                uniqueSkillIds.add(id);
                const skillInfo = SKILL_ID_TO_INFO.get(id);
                if (skillInfo)
                    skills.push(skillInfo);
            }
        }
        return skills;
    }
    /**
     * Classify the type of XP drop based on the skills present.
     */
    classifyDropType(skills) {
        if (skills.length === 0) {
            return XpDropType.UNKNOWN_MULTI;
        }
        if (skills.length === 1) {
            return XpDropType.SINGLE_SKILL;
        }
        // Check if all skills are combat skills
        const allCombat = skills.every(s => COMBAT_SKILL_IDS.has(s.id));
        const hasConstitution = skills.some(s => s.id === SKILL_SPRITES.constitution.id);
        // 2-4 combat skills with Constitution = typical combat split
        if (allCombat && hasConstitution && skills.length <= 4) {
            return XpDropType.COMBAT_SPLIT;
        }
        // Many skills (10+) = likely celebration lamp or all-skills lamp
        if (skills.length >= 10) {
            // If it's close to all skills (25+), it's an all-skills lamp
            if (skills.length >= 20) {
                return XpDropType.ALL_SKILLS_LAMP;
            }
            // Otherwise likely celebration lamp
            return XpDropType.CELEBRATION_LAMP;
        }
        // 5-9 skills could be celebration lamp with combat opt-out
        if (skills.length >= 5) {
            return XpDropType.CELEBRATION_LAMP;
        }
        // Fallback for other multi-skill scenarios
        return XpDropType.UNKNOWN_MULTI;
    }
    /**
     * Calculate per-skill XP attribution based on drop type.
     *
     * Distribution rules:
     * - SINGLE_SKILL: 100% to that skill
     * - COMBAT_SPLIT: Constitution gets 1/3, others split 2/3
     * - CELEBRATION_LAMP: Primary skill gets full XP, others get 5%
     * - ALL_SKILLS_LAMP: Equal split among all skills
     * - UNKNOWN_MULTI: Equal split as fallback
     */
    calculateSkillXp(totalXp, dropType, singleSkill, skills) {
        // Single skill drop - 100% to that skill
        if (dropType === XpDropType.SINGLE_SKILL && singleSkill) {
            return [{ skill: singleSkill, xp: totalXp, isPrimary: true }];
        }
        // No skills identified
        if (skills.length === 0) {
            return [];
        }
        switch (dropType) {
            case XpDropType.COMBAT_SPLIT:
                return this.calculateCombatSplit(totalXp, skills);
            case XpDropType.CELEBRATION_LAMP:
                return this.calculateCelebrationLamp(totalXp, skills);
            case XpDropType.ALL_SKILLS_LAMP:
                return this.calculateEqualSplit(totalXp, skills);
            case XpDropType.UNKNOWN_MULTI:
            default:
                return this.calculateEqualSplit(totalXp, skills);
        }
    }
    /**
     * Combat XP split: Constitution gets 1/3, PRIMARY attack skill gets 2/3
     * If Prayer is detected (Soul Split/Leech curses), estimate Prayer XP as bonus
     */
    calculateCombatSplit(totalXp, skills) {
        const hasConstitution = skills.some(s => s.id === SKILL_SPRITES.constitution.id);
        const hasPrayer = skills.some(s => s.id === SKILL_SPRITES.prayer.id);
        // Find the primary attack skill (Magic, Ranged, Attack, Strength, Defence, Necromancy)
        const primaryAttackSkill = skills.find(s => PRIMARY_ATTACK_SKILL_IDS.has(s.id));
        if (hasConstitution && primaryAttackSkill) {
            // Standard RS3 combat split: 1/3 Constitution, 2/3 attack skill
            const constitutionXp = Math.round(totalXp / 3 * 10) / 10;
            const attackXp = Math.round((totalXp * 2 / 3) * 10) / 10;
            const result = [
                { skill: { id: SKILL_SPRITES.constitution.id, label: "Constitution" }, xp: constitutionXp },
                { skill: primaryAttackSkill, xp: attackXp, isPrimary: true }
            ];
            // If Prayer is displayed alongside combat skills, try to identify the bone/ash type
            // from the JSON database. Bonecrusher/ectoplasmator give fixed XP based on bone type.
            if (hasPrayer) {
                // Check if this total XP matches a known Prayer source (might be misclassified Prayer drop)
                const prayerSource = findMatchingPrayerSource(totalXp);
                if (prayerSource) {
                    // This looks like a Prayer drop being misclassified as combat!
                    // Return just the Prayer XP
                    console.log(`[Combat Split] Detected as Prayer drop: ${prayerSource.name} (${prayerSource.xp} XP)`);
                    return [{
                            skill: { id: SKILL_SPRITES.prayer.id, label: "Prayer" },
                            xp: prayerSource.xp
                        }];
                }
                // Otherwise estimate Prayer XP at ~10% (Soul Split healing)
                const prayerXp = Math.round(totalXp * 0.1 * 10) / 10;
                result.push({
                    skill: { id: SKILL_SPRITES.prayer.id, label: "Prayer" },
                    xp: prayerXp
                });
                console.log(`[Combat Split] Added estimated Prayer XP: ${prayerXp} (10% of ${totalXp})`);
            }
            return result;
        }
        // If only Constitution (no attack skill), it gets 1/3
        if (hasConstitution && !primaryAttackSkill) {
            const constitutionXp = Math.round(totalXp / 3 * 10) / 10;
            return [{ skill: { id: SKILL_SPRITES.constitution.id, label: "Constitution" }, xp: constitutionXp }];
        }
        // If only attack skill (no Constitution), it gets 2/3
        if (!hasConstitution && primaryAttackSkill) {
            const attackXp = Math.round((totalXp * 2 / 3) * 10) / 10;
            return [{ skill: primaryAttackSkill, xp: attackXp }];
        }
        // Fallback to equal split if neither Constitution nor attack skill
        return this.calculateEqualSplit(totalXp, skills);
    }
    /**
     * Celebration lamp: Primary skill gets full XP (shown as totalXp),
     * all other skills get 5% of that amount.
     * The totalXp shown is the PRIMARY skill's XP, not the sum.
     */
    calculateCelebrationLamp(totalXp, skills) {
        if (skills.length === 0)
            return [];
        // First skill is typically the primary (selected) skill
        const primarySkill = skills[0];
        const secondarySkills = skills.slice(1);
        // Primary gets full amount shown
        const primaryXp = totalXp;
        // Secondary skills get 5% of primary
        const secondaryXp = Math.round(totalXp * 0.05 * 10) / 10;
        const result = [
            { skill: primarySkill, xp: primaryXp, isPrimary: true }
        ];
        for (const skill of secondarySkills) {
            result.push({ skill, xp: secondaryXp, isPrimary: false });
        }
        return result;
    }
    /**
     * Equal split: Total XP divided evenly among all skills
     */
    calculateEqualSplit(totalXp, skills) {
        if (skills.length === 0)
            return [];
        const perSkillXp = Math.round(totalXp / skills.length * 10) / 10;
        return skills.map(skill => ({ skill, xp: perSkillXp }));
    }
    /**
     * Check if text looks like a valid XP value (not tooltip/item text)
     * Valid: "+5", "150", "1.5k", "2.3m xp", "1,234"
     * Invalid: "Stack value: 1.8K", "1 x Law talisman", "Level 99", "1,232/1,232", "100%"
     */
    isValidXpText(text) {
        const trimmed = text.trim();
        // Reject text containing "/" - HP/prayer/summoning bars like "1,232/1,232"
        if (trimmed.includes("/"))
            return false;
        // Reject text containing "%" - adrenaline bar like "100%"
        if (trimmed.includes("%"))
            return false;
        // Reject very short text that's just 1-2 digits (likely UI labels/levels/crafting values)
        // UNLESS it starts with "+" which indicates an actual XP drop (e.g., "+ 6 xp")
        // Static UI labels like "6.3xp" in crafting interface don't have "+" prefix.
        const digitsOnly = trimmed.replace(/[^0-9]/g, "");
        const startsWithPlus = trimmed.startsWith("+");
        if (digitsOnly.length <= 2 && !startsWithPlus && !trimmed.includes("k") && !trimmed.includes("K") && !trimmed.includes("m") && !trimmed.includes("M"))
            return false;
        // Reject if contains "x" followed by space (item count like "1 x Law")
        if (/\d+\s*x\s+/i.test(trimmed))
            return false;
        // Reject if contains colon (tooltip labels like "Stack value:")
        if (trimmed.includes(":"))
            return false;
        // Reject if starts with a letter (not + or digit)
        if (/^[a-z]/i.test(trimmed))
            return false;
        // Reject if contains multiple words with letters (item names)
        // Allow "xp" at the end but not other words
        const withoutXp = trimmed.replace(/\s*xp\s*$/i, "");
        if (/[a-z]{2,}/i.test(withoutXp.replace(/[km]$/i, "")))
            return false;
        // Must contain at least one digit
        if (!/\d/.test(trimmed))
            return false;
        return true;
    }
    /**
     * Parse XP value from text, handling +, k/m suffixes, and "xp" suffix
     * Examples: "+5 xp", "150", "1.5k", "2.3m xp"
     */
    parseXpValue(text) {
        // Handle "current/target" format from XP tracker (e.g., "7,673/13,200")
        // Extract only the current value (left of "/") for delta tracking.
        let processText = text;
        if (text.includes("/")) {
            const parts = text.split("/");
            if (parts.length === 2 && /\d/.test(parts[0]) && /\d/.test(parts[1])) {
                processText = parts[0].trim();
            }
        }
        // First validate this looks like XP text, not a tooltip
        if (!this.isValidXpText(processText)) {
            return 0;
        }
        // Remove +, spaces, "xp" suffix, and other non-numeric chars except . k m
        const clean = processText.toLowerCase()
            .replace(/xp/g, "")
            .replace(/[^0-9.km]/g, "");
        const match = clean.match(/^([\d.]+)(k|m)?/);
        if (!match)
            return 0;
        let value = parseFloat(match[1]);
        if (isNaN(value))
            return 0;
        if (match[2] === "k")
            value *= 1000;
        if (match[2] === "m")
            value *= 1000000;
        return Math.round(value * 10) / 10;
    }
    /**
     * Find the skill(s) associated with an XP drop.
     * In RS3, XP can be split among multiple skills (e.g., Constitution + Magic + Prayer)
     *
     * XP drop UI structure: skill icons appear DIRECTLY above the XP text,
     * typically within 20-35 pixels vertically and nearly centered horizontally.
     *
     * IMPORTANT: Uses tight thresholds to avoid picking up sprites from OTHER drops
     * that are still animating on screen.
     */
    findAssociatedSkill(elements, xpTextElement) {
        if (!xpTextElement) {
            return { skill: null, skillSprite: null, skills: [] };
        }
        const xpX = xpTextElement.x;
        const xpY = xpTextElement.y;
        // Find ALL skill sprites on screen
        const allSkillSprites = elements.filter(el => {
            const id = el.sprite.known?.id;
            return id !== undefined && ALL_SKILL_IDS.includes(id);
        });
        // Separate into menu skills (always visible in UI) vs XP indicators (actual drop indicators)
        const menuSprites = [];
        const xpIndicatorSprites = [];
        for (const sprite of allSkillSprites) {
            const id = sprite.sprite.known.id;
            if (MENU_SKILL_IDS.has(id)) {
                menuSprites.push(sprite);
            }
            else {
                // Non-menu skill - potential XP indicator
                xpIndicatorSprites.push(sprite);
            }
        }
        // Find XP indicator sprites that are close enough to the XP text
        // Use MAX_INDICATOR_DISTANCE for horizontal distance
        const nearbyIndicators = xpIndicatorSprites.filter(el => {
            const horizontalDist = Math.abs(el.x - xpX);
            // Must be within MAX_INDICATOR_DISTANCE horizontally
            if (horizontalDist > MAX_INDICATOR_DISTANCE)
                return false;
            // Vertically: skill icons typically appear above/near the XP text
            const verticalDist = Math.abs(el.y - xpY);
            if (verticalDist > 50)
                return false;
            return true;
        });
        // If no non-menu indicators found, the drop might not have a visible skill indicator
        // (e.g., Prayer from bonecrusher, or skill icon faded)
        if (nearbyIndicators.length === 0) {
            console.log(`[XP Skills] XP at (${xpX},${xpY}), no XP indicator sprites found (${menuSprites.length} menu sprites ignored)`);
            return { skill: null, skillSprite: null, skills: [] };
        }
        // Deduplicate by skill ID and cache sprites
        const uniqueSkillIds = new Set();
        const uniqueSkills = [];
        for (const sprite of nearbyIndicators) {
            const id = sprite.sprite.known.id;
            if (!uniqueSkillIds.has(id)) {
                uniqueSkillIds.add(id);
                uniqueSkills.push(sprite);
                // Cache the sprite for later use in display
                skillSpriteCache.cacheFromRenderRect(sprite);
            }
        }
        // Convert to SkillInfo array
        let skills = uniqueSkills
            .map(s => SKILL_ID_TO_INFO.get(s.sprite.known.id))
            .filter(Boolean);
        // Debug: Log what skills we found near the XP drop WITH positions
        const skillPositions = uniqueSkills.map(s => {
            const info = SKILL_ID_TO_INFO.get(s.sprite.known.id);
            const dist = Math.abs(s.x - xpX);
            return `${info?.label}@(${s.x},${s.y}) d=${dist}`;
        });
        console.log(`[XP Skills] XP at (${xpX},${xpY}), found ${skills.length} XP indicator(s): ${skillPositions.join(", ")} (${menuSprites.length} menu sprites ignored)`);
        // VALIDATION: Check for impossible skill combinations
        // Normal skilling activities should NEVER combine non-combat skills
        if (skills.length > 1) {
            const validatedSkills = this.validateSkillCombination(skills, xpX, xpY, uniqueSkills);
            if (validatedSkills.length !== skills.length) {
                console.log(`[XP Validation] Reduced ${skills.length} skills to ${validatedSkills.length}: ${validatedSkills.map(s => s.label).join(", ")}`);
                skills = validatedSkills;
                // Update uniqueSkills to match
                const validIds = new Set(validatedSkills.map(s => s.id));
                uniqueSkills.length = 0;
                for (const sprite of nearbyIndicators) {
                    const id = sprite.sprite.known.id;
                    if (validIds.has(id) && !uniqueSkills.some(s => s.sprite.known.id === id)) {
                        uniqueSkills.push(sprite);
                    }
                }
            }
        }
        // Single skill - return with skill info
        if (uniqueSkills.length === 1) {
            const sprite = uniqueSkills[0];
            const skillInfo = skills[0];
            return {
                skill: skillInfo,
                skillSprite: sprite,
                skills,
            };
        }
        // Multiple skills
        return {
            skill: null,
            skillSprite: null,
            skills,
        };
    }
    /**
     * Validate that a combination of skills makes sense for RS3.
     * Returns the validated (possibly reduced) list of skills.
     *
     * Valid multi-skill combinations:
     * - Combat skills only (Attack/Str/Def/Ranged/Magic/Necro/Constitution/Prayer/Summoning)
     * - Many skills together (10+) = celebration/all-skills lamp
     *
     * Invalid combinations (likely from OTHER drops on screen):
     * - Woodcutting + Firemaking (these never share XP)
     * - Fletching + Firemaking
     * - Mining + Smithing (unless possibly DXP but rare)
     * - Any non-combat skill + combat skill (unless it's Prayer from bonecrusher)
     */
    validateSkillCombination(skills, xpX, xpY, skillSprites) {
        // If many skills (5+), it's likely a lamp - accept it
        if (skills.length >= 5) {
            return skills;
        }
        // Check if all skills are combat skills - valid combination
        const allCombat = skills.every(s => COMBAT_SKILL_IDS.has(s.id));
        if (allCombat) {
            return skills;
        }
        // For 2-4 skills with non-combat mixed in, check if it's valid
        const combatSkills = skills.filter(s => COMBAT_SKILL_IDS.has(s.id));
        const nonCombatSkills = skills.filter(s => !COMBAT_SKILL_IDS.has(s.id));
        // If we have combat + non-combat, it's almost always wrong
        // Exception: Prayer can appear with combat skills from Soul Split
        if (combatSkills.length > 0 && nonCombatSkills.length > 0) {
            // If non-combat skills are ONLY Prayer, and we have combat skills, that's valid
            const nonCombatWithoutPrayer = nonCombatSkills.filter(s => s.id !== SKILL_SPRITES.prayer.id);
            if (nonCombatWithoutPrayer.length === 0) {
                return skills; // Valid: combat + Prayer
            }
            // Otherwise, pick the skill CLOSEST to the XP text position
            console.log(`[XP Validation] Mixed combat/non-combat detected, picking closest skill`);
            return this.pickClosestSkill(skills, xpX, xpY, skillSprites);
        }
        // Multiple non-combat skills together - almost always wrong
        // (e.g., Woodcutting + Firemaking from different drops)
        if (nonCombatSkills.length > 1) {
            console.log(`[XP Validation] Multiple non-combat skills detected, picking closest skill`);
            return this.pickClosestSkill(skills, xpX, xpY, skillSprites);
        }
        return skills;
    }
    /**
     * Pick the single skill that is closest to the XP text position.
     * Used when we detect what are likely skills from different drops.
     */
    pickClosestSkill(skills, xpX, xpY, skillSprites) {
        let closestSkill = null;
        let closestDist = Infinity;
        for (const sprite of skillSprites) {
            const id = sprite.sprite.known.id;
            const skillInfo = SKILL_ID_TO_INFO.get(id);
            if (!skillInfo)
                continue;
            // Calculate distance (prioritize horizontal alignment)
            const hDist = Math.abs(sprite.x - xpX);
            const vDist = Math.abs(sprite.y - xpY);
            const dist = hDist * 2 + vDist; // Weight horizontal more
            if (dist < closestDist) {
                closestDist = dist;
                closestSkill = skillInfo;
            }
        }
        return closestSkill ? [closestSkill] : skills.slice(0, 1);
    }
}
XpDetector.FONT_STORAGE_KEY = 'xpbuddy:learned_font';
XpDetector.XP_LOG_KEY = 'xpbuddy:xp_log';
XpDetector.XP_LOG_MAX_ENTRIES = 5000;
// ============================================================================
// SPRITE CAPTURE UTILITY
// ============================================================================
function captureSprite(sprite) {
    try {
        const { x, y, width, height } = sprite;
        const imgData = (0,_reflect2d_reflect2d__WEBPACK_IMPORTED_MODULE_1__.safeCapture)(sprite.basetex, x, y, width, height);
        if (!imgData)
            return null;
        const canvas = document.createElement("canvas");
        canvas.width = width;
        canvas.height = height;
        const ctx = canvas.getContext("2d");
        if (!ctx)
            return null;
        ctx.putImageData(imgData, 0, 0);
        return canvas.toDataURL();
    }
    catch {
        return null;
    }
}
/**
 * Analyze the brightness of an ImageData object.
 * XP drop text is typically white/bright colored, so we can use
 * brightness thresholds to validate detection.
 */
function analyzeBrightness(imgData, brightnessThreshold = 200) {
    const data = imgData.data;
    const totalPixels = imgData.width * imgData.height;
    let sumBrightness = 0;
    let sumAlpha = 0;
    let maxBrightness = 0;
    let brightPixelCount = 0;
    let visiblePixels = 0;
    for (let i = 0; i < data.length; i += 4) {
        const r = data[i];
        const g = data[i + 1];
        const b = data[i + 2];
        const a = data[i + 3];
        // Skip fully transparent pixels
        if (a < 10)
            continue;
        visiblePixels++;
        sumAlpha += a;
        // Calculate perceived brightness (luminance formula)
        // Human eye is more sensitive to green, less to blue
        const brightness = (0.299 * r + 0.587 * g + 0.114 * b);
        sumBrightness += brightness;
        if (brightness > maxBrightness)
            maxBrightness = brightness;
        if (brightness >= brightnessThreshold)
            brightPixelCount++;
    }
    return {
        avgBrightness: visiblePixels > 0 ? sumBrightness / visiblePixels : 0,
        maxBrightness,
        brightPixelRatio: visiblePixels > 0 ? brightPixelCount / visiblePixels : 0,
        avgAlpha: visiblePixels > 0 ? sumAlpha / visiblePixels : 0,
        visiblePixels,
        totalPixels,
    };
}
/**
 * Capture a sprite region and analyze its brightness.
 * Returns null if capture fails.
 */
function captureSpriteWithBrightness(sprite) {
    try {
        const { x, y, width, height } = sprite;
        const imgData = (0,_reflect2d_reflect2d__WEBPACK_IMPORTED_MODULE_1__.safeCapture)(sprite.basetex, x, y, width, height);
        if (!imgData)
            return null;
        const brightness = analyzeBrightness(imgData);
        // Create canvas for data URL
        const canvas = document.createElement("canvas");
        canvas.width = width;
        canvas.height = height;
        const ctx = canvas.getContext("2d");
        if (!ctx)
            return null;
        ctx.putImageData(imgData, 0, 0);
        return {
            dataUrl: canvas.toDataURL(),
            brightness,
        };
    }
    catch {
        return null;
    }
}
/**
 * Check if brightness characteristics match an XP drop.
 * XP drops have bright white text that should:
 * - Have high average brightness (text is white/light)
 * - Have a good ratio of bright pixels
 * - Have visible alpha (not fully transparent)
 */
function isBrightnessValidForXpDrop(brightness) {
    // Need at least some visible pixels
    if (brightness.visiblePixels < 5)
        return false;
    // XP text should have high brightness (white text)
    // Threshold: at least 150 average brightness or 30% bright pixels
    const hasHighBrightness = brightness.avgBrightness >= 150;
    const hasBrightPixels = brightness.brightPixelRatio >= 0.3;
    // Need good alpha (visible, not faded)
    const hasGoodAlpha = brightness.avgAlpha >= 100;
    return (hasHighBrightness || hasBrightPixels) && hasGoodAlpha;
}
// ============================================================================
// HELPER - Get skill label from ID
// ============================================================================
function getSkillLabel(id) {
    return SKILL_ID_TO_INFO.get(id)?.label ?? null;
}


/***/ },

/***/ "./reflect2d/UIRenderTextureCache.ts"
/*!*******************************************!*\
  !*** ./reflect2d/UIRenderTextureCache.ts ***!
  \*******************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   UIRenderTextureCache: () => (/* binding */ UIRenderTextureCache)
/* harmony export */ });
/* harmony import */ var _reflect2d__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./reflect2d */ "./reflect2d/reflect2d.ts");

class UIRenderTextureCache {
    constructor() {
        this.cache = new Map();
    }
    getTexture(snap) {
        let cnv = this.cache.get(snap);
        if (!cnv) {
            // try to find a parent snapshot to update from
            for (let [oldsnap, oldcnv] of this.cache) {
                if (snap.isChild(oldsnap)) {
                    cnv = oldcnv;
                    let ctx = cnv.getContext("2d");
                    let changesize = 0;
                    for (let edit of snap.changesSince(oldsnap)) {
                        let imgdata = (0,_reflect2d__WEBPACK_IMPORTED_MODULE_0__.safeCapture)(snap, edit.x, edit.y, edit.width, edit.height);
                        if (!imgdata)
                            continue;
                        ctx.putImageData(imgdata, edit.x, edit.y);
                        changesize += edit.width * edit.height;
                    }
                    this.cache.delete(oldsnap);
                    this.cache.set(snap, cnv);
                    break;
                }
            }
        }
        if (!cnv) {
            // original capture
            cnv = document.createElement("canvas");
            cnv.width = snap.width;
            cnv.height = snap.height;
            let ctx = cnv.getContext("2d");
            let img = (0,_reflect2d__WEBPACK_IMPORTED_MODULE_0__.safeCapture)(snap, 0, 0, snap.width, snap.height);
            if (!img) {
                img = new ImageData(snap.width, snap.height);
            }
            ctx.putImageData(img, 0, 0);
            this.cache.set(snap, cnv);
        }
        return cnv;
    }
}


/***/ },

/***/ "./reflect2d/reflect2d.ts"
/*!********************************!*\
  !*** ./reflect2d/reflect2d.ts ***!
  \********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   AtlasTracker: () => (/* binding */ AtlasTracker),
/* harmony export */   getUIState: () => (/* binding */ getUIState),
/* harmony export */   pointBoxDistance: () => (/* binding */ pointBoxDistance),
/* harmony export */   rectContainsPoint: () => (/* binding */ rectContainsPoint),
/* harmony export */   rectsHaveOverlap: () => (/* binding */ rectsHaveOverlap),
/* harmony export */   safeCapture: () => (/* binding */ safeCapture)
/* harmony export */ });
/* harmony import */ var _render_renderprogram__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../render/renderprogram */ "./render/renderprogram.ts");
/* harmony import */ var _spritecache__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./spritecache */ "./reflect2d/spritecache.ts");


function rectsHaveOverlap(a, b) {
    let overlapx = (a.x >= b.x && a.x < b.x + b.width || b.x >= a.x && b.x < a.x + a.width);
    let overlapy = (a.y >= b.y && a.y < b.y + b.height || b.y >= a.y && b.y < a.y + a.height);
    return overlapx && overlapy;
}
function rectContainsPoint(rect, px, py) {
    return (px >= rect.x && px < rect.x + rect.width && py >= rect.y && py < rect.y + rect.height);
}
function pointBoxDistance(x1, y1, x2, y2) {
    return Math.abs(x1 - x2) + Math.abs(y1 - y2);
}
class AtlasTextureSnapshotCache {
    constructor(snap) {
        this.sprites = new Map();
        this.fontsheets = [];
        this.lastChanges = [];
        this.snapshot = snap;
        this.whitesprite = {
            x: -1, y: -1, height: 1, width: 1,
            pixelhash: 0,
            basetex: snap,
            known: _spritecache__WEBPACK_IMPORTED_MODULE_1__.emptySpriteInfo,
        };
    }
    cacheKey(x, y) {
        return (x << 16) | y;
    }
    adsorbKnowns(old) {
        if (!this.snapshot.isChild(old.snapshot)) {
            return false;
        }
        let edits = this.snapshot.changesSince(old.snapshot);
        this.lastChanges = edits;
        // keep known sprites that do not overlap edits
        for (let [key, val] of old.sprites.entries()) {
            if (!edits.some(edit => rectsHaveOverlap(edit, val))) {
                val.basetex = this.snapshot;
                this.sprites.set(key, val);
            }
        }
        for (let sheet of old.fontsheets) {
            if (!edits.some(edit => rectsHaveOverlap(edit, sheet.frag))) {
                this.fontsheets.push(sheet);
            }
        }
        return true;
    }
    removeFragment(frag) {
        let oldkey = this.cacheKey(frag.x, frag.y);
        this.sprites.delete(oldkey);
    }
    findFragment(sprites, x, y, w, h) {
        let key = this.cacheKey(x, y);
        // let frag = this.sprites.find(q => q.x == x && q.y == y && q.width == w && q.height == h);
        let frag = this.sprites.get(key);
        if (frag && (frag.width != w || frag.height != h)) {
            console.log("overwrote atlas sprite with different size");
            frag = undefined;
        }
        if (!frag) {
            frag = this.getFragment(sprites, x, y, w, h, null);
        }
        return frag;
    }
    getHash(x, y, w, h) {
        if (w <= 0 || h <= 0)
            return 0;
        let buf = safeCapture(this.snapshot, x, y, w, h) ?? new ImageData(w, h);
        return (0,_spritecache__WEBPACK_IMPORTED_MODULE_1__.imgcrc)(buf);
    }
    makeFragment(x, y, w, h, known = null) {
        let frag = {
            x: x, y: y, width: w, height: h,
            pixelhash: this.getHash(x, y, w, h),
            basetex: this.snapshot,
            known: known,
        };
        return frag;
    }
    getFragment(sprites, x, y, w, h, ifmatched) {
        let key = this.cacheKey(x, y);
        let hash = this.getHash(x, y, w, h);
        if (ifmatched && ifmatched.hash != hash) {
            return null;
        }
        let frag = {
            x: x, y: y, width: w, height: h,
            pixelhash: hash,
            basetex: this.snapshot,
            known: null,
        };
        this.sprites.set(key, frag);
        if (ifmatched) {
            frag.known = ifmatched;
        }
        else {
            let spritematch = sprites.hashes.get(hash);
            if (spritematch) {
                frag.known = spritematch;
            }
            else {
                this.tryMatchFontChar(sprites, frag);
            }
        }
        if (frag.known && frag.known.font) {
            if (frag.known.fontchr) {
                this.tryIdentifyFont(sprites, frag);
            }
        }
        return frag;
    }
    tryMatchFontChar(sprites, frag) {
        if (frag.known) {
            return;
        }
        for (let { frag: fontfrag, font } of this.fontsheets) {
            if (!rectsHaveOverlap(fontfrag, frag)) {
                continue;
            }
            let match = font.unknownsubs.find(unk => rectContainsPoint(frag, fontfrag.x + unk.x, fontfrag.y + unk.y));
            let charcode = match?.charcode ?? "�".charCodeAt(0);
            frag.known = font.identifyMissingCharacter(charcode, frag.x - frag.x, frag.y - frag.y, frag.width, frag.height, frag.pixelhash);
            sprites.hashes.set(frag.pixelhash, frag.known);
            break;
        }
    }
    tryIdentifyFont(sprites, charfrag) {
        if (!charfrag.known || !charfrag.known.fontchr || !charfrag.known.font) {
            return;
        }
        let font = charfrag.known.font;
        let fontchr = charfrag.known.fontchr;
        let fontx = charfrag.x - fontchr.x;
        let fonty = charfrag.y - fontchr.y;
        if (this.fontsheets.some(f => f.frag.x == fontx && f.frag.y == fonty)) {
            return;
        }
        let basekey = this.cacheKey(fontx, fonty);
        let basesprite = this.sprites.get(basekey);
        if (!basesprite || basesprite.known != charfrag.known.font?.basesprite) {
            let fontw = font.sheetwidth;
            let fonth = font.sheetheight;
            if (fontw == -1 || fonth == -1) {
                throw new Error("incomplete font");
            }
            let hash = this.getHash(fontx, fonty, fontw, fonth);
            if (hash == font.basesprite.hash) {
                let basefrag = this.makeFragment(fontx, fonty, fontw, fonth, font.basesprite);
                this.fontsheets.push({ font: font, frag: basefrag });
                console.log(`font ${font.spriteid} base matched by char ${fontchr.charcode}`);
                // detect fragments that belong to this font
                for (let chr of font.subs.values()) {
                    let fontchr = chr.fontchr;
                    this.getFragment(sprites, fontx + fontchr.x, fonty + fontchr.y, fontchr.width, fontchr.height, chr);
                }
                // try find fragments 
                for (let frag of this.sprites.values()) {
                    if (frag.known || !rectsHaveOverlap(frag, basefrag)) {
                        continue;
                    }
                    let match = font.unknownsubs.find(unkchr => rectContainsPoint(frag, fontx + unkchr.x, fonty + unkchr.y));
                    let charcode = match?.charcode ?? "�".charCodeAt(0);
                    let known = font.identifyMissingCharacter(charcode, frag.x - fontx, frag.y - fonty, frag.width, frag.height, frag.pixelhash);
                    frag.known = known;
                    sprites.hashes.set(frag.pixelhash, known);
                }
            }
        }
    }
}
class AtlasTracker {
    constructor(spriteCache) {
        this.cache = new Map();
        // Limit cache size to prevent unbounded memory growth
        this.MAX_CACHE_SIZE = 10;
        this.spriteCache = spriteCache;
    }
    getSubcache(tex) {
        let subcache = this.cache.get(tex);
        if (!subcache) {
            subcache = new AtlasTextureSnapshotCache(tex);
            let absorbed = false;
            for (let [key, val] of this.cache.entries()) {
                if (subcache.adsorbKnowns(val)) {
                    this.cache.delete(key);
                    absorbed = true;
                    break;
                }
            }
            this.cache.set(tex, subcache);
            // If we didn't absorb and cache is too large, remove oldest entries
            if (!absorbed && this.cache.size > this.MAX_CACHE_SIZE) {
                const keysToDelete = [];
                let count = 0;
                for (const key of this.cache.keys()) {
                    if (count < this.cache.size - this.MAX_CACHE_SIZE) {
                        keysToDelete.push(key);
                        count++;
                    }
                    else {
                        break;
                    }
                }
                for (const key of keysToDelete) {
                    this.cache.delete(key);
                }
            }
        }
        return subcache;
    }
}
function getUIState(renders, cache) {
    let elements = [];
    for (let render of renders) {
        let progmeta = (0,_render_renderprogram__WEBPACK_IMPORTED_MODULE_0__.getProgramMeta)(render.program);
        if (!progmeta.isUi) {
            continue;
        }
        let data = (0,_render_renderprogram__WEBPACK_IMPORTED_MODULE_0__.getRenderFunc)(render);
        if (!data.uniforms["uDiffuseMap"]) {
            continue;
        }
        let posget = data.getters["aVertexPosition2D"];
        let texget = data.getters["aTextureUV"];
        let texminget = data.getters["aTextureUVAtlasMin"];
        let texextget = data.getters["aTextureUVAtlasExtents"];
        let colget = data.getters["aVertexColour"];
        let samplerid = data.uniforms["uDiffuseMap"][0][0];
        let tex = render.samplers[samplerid];
        if (!tex) {
            // TODO fix underlying, this seems to happen after for some reason the uniform buffer is all 0
            //console.log("ui texture sampler not found");
            continue;
        }
        let subcache = cache.getSubcache(tex);
        const eps = 0.4; //bias for weird rounding situation
        for (let a = 0; a < data.indices.length; a += 6) {
            let botleft = data.indices[a + 3];
            let topright = data.indices[a + 1];
            let botright = data.indices[a + 0];
            let topleft = data.indices[a + 2];
            let samplex = Math.floor(tex.width * texget(topleft, 0) + eps);
            let sampley = Math.floor(tex.height * texget(topleft, 1) + eps);
            let samplew = Math.floor(tex.width * texget(botright, 0) + eps) - samplex;
            let sampleh = Math.floor(tex.height * texget(botright, 1) + eps) - sampley;
            let texboxx = Math.floor(tex.width * texminget(topleft, 0) + eps);
            let texboxy = Math.floor(tex.height * texminget(topleft, 1) + eps);
            let texboxw = Math.floor(tex.width * texextget(topleft, 0) + eps);
            let texboxh = Math.floor(tex.height * texextget(topleft, 1) + eps);
            //deal with 1px profile sweeping, they have 0 width attribute arguments
            if (texboxw == 0 && samplew == 0 && texboxh != 0) {
                texboxw = 1;
                samplew = 1;
            }
            if (texboxh == 0 && sampleh == 0 && texboxw != 0) {
                texboxh = 1;
                sampleh = 1;
            }
            let xb = posget(botleft, 0);
            let yb = posget(botleft, 1);
            let dxx = posget(botright, 0) - xb;
            let dxy = posget(botright, 1) - yb;
            let dyx = posget(topleft, 0) - xb;
            let dyy = posget(topleft, 1) - yb;
            texboxw = Math.abs(texboxw);
            texboxh = Math.abs(texboxh);
            let frag;
            //hardcoded case in shader, this produces white
            if (samplex < -60000 || sampley < -60000) {
                frag = subcache.whitesprite;
            }
            else {
                if (dxx == 0 || dyy == 0) {
                    continue;
                }
                if (samplew == 0 || sampleh == 0) {
                    console.log("skipped zero size tex");
                    continue;
                }
                if (texboxw == 0 || texboxh == 0) {
                    console.log("skipped zero size tex bounding box");
                    continue;
                }
                frag = subcache.findFragment(cache.spriteCache, texboxx, texboxy, texboxw, texboxh);
            }
            let el = {
                sprite: frag,
                x: xb, y: yb,
                width: dxx, height: dyy,
                m12: dyx, m21: dxy,
                samplex: samplex - texboxx,
                sampley: sampley - texboxy,
                samplewidth: samplew,
                sampleheight: sampleh,
                color: [colget(botleft, 0), colget(botleft, 1), colget(botleft, 2), colget(botleft, 3)]
            };
            elements.push(el);
        }
    }
    return { elements, frametime: renders[0]?.ownFrameTime ?? 0, lastframetime: renders[0]?.lastFrameTime ?? 0 };
}
/**
 * Safe wrapper around TextureSnapshot.capture() that validates bounds
 * before calling native code. The native SubImage() has a FATAL_ASSERT
 * that crashes the process if coordinates exceed the data buffer.
 */
function safeCapture(snapshot, x, y, w, h) {
    if (!snapshot.canCapture())
        return null;
    if (w <= 0 || h <= 0)
        return null;
    if (x < 0 || y < 0)
        return null;
    if (x + w > snapshot.width || y + h > snapshot.height)
        return null;
    try {
        return snapshot.capture(x, y, w, h);
    }
    catch {
        return null;
    }
}


/***/ },

/***/ "./reflect2d/render.ts"
/*!*****************************!*\
  !*** ./reflect2d/render.ts ***!
  \*****************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   renderGameUI: () => (/* binding */ renderGameUI),
/* harmony export */   renderGameUITextureUsage: () => (/* binding */ renderGameUITextureUsage)
/* harmony export */ });
function renderGameUITextureUsage(ctx, atlas, texturecache, elements, startindex, endindex, showBorders, selectedSprite) {
    let texture = texturecache.getTexture(selectedSprite.basetex);
    ctx.drawImage(texture, 0, 0);
    ctx.fillStyle = "blue";
    ctx.strokeStyle = "blue";
    let subcache = atlas.getSubcache(selectedSprite.basetex);
    for (let sprite of subcache.sprites.values()) {
        ctx.strokeRect(sprite.x, sprite.y, sprite.width, sprite.height);
    }
    for (let { frag, font } of subcache.fontsheets) {
        ctx.fillStyle = "purple";
        ctx.strokeStyle = "purple";
        ctx.strokeRect(frag.x, frag.y, frag.width, frag.height);
        for (let unkchr of font.unknownsubs) {
            ctx.fillRect(frag.x + unkchr.x - 5, frag.y + unkchr.y - 0.5, 10, 1);
            ctx.fillRect(frag.x + unkchr.x - 0.5, frag.y + unkchr.y - 5, 1, 10);
        }
    }
    for (let i = startindex; i < endindex && i < elements.length; i++) {
        let el = elements[i];
        if (el.sprite.basetex != selectedSprite.basetex) {
            continue;
        }
        ctx.lineWidth = (el.sprite == selectedSprite ? 4 : 1);
        ctx.strokeStyle = (el.sprite.known ? (el.sprite.known.subid == -1 ? "purple" : "green") : "red");
        ctx.strokeRect(el.samplex + el.sprite.x, el.sampley + el.sprite.y, el.samplewidth, el.sampleheight);
    }
}
function renderGameUI(ctx, texturecache, elements, startindex, endindex, showBorders, selectedSprite) {
    for (let i = startindex; i < elements.length && i < endindex; i++) {
        let el = elements[i];
        let texture = texturecache.getTexture(el.sprite.basetex);
        ctx.save();
        // sign flipping magic because drawImage doesn't support negative width/height for flipping
        // rotation+flip is still broken
        let widthsign = Math.sign(el.samplewidth);
        let heightsign = -Math.sign(el.sampleheight);
        let paintwidth = el.width * widthsign;
        let paintheight = el.height * heightsign;
        let samplex = el.samplex + (widthsign == -1 ? el.samplewidth : 0);
        let sampley = el.sampley + (heightsign == -1 ? el.sampleheight : 0);
        let samplewidth = Math.abs(el.samplewidth);
        let sampleheight = Math.abs(el.sampleheight);
        ctx.transform(widthsign, el.m21 / paintwidth, el.m12 / paintheight, heightsign, el.x + (widthsign == -1 ? el.width : 0), el.y + (heightsign == -1 ? el.height : 0));
        for (let srcx = samplex; srcx < samplex + samplewidth;) {
            let srcx2 = Math.min(samplex + samplewidth, (Math.floor(srcx / el.sprite.width) + 1) * el.sprite.width);
            for (let srcy = sampley; srcy < sampley + sampleheight;) {
                let srcy2 = Math.min(sampley + sampleheight, (Math.floor(srcy / el.sprite.height) + 1) * el.sprite.height);
                ctx.drawImage(texture, el.sprite.x + srcx % el.sprite.width, el.sprite.y + srcy % el.sprite.height, srcx2 - srcx, srcy2 - srcy, (srcx - samplex) / samplewidth * el.width, (srcy - sampley) / sampleheight * el.height, (srcx2 - srcx) / samplewidth * el.width, (srcy2 - srcy) / sampleheight * el.height);
                srcy = srcy2;
            }
            srcx = srcx2;
        }
        if (showBorders) {
            ctx.lineWidth = (selectedSprite && el.sprite == selectedSprite ? 4 : 1);
            ctx.strokeStyle = (el.sprite.known ? (el.sprite.known.subid == -1 ? "purple" : "green") : "red");
            ctx.strokeRect(0, 0, el.width, el.height);
        }
        ctx.restore();
    }
}


/***/ },

/***/ "./reflect2d/spritecache.ts"
/*!**********************************!*\
  !*** ./reflect2d/spritecache.ts ***!
  \**********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   KnownSpriteSheet: () => (/* binding */ KnownSpriteSheet),
/* harmony export */   SpriteCache: () => (/* binding */ SpriteCache),
/* harmony export */   SpriteInfo: () => (/* binding */ SpriteInfo),
/* harmony export */   emptySpriteInfo: () => (/* binding */ emptySpriteInfo),
/* harmony export */   imgcrc: () => (/* binding */ imgcrc)
/* harmony export */ });
/* harmony import */ var _util_crc32__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../util/crc32 */ "./util/crc32.ts");

// sprite hash files generated using runeapps model viewer https://runeapps.org/modelviewer
// scripts->cli->run
// alternatively use cli version and prepend `node dist/cli.js -o openrs2last`
// extract -m spritehash --batchsize=1000000
const spritehashes = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/spritehash.batch.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/spritehash.batch.json");
// extract -m fonthash --batchsize=1000000
const fontfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/fonthash.batch.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/fonthash.batch.json");
// Chat fonts (created manually using the tools in UI State)
const chat10ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/chat10pt.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat10pt.json");
const chat12ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/chat12pt.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat12pt.json");
const chat14ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/chat14pt.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat14pt.json");
const chat16ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/chat16pt.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat16pt.json");
const chat18ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/chat18pt.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat18pt.json");
const chat20ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/chat20pt.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat20pt.json");
const chat22ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/chat22pt.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/chat22pt.json");
// Legacy/special fonts
const s8x11ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/8x11Chars.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/8x11Chars.json");
const s11x12ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/11x12Chars.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/11x12Chars.json");
const s7x9ptfile = __webpack_require__(/*! !!file-loader?name=data/[name].[ext]!./data/7x9Chars.json */ "../node_modules/file-loader/dist/cjs.js?name=data/[name].[ext]!./reflect2d/data/7x9Chars.json");
class SpriteInfo {
    constructor(id, subid, hash) {
        this.fontchr = null;
        this.font = null;
        this.id = id;
        this.subid = subid;
        this.hash = hash;
        this.synonym = this;
    }
}
function imgcrc(img) {
    let data = img.data.slice();
    // for some reason *some times* 0 blue gets turned into 1 blue
    // always set it to 1 for hash computation
    for (let i = 0; i < data.length; i += 4) {
        if (data[i + 2] == 0) {
            data[i + 2] = 1;
        }
    }
    return (0,_util_crc32__WEBPACK_IMPORTED_MODULE_0__.crc32)(data);
}
class KnownSpriteSheet {
    constructor(spriteid, width, height, sheethash) {
        this.subs = new Set();
        this.unknownsubs = [];
        this.fontfile = null;
        this.spriteid = spriteid;
        this.basesprite = new SpriteInfo(spriteid, 0, sheethash);
        this.basesprite.font = this;
        this.sheetwidth = width;
        this.sheetheight = height;
    }
    addFontFile(fontfile) {
        this.fontfile = fontfile;
        for (let chr of fontfile.characters) {
            if (!chr) {
                continue;
            }
            let sub = new SpriteInfo(fontfile.spriteid, chr.charcode, chr.hash);
            this.subs.add(sub);
            sub.font = this;
            sub.fontchr = chr;
        }
    }
    addUknownSub(dx, dy, charcode) {
        this.unknownsubs.push({ x: dx, y: dy, charcode: charcode });
    }
    addCharSprite(charcode, dx, dy, width, height, hash) {
        let known = new SpriteInfo(this.spriteid, charcode, hash);
        known.font = this;
        known.fontchr = {
            chr: String.fromCharCode(charcode),
            charcode: charcode,
            x: dx,
            y: dy,
            width: width,
            height: height,
            hash: known.hash,
            bearingy: 0, //unknown
        };
        this.subs.add(known);
        return known;
    }
    identifyMissingCharacter(charcode, dx, dy, width, height, hash) {
        let known = this.addCharSprite(charcode, dx, dy, width, height, hash);
        this.unknownsubs = this.unknownsubs.filter(c => c.charcode != charcode);
        console.log(`font char "${String.fromCharCode(charcode)}" matched by containment in font ${this.spriteid}`);
        return known;
    }
    resizeSheetBox() {
        if (this.unknownsubs.length == 0 && this.subs.size == 0) {
            throw new Error("no subs to size from");
        }
        let minx = Math.min.apply(null, [...this.subs.values().map(q => q.fontchr.x), ...this.unknownsubs.map(q => q.x)]);
        let miny = Math.min.apply(null, [...this.subs.values().map(q => q.fontchr.y), ...this.unknownsubs.map(q => q.y)]);
        let maxx = Math.max.apply(null, [...this.subs.values().map(q => q.fontchr.x + q.fontchr.width), ...this.unknownsubs.map(q => q.x + 1)]);
        let maxy = Math.max.apply(null, [...this.subs.values().map(q => q.fontchr.y + q.fontchr.height), ...this.unknownsubs.map(q => q.y + 1)]);
        this.sheetwidth = maxx - minx;
        this.sheetheight = maxy - miny;
        this.basesprite.hash = 0;
        for (let sub of this.subs) {
            sub.fontchr.x -= minx;
            sub.fontchr.y -= miny;
        }
        for (let chr of this.unknownsubs) {
            chr.x -= minx;
            chr.y -= miny;
        }
        return { dx: minx, dy: miny };
    }
    toJSON() {
        let res = {
            sheetwidth: this.sheetwidth,
            sheetheight: this.sheetheight,
            sheethash: this.basesprite.hash,
            spriteid: this.spriteid,
            characters: [...this.subs].map(s => s.fontchr).filter(c => c != null),
            unknownchars: [...this.unknownsubs],
        };
        return res;
    }
}
const whitePixelImage = new ImageData(new Uint8ClampedArray([255, 255, 255, 255]), 1, 1);
const emptySpriteInfo = new SpriteInfo(-1, 0, imgcrc(whitePixelImage));
class SpriteCache {
    constructor() {
        this.hashes = new Map();
        this.fonts = new Map();
        this.readyResolvers = Promise.withResolvers();
        this.ready = this.readyResolvers.promise;
        this.hashes.set(emptySpriteInfo.hash, emptySpriteInfo);
    }
    addSprite(info) {
        let prev = this.hashes.get(info.hash);
        if (prev) {
            info.synonym = prev.synonym;
            prev.synonym = info;
        }
        else {
            this.hashes.set(info.hash, info);
        }
        return info;
    }
    loadSpriteList(list) {
        list.forEach(spr => this.addSprite(new SpriteInfo(spr.id, spr.sub, spr.hash)));
    }
    loadCacheFontFile(fonts) {
        for (let fontjson of fonts) {
            let font = new KnownSpriteSheet(fontjson.spriteid, fontjson.sheetwidth, fontjson.sheetheight, fontjson.sheethash);
            font.addFontFile(fontjson);
            this.fonts.set(font.spriteid, font);
            font.subs.forEach(sub => this.addSprite(sub));
        }
    }
    loadCustomFontFile(fontjson) {
        let font = new KnownSpriteSheet(fontjson.spriteid, fontjson.sheetwidth, fontjson.sheetheight, fontjson.sheethash);
        for (let chr of fontjson.characters) {
            font.addCharSprite(chr.charcode, chr.x, chr.y, chr.width, chr.height, chr.hash);
        }
        for (let unk of fontjson.unknownchars) {
            font.addUknownSub(unk.x, unk.y, unk.charcode);
        }
        // TODO need unique id
        this.fonts.set(font.spriteid, font);
        font.subs.forEach(sub => this.addSprite(sub));
    }
    async downloadCacheData() {
        try {
            // Load sprite hashes
            let spritedata = await fetch(spritehashes.default).then(res => res.json());
            this.loadSpriteList(spritedata);
            console.log(`Loaded ${spritedata.length} sprite hashes`);
            // Load font hashes from cache
            let fontdata = await fetch(fontfile.default).then(res => res.json());
            this.loadCacheFontFile(fontdata);
            console.log(`Loaded ${fontdata.length} cached fonts`);
            // Load all chat fonts (different sizes for different UI scales)
            const chatFonts = [
                { file: chat10ptfile, name: "chat10pt" },
                { file: chat12ptfile, name: "chat12pt" },
                { file: chat14ptfile, name: "chat14pt" },
                { file: chat16ptfile, name: "chat16pt" },
                { file: chat18ptfile, name: "chat18pt" },
                { file: chat20ptfile, name: "chat20pt" },
                { file: chat22ptfile, name: "chat22pt" },
            ];
            for (const { file, name } of chatFonts) {
                try {
                    const data = await fetch(file.default).then(res => res.json());
                    this.loadCustomFontFile(data);
                    console.log(`Loaded ${name} font (${data.characters.length} chars)`);
                }
                catch (e) {
                    console.warn(`Failed to load ${name}:`, e);
                }
            }
            // Load legacy/special fonts
            const specialFonts = [
                { file: s8x11ptfile, name: "8x11" },
                { file: s11x12ptfile, name: "11x12" },
                { file: s7x9ptfile, name: "7x9" },
            ];
            for (const { file, name } of specialFonts) {
                try {
                    const data = await fetch(file.default).then(res => res.json());
                    this.loadCustomFontFile(data);
                    console.log(`Loaded ${name} font (${data.characters.length} chars)`);
                }
                catch (e) {
                    console.warn(`Failed to load ${name}:`, e);
                }
            }
            console.log(`SpriteCache ready: ${this.hashes.size} hashes, ${this.fonts.size} fonts`);
            this.readyResolvers.resolve();
        }
        catch (e) {
            console.error("Failed to load sprite cache data:", e);
            this.readyResolvers.reject(e);
        }
    }
}
function imageMemCompare(a, b) {
    if (a.width != b.width || a.height != b.height) {
        return false;
    }
    // let simpletrue = simpleCompare(b, a, 0, 0, 5) < Infinity;
    // return simpletrue;
    let memtrue = true;
    for (let i = 0; i < a.data.length; i += 4) {
        if (a.data[i + 3] != b.data[i + 3]) {
            memtrue = false;
            break;
        }
        if (a.data[i + 3] == 0) {
            continue;
        }
        if (a.data[i + 0] != b.data[i + 0]) {
            memtrue = false;
            break;
        }
        if (a.data[i + 1] != b.data[i + 1]) {
            memtrue = false;
            break;
        }
        if (a.data[i + 2] != b.data[i + 2]) {
            memtrue = false;
            break;
        }
    }
    // // if (simpletrue != memtrue) {
    // // 	debugger;
    // // }
    return memtrue;
}


/***/ },

/***/ "./reflect2d/uiparser.ts"
/*!*******************************!*\
  !*** ./reflect2d/uiparser.ts ***!
  \*******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   UIRenderParser: () => (/* binding */ UIRenderParser)
/* harmony export */ });
/* harmony import */ var _UIRenderTextureCache__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./UIRenderTextureCache */ "./reflect2d/UIRenderTextureCache.ts");
/* harmony import */ var _render__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./render */ "./reflect2d/render.ts");


class UIRenderParser {
    constructor(els, index, end) {
        this.els = els;
        this.index = index ?? 0;
        this.end = end ?? els.length;
    }
    searchUIPattern(search) {
        return searchUIPattern(this, search);
    }
    readFont() {
        return readFont(this);
    }
    boundToArea(bounds) {
        for (let i = this.index; i < this.end; i++) {
            let r = this.els[i];
            if (r.x < bounds.x || r.y < bounds.y || r.x + r.width > bounds.x + bounds.width || r.y + r.height > bounds.y + bounds.height) {
                this.end = i;
            }
        }
    }
    skip(n) {
        this.index += n;
    }
    next() {
        if (this.index < this.end) {
            return this.els[this.index++];
        }
        return undefined;
    }
    nextKnown(limit = 0) {
        let count = 0;
        while (this.index < this.end && (limit == 0 || count++ < limit)) {
            let el = this.els[this.index++];
            if (el.sprite.known) {
                return el;
            }
        }
        return undefined;
    }
    peek(n = 0) {
        if (this.index + n < this.end) {
            return this.els[this.index + n];
        }
        return undefined;
    }
    branch() {
        return new UIRenderParser(this.els, this.index);
    }
    show(x, y, scale) {
        if (typeof document == "undefined") {
            console.error("can't show in context without document");
            return;
        }
        let cnv = document.createElement("canvas");
        let ctx = cnv.getContext("2d");
        cnv.width = alt1.rsWidth;
        cnv.height = alt1.rsHeight;
        (0,_render__WEBPACK_IMPORTED_MODULE_1__.renderGameUI)(ctx, new _UIRenderTextureCache__WEBPACK_IMPORTED_MODULE_0__.UIRenderTextureCache(), this.els, 0, this.els.length, false, undefined);
        cnv.style.position = "absolute";
        cnv.style.left = (x ?? 5) + "px";
        cnv.style.top = (y ?? 5) + "px";
        cnv.style.outline = "1px solid green";
        cnv.style.background = "gray";
        cnv.style.width = cnv.width * (scale ?? 1) + "px";
        cnv.style.height = cnv.height * (scale ?? 1) + "px";
        if (scale && scale > 1) {
            cnv.style.imageRendering = "pixelated";
        }
        cnv.onclick = () => cnv.remove();
        document.body.appendChild(cnv);
    }
}
function readFont(parser) {
    let r = "";
    let el;
    let prev = undefined;
    let liney1 = 0;
    let liney2 = 0;
    let minx = 0;
    let miny = 0;
    let maxx = 0;
    let maxy = 0;
    let startindex = parser.index;
    let color = null;
    let font = null;
    while (el = parser.peek(0)) {
        if (!el.sprite.known || !el.sprite.known.fontchr) {
            break;
        }
        if (!prev) {
            font = el.sprite.known.font;
            color = el.color;
            liney1 = el.y;
            liney2 = el.y + el.height;
            minx = el.x;
            miny = el.y;
            maxx = el.x + el.width;
            maxy = el.y + el.height;
        }
        else if (el.y > liney2 || el.y + el.height < liney1 || el.x < prev.x - 5 || el.x > prev.x + prev.width + 10) {
            break;
        }
        //is foreground of already read drop-shadow render
        if (prev && el.x == prev.x - 1) {
            parser.next();
            continue;
        }
        //space
        if (prev && el.x >= prev.x + prev.width + 3) {
            r += " ";
        }
        r += el.sprite.known.fontchr.chr;
        minx = Math.min(minx, el.x);
        miny = Math.min(miny, el.y);
        maxx = Math.max(maxx, el.x + el.width);
        maxy = Math.max(maxy, el.y + el.height);
        parser.next();
        prev = el;
    }
    return { text: r, x: minx, y: miny, width: maxx - minx, height: maxy - miny, startindex, endindex: parser.index, color, font };
}
function matchRenderRect(crit, render) {
    if (!render) {
        return false;
    }
    if (crit.sprite) {
        if (Array.isArray(crit.sprite)) {
            // TODO deal with synonyms?
            if (!crit.sprite.find(q => render.sprite.known == q)) {
                return false;
            }
        }
        else {
            if (render.sprite.known != crit.sprite) {
                return false;
            }
        }
    }
    if (crit.id != undefined) {
        if (Array.isArray(crit.id)) {
            // TODO deal with synonyms?
            if (!crit.id.find(q => render.sprite.known?.id == q)) {
                return false;
            }
        }
        else {
            if (render.sprite.known?.id != crit.id) {
                return false;
            }
        }
    }
    if (crit.color) {
        if (crit.color[0] != render.color[0]) {
            return false;
        }
        if (crit.color[1] != render.color[1]) {
            return false;
        }
        if (crit.color[2] != render.color[2]) {
            return false;
        }
        if (crit.color[3] != render.color[3]) {
            return false;
        }
    }
    if (crit.not) {
        for (let neg of crit.not) {
            if (matchRenderRect(neg, render)) {
                return false;
            }
        }
    }
    return true;
}
function searchAllUIPatterns(els, search) {
    let parser = new UIRenderParser(els);
    let res = [];
    while (parser.peek(0)) {
        searchUIPattern(parser, search);
    }
    return res;
}
function matchUIElement(parser, q, peek, refs) {
    let startindex = parser.index;
    let el = parser.peek();
    if (!el) {
        return 0;
    }
    if (typeof q.string != "undefined") {
        if (!el.sprite.known?.fontchr) {
            return 0;
        }
        let text = readFont(parser);
        if (typeof q.string == "string" && !text.text.toLowerCase().includes(q.string.toLowerCase())) {
            parser.index = startindex;
            return 0;
        }
        if (!peek && q.ref) {
            if (!refs[q.ref]) {
                refs[q.ref] = {
                    allmatches: [],
                    index: text.startindex,
                    endindex: text.endindex,
                    match: parser.els[text.startindex],
                    text: ""
                };
            }
            refs[q.ref].allmatches.push(...parser.els.slice(text.startindex, text.endindex));
            refs[q.ref].endindex = text.endindex;
            refs[q.ref].text += (refs[q.ref].text != "" ? " " : "") + text.text;
        }
        if (peek) {
            parser.index = startindex;
        }
        return parser.index - startindex;
    }
    if ((!q.sprite && q.id == undefined) || matchRenderRect(q, el)) {
        if (!peek && q.ref) {
            if (!refs[q.ref]) {
                refs[q.ref] = {
                    allmatches: [],
                    endindex: parser.index,
                    index: parser.index - 1,
                    match: el,
                    text: ""
                };
            }
            refs[q.ref].allmatches.push(el);
            refs[q.ref].endindex = parser.index;
        }
        if (!peek) {
            parser.skip(1);
        }
        return 1;
    }
    return 0;
}
function matchUIPattern(parser, search) {
    let refs = {};
    let loop = (qindex) => {
        if (qindex >= search.length) {
            return qindex;
        }
        let q = search[qindex];
        let minmatches = (Array.isArray(q.repeat) ? q.repeat[0] : q.repeat) ?? 1;
        let maxmatches = (Array.isArray(q.repeat) ? q.repeat[1] : q.repeat) ?? 1;
        let matchcount = 0;
        let startindex = parser.index;
        while (true) {
            if (matchcount >= minmatches) {
                let depth = loop(qindex + 1);
                //prevent backtracking
                if (depth != qindex + 1) {
                    return depth;
                }
            }
            if (matchcount < maxmatches && matchUIElement(parser, q, false, refs)) {
                matchcount++;
                continue;
            }
            if (matchcount >= minmatches) {
                return loop(qindex + 1);
            }
            parser.index = startindex;
            return qindex;
        }
    };
    let depth = loop(0);
    if (depth != search.length) {
        return null;
    }
    return refs;
}
function searchUIPattern(parser, search) {
    let startindex = parser.index;
    let el;
    while (el = parser.nextKnown()) {
        parser.skip(-1);
        if (matchUIElement(parser, search[0], true, {})) {
            let oldindex = parser.index;
            let refs = matchUIPattern(parser, search);
            if (refs) {
                return refs;
            }
            parser.index = oldindex;
        }
        parser.skip(1);
    }
    parser.index = startindex;
    return null;
}


/***/ },

/***/ "./render/modelviewer/avautils.ts"
/*!****************************************!*\
  !*** ./render/modelviewer/avautils.ts ***!
  \****************************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   buildVertexBuffer: () => (/* binding */ buildVertexBuffer),
/* harmony export */   modelAttributeGetter: () => (/* binding */ modelAttributeGetter),
/* harmony export */   namedVartype: () => (/* binding */ namedVartype),
/* harmony export */   vartypes: () => (/* binding */ vartypes)
/* harmony export */ });
function modelAttributeGetter(attr, buffer) {
    let typemeta = vartypes[attr.gltype];
    let view = new typemeta.constr(buffer, attr.byteoffset);
    let stridestep = attr.bytestride / typemeta.size;
    return (v, vi) => {
        return view[v * stridestep + vi];
    };
}
var vartypes = {
    0x1400: { id: 0x1400, readfn: "getInt8", writefn: "setInt8", typeid: "i8", size: 1, constr: Int8Array, webgl: true },
    0x1401: { id: 0x1401, readfn: "getUint8", writefn: "setUint8", typeid: "u8", size: 1, constr: Uint8Array, webgl: true },
    0x1402: { id: 0x1402, readfn: "getInt16", writefn: "setInt16", typeid: "i16", size: 2, constr: Int16Array, webgl: true },
    0x1403: { id: 0x1403, readfn: "getUint16", writefn: "setUint16", typeid: "u16", size: 2, constr: Uint16Array, webgl: true },
    0x1404: { id: 0x1404, readfn: "getInt32", writefn: "setInt32", typeid: "i32", size: 4, constr: Int32Array, webgl: false },
    0x1405: { id: 0x1405, readfn: "getUint32", writefn: "setUint32", typeid: "u32", size: 4, constr: Uint32Array, webgl: false },
    0x1406: { id: 0x1406, readfn: "getFloat32", writefn: "setFloat32", typeid: "f32", size: 4, constr: Float32Array, webgl: true },
    0x140a: { id: 0x140a, readfn: "getFloat64", writefn: "setFloat64", typeid: "f64", size: 8, constr: Float64Array, webgl: false },
    0x140b: { id: 0x140b, readfn: "getFloat16", writefn: "setFloat16", typeid: "f16", size: 2, constr: null, webgl: false }, //FUCK why
};
function namedVartype(name) {
    for (var a in vartypes) {
        if (vartypes[a].typeid == name) {
            return vartypes[a];
        }
    }
    throw new Error("var type " + name + " not found");
}
function buildVertexBuffer(inputs) {
    var sorted = inputs.sort((a, b) => namedVartype(a.type).size * b.arrlength - namedVartype(b.type).size * a.arrlength);
    var elemcount = inputs[0].sources.reduce((a, v) => a + v.indices.length, 0);
    //figure out a layout, could still reorder and fill gaps with smaller types
    var layout = [];
    var byteoffset = 0;
    var inpmap = new Map();
    for (let inp of sorted) {
        var t = namedVartype(inp.type);
        byteoffset = Math.ceil(byteoffset / t.size) * t.size;
        var inpmeta = {
            gltype: t.id,
            byteoffset: byteoffset,
            name: inp.name,
            normalize: !!inp.normalize,
            veclength: inp.arrlength,
            bytestride: 0,
            min: [],
            max: [],
        };
        for (let i = 0; i < inp.arrlength; i++) {
            inpmeta.max[i] = -Infinity;
            inpmeta.min[i] = Infinity;
        }
        layout.push(inpmeta);
        inpmap.set(inpmeta, inp);
        byteoffset += t.size * inp.arrlength;
    }
    var inp0 = inpmap.get(layout[0]).sources;
    var indexmap = new Map();
    var indexcount = 0;
    var indices = new Uint16Array(elemcount);
    var entrycount = 0;
    for (let a = 0; a < inp0.length; a++) {
        var src = inp0[a];
        var map = new Map();
        indexmap.set(a, map);
        for (let b = 0; b < src.indices.length; b++) {
            let oldindex = src.indices[b];
            if (!map.has(oldindex)) {
                indices[entrycount++] = indexcount;
                map.set(oldindex, indexcount++);
            }
            else {
                indices[entrycount++] = map.get(oldindex);
            }
        }
    }
    //copy data from input arraybuffer
    var stride = Math.ceil(byteoffset / 4) * 4; //always pad to 32bit
    var buffer = new ArrayBuffer(stride * indexcount);
    for (var meta of layout) {
        meta.bytestride = stride;
        var inp = inpmap.get(meta);
        for (let srci = 0; srci < inp.sources.length; srci++) {
            var source = inp.sources[srci];
            var map = indexmap.get(srci);
            var t = namedVartype(inp.type);
            var view = new t.constr(buffer);
            for (let oldindex of map.keys()) {
                var newindex = map.get(oldindex);
                for (let b = 0; b < inp.arrlength; b++) {
                    let v = source.getter(oldindex, b);
                    view[(newindex * stride + meta.byteoffset) / t.size + b] = v;
                    meta.max[b] = Math.max(meta.max[b], v);
                    meta.min[b] = Math.min(meta.min[b], v);
                }
            }
        }
    }
    return {
        indices,
        buffer: new Uint8Array(buffer),
        stride,
        layout,
        vertexcount: indexcount
    };
}


/***/ },

/***/ "./render/renderprogram.ts"
/*!*********************************!*\
  !*** ./render/renderprogram.ts ***!
  \*********************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   decodeUniformBuffer: () => (/* binding */ decodeUniformBuffer),
/* harmony export */   generateMeshMeta: () => (/* binding */ generateMeshMeta),
/* harmony export */   getProgramMeta: () => (/* binding */ getProgramMeta),
/* harmony export */   getRenderFunc: () => (/* binding */ getRenderFunc),
/* harmony export */   getUniformValue: () => (/* binding */ getUniformValue),
/* harmony export */   renderToSprite: () => (/* binding */ renderToSprite)
/* harmony export */ });
/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! three */ "../node_modules/three/build/three.core.js");
/* harmony import */ var _util_halffloat__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../util/halffloat */ "./util/halffloat.ts");
/* harmony import */ var _modelviewer_avautils__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./modelviewer/avautils */ "./render/modelviewer/avautils.ts");
/* harmony import */ var _util_crc32__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../util/crc32 */ "./util/crc32.ts");




const spriteShaderFragment = (__webpack_require__(/*! !raw-loader!./shaders/sprite_fragment.glsl */ "../node_modules/raw-loader/dist/cjs.js!./render/shaders/sprite_fragment.glsl")["default"]);
const spriteShaderVertex = (__webpack_require__(/*! !raw-loader!./shaders/sprite_vertex.glsl */ "../node_modules/raw-loader/dist/cjs.js!./render/shaders/sprite_vertex.glsl")["default"]);
var cachedPrograms = new WeakMap();
var vertexPosAliases = ["aVertexPosition_BoneLabel", "aWaterPosition_Depth", "aVertexPosition2D", "aVertexPosition"];
function getProgramMeta(prog) {
    if (cachedPrograms.has(prog)) {
        return cachedPrograms.get(prog);
    }
    var r = fetchProgramMeta(prog);
    cachedPrograms.set(prog, r);
    return r;
}
function fetchProgramMeta(prog) {
    var fragdefines = [];
    var vertdefines = [];
    var reg = /^#define\s+(\w+)\s*$/gm;
    let m;
    while (m = reg.exec(prog.fragmentShader.source)) {
        fragdefines.push(m[1]);
    }
    while (m = reg.exec(prog.vertexShader.source)) {
        vertdefines.push(m[1]);
    }
    let isTinted = !!prog.fragmentShader.source.match(/\bgl_FragColor\s*=\s*uTint\b/);
    let isUiScaler = !!prog.fragmentShader.source.match(/\bLanczosShaderConsts\b/);
    let uTint = prog.uniforms.find(q => q.name == "uTint");
    let uHighlightScale = prog.uniforms.find(q => q.name == "uHighlightScale");
    let uBoneTransforms = prog.uniforms.find(q => q.name == "uBoneTransforms[0]");
    let uViewMatrix = prog.uniforms.find(q => q.name == "uViewMatrix");
    let uProjectionMatrix = prog.uniforms.find(q => q.name == "uProjectionMatrix");
    let uSrcUVRegion = prog.uniforms.find(q => q.name == "uSrcUVRegion");
    let uColourRemapWeightings = prog.uniforms.find(q => q.name == "uColourRemapWeightings");
    let aTexUV = prog.inputs.find(q => q.name == "aTextureUV");
    let aVertexNormal = prog.inputs.find(q => q.name == "aVertexNormal_BatchFlags");
    let aParticleSize = prog.inputs.find(q => q.name == "aBillboardSize" || q.name == "aParticleSize");
    let aMaterialSettingsSlotXY3 = prog.inputs.find(q => q.name == "aMaterialSettingsSlotXY3");
    let aVertexPosition2D = prog.inputs.find(q => q.name == "aVertexPosition2D");
    let aPos = prog.inputs.find(i => vertexPosAliases.indexOf(i.name) != -1);
    let isLighted = fragdefines.includes("AMBIENT_LIGHTING") || fragdefines.includes("DIFFUSE_LIGHTING") || fragdefines.includes("ALBEDO_LIGHTING");
    let isShadowRender = vertdefines.includes("MODEL_GEOMETRY_SHADOW_VS");
    return {
        uModelMatrix: prog.uniforms.find(q => q.name == "uModelMatrix"),
        uBones: uBoneTransforms,
        uTint: uTint,
        uHighlightScale: uHighlightScale,
        uViewMatrix: uViewMatrix,
        uProjectionMatrix: uProjectionMatrix,
        aPos: aPos,
        aColor: prog.inputs.find(q => q.name == "aVertexColour"),
        aTexUV: aTexUV,
        aTexMetaLookup: prog.inputs.find(q => q.name == "aMaterialSettingsSlotXY_TilePositionXZ"),
        aBones: prog.inputs.find(q => q.name == "aVertexPosition_BoneLabel"),
        aSkinbones: prog.inputs.find(q => q.name == "aVertexSkinBones"),
        aVertexNormal_BatchFlags: aVertexNormal,
        aParticleSize: aParticleSize,
        isPostProcess: !!uColourRemapWeightings,
        isCompute: prog.computeShader.source.length != 0,
        isFloor: !!aMaterialSettingsSlotXY3,
        isFloorWater: !!aPos && aPos.name == "aWaterPosition_Depth",
        isAnimated: !!uBoneTransforms,
        isUi: !!aVertexPosition2D,
        isUiScaler: isUiScaler,
        isUiGameCopy: !!uSrcUVRegion,
        isParticles: !!aParticleSize,
        isLighted,
        isShadowRender,
        isTinted,
        isMainMesh: isLighted && !aVertexPosition2D && !aParticleSize,
        raw: prog,
        fragdefines,
        vertdefines,
        flags: {
            vertexcolor: fragdefines.includes("VERTEX_COLOUR"),
            texalpha: fragdefines.includes("TEXTURE_ALPHA_USAGE"),
            alpha: fragdefines.includes("ALPHA_ENABLED"),
            forceOpaque: fragdefines.includes("FORCE_OPAQUE"),
            albedoTexture: !!aTexUV,
            //TODO rt7 materials
            metalTexture: false,
            roughnessTexture: false
        }
    };
}
function getRenderFunc(json) {
    var program = getProgramMeta(json.program);
    var uniforms = decodeUniformBuffer(json.uniformState, program);
    var ntriangles = 0;
    for (let a = 0; a < json.renderRanges.length; a++) {
        if (json.renderMode == "triangles") {
            ntriangles += json.renderRanges[a].length / 3;
        }
        else {
            ntriangles += json.renderRanges[a].length - 2;
        }
    }
    var indices = new Uint32Array(ntriangles * 3);
    var nvertices = json.vertexArray.attributes[0].buffer.byteLength / json.vertexArray.attributes[0].stride;
    if (!json.vertexArray.indexBuffer || json.vertexArray.indexBuffer.byteLength == 0) {
        var id = 0;
        var offset = 0;
        //TODO don't know length in this mode, currently not working
        for (let a = 0; a < json.renderRanges.length; a++) {
            for (let b = 0; b < json.renderRanges[a].length;) {
                if (json.renderMode == "strips" && b != 0) {
                    id -= 2;
                    b -= 2;
                }
                indices[offset++] = id++;
                indices[offset++] = id++;
                indices[offset++] = id++;
                b += 3;
            }
        }
    }
    else {
        //convert and normalize vertex attributes
        var buf = json.vertexArray.indexBuffer;
        var indextype = _modelviewer_avautils__WEBPACK_IMPORTED_MODULE_2__.vartypes[json.indexType];
        var bufview = new indextype.constr(buf.buffer, buf.byteOffset, buf.byteLength / indextype.size);
        var indexsize = indextype.size;
        var c = 0;
        for (let a = 0; a < json.renderRanges.length; a++) {
            let b = 0;
            let ptr = json.renderRanges[a].start / indexsize;
            while (b < json.renderRanges[a].length) {
                if (json.renderMode == "strips" && b != 0) {
                    b -= 2;
                }
                indices[c++] = bufview[ptr + b++];
                indices[c++] = bufview[ptr + b++];
                indices[c++] = bufview[ptr + b++];
            }
        }
    }
    var getters = {};
    for (var a in program.raw.inputs) {
        var proginp = program.raw.inputs[a];
        var inp = json.vertexArray.attributes[proginp.location];
        if (inp) {
            // TODO this would be way more efficient with getAttributeView
            let buf = new DataView(inp.buffer.buffer, inp.buffer.byteOffset, inp.buffer.byteLength);
            var type = _modelviewer_avautils__WEBPACK_IMPORTED_MODULE_2__.vartypes[inp.scalartype];
            getters[proginp.name] = (function (inp, buf, type) {
                return function (i, vi) {
                    if (vi < inp.vectorlength) {
                        return buf[type.readfn](inp.offset + i * inp.stride + vi * type.size, true);
                    }
                    return (vi == 3 ? 1 : 1);
                };
            })(inp, buf, type);
        }
        else {
            getters[proginp.name] = () => 0;
        }
    }
    var keyframes = [{ time: 0, uniforms }];
    var utexatlasmeta = program.raw.uniforms.find(u => u.name == "uTextureAtlasSettings");
    var utexatlas = program.raw.uniforms.find(u => u.name == "uTextureAtlas");
    var texture = null;
    if (utexatlasmeta && utexatlas) {
        let atlas = json.samplers[uniforms[utexatlas.name][0][0]];
        let atlasmeta = json.samplers[uniforms[utexatlasmeta.name][0][0]];
        if (atlas && atlasmeta) {
            texture = { atlas, atlasmeta };
        }
    }
    return { uniforms, nvertices, raw: json, indices, getters, progmeta: program, keyframes, texture };
}
function decodeUniformBuffer(snap, program) {
    var uniforms = {};
    for (let uni of program.raw.uniforms) {
        if (!uni.snapshotTracked) {
            continue;
        }
        uniforms[uni.name] = getUniformValue(snap, uni);
    }
    return uniforms;
}
function getUniformValue(snap, uni) {
    var t = _modelviewer_avautils__WEBPACK_IMPORTED_MODULE_2__.vartypes[uni.type.scalarType];
    var v = [];
    var unireader = new DataView(snap.buffer, snap.byteOffset, snap.byteLength);
    for (let a = 0; a < uni.length; a++) {
        var sub = [];
        v.push(sub);
        for (let b = 0; b < uni.type.vectorLength; b++) {
            sub.push(unireader[t.readfn](uni.snapshotOffset + uni.type.vectorLength * uni.type.scalarSize * a + uni.type.scalarSize * b, true));
        }
    }
    return v;
}
var cachedSpriteRenderContext = null;
function getAttributeView(attr) {
    const type = _modelviewer_avautils__WEBPACK_IMPORTED_MODULE_2__.vartypes[attr.scalartype];
    const stride = Math.floor(attr.stride / type.size);
    const len = Math.floor(attr.buffer.byteLength / attr.stride);
    const view = new type.constr(attr.buffer.buffer, attr.buffer.byteOffset + attr.offset, len * stride - attr.offset);
    return [view, len, stride];
}
function generateMeshMeta(render, progmeta) {
    let posinput = render.vertexArray.attributes[progmeta.aPos.location];
    let [view, len, stride] = getAttributeView(posinput);
    let hash = new _util_crc32__WEBPACK_IMPORTED_MODULE_3__.CrcBuilder();
    let sumx = 0, sumy = 0, sumz = 0;
    for (let i = 0; i < len; i++) {
        sumx += view[i * stride + 0];
        sumy += view[i * stride + 1];
        sumz += view[i * stride + 2];
        hash.addUint16(view[i * stride + 0]);
        hash.addUint16(view[i * stride + 1]);
        hash.addUint16(view[i * stride + 2]);
    }
    let boneids = [];
    if (progmeta.aSkinbones) {
        let boneinput = render.vertexArray.attributes[progmeta.aSkinbones.location];
        let [boneview, bonelen, bonestride] = getAttributeView(boneinput);
        let boneset = new Set();
        for (let i = 0; i < bonelen; i++) {
            boneset.add(boneview[i * bonestride + 0]);
            boneset.add(boneview[i * bonestride + 1]);
            boneset.add(boneview[i * bonestride + 2]);
            boneset.add(boneview[i * bonestride + 3]);
        }
        boneids = [...boneset];
    }
    else if (progmeta.aBones) {
        if (posinput.vectorlength != 4) {
            throw new Error("expecting bone to be packed in lo(pos[3])");
        }
        let boneset = new Set();
        for (let i = 0; i < len; i++) {
            boneset.add(view[i * stride + 3] % 256);
        }
        boneids = [...boneset];
    }
    let r = {
        vertexcount: len,
        vertexcenter: [sumx / len, sumy / len, sumz / len],
        posbufferhash: hash.get(),
        usedbones: boneids
    };
    return r;
}
async function renderToSprite(renders, pxperunit, camera = "topdown") {
    const stride = 6;
    let meshes = [];
    let max = [-Infinity, -Infinity, -Infinity];
    let min = [Infinity, Infinity, Infinity];
    for (let render of renders) {
        let buf = new Float32Array(stride * render.nvertices);
        let colget = (render.progmeta.aColor ? render.getters[render.progmeta.aColor.name] : ((i, vi) => vi == 3 ? 255 : 128));
        let posget = render.getters[render.progmeta.aPos.name]; //TODO could be null
        for (let a = 0; a < render.nvertices; a++) {
            let base = a * stride;
            buf[base + 0] = posget(a, 0);
            buf[base + 1] = posget(a, 1);
            buf[base + 2] = posget(a, 2);
            buf[base + 3] = colget(a, 0) / 255;
            buf[base + 4] = colget(a, 1) / 255;
            buf[base + 5] = colget(a, 2) / 255;
            max[0] = Math.max(max[0], buf[base + 0]);
            max[1] = Math.max(max[1], buf[base + 1]);
            max[2] = Math.max(max[2], buf[base + 2]);
            min[0] = Math.min(min[0], buf[base + 0]);
            min[1] = Math.min(min[1], buf[base + 1]);
            min[2] = Math.min(min[2], buf[base + 2]);
        }
        let indices = render.indices;
        meshes.push({ buf, indices });
    }
    let dx = Math.floor(min[0] * pxperunit);
    let dy = Math.floor(min[2] * pxperunit);
    let xsize = Math.ceil(Math.max(2, (max[0] - min[0]) * pxperunit));
    let ysize = Math.ceil(Math.max(2, (max[1] - min[1]) * pxperunit));
    let zsize = Math.ceil(Math.max(2, (max[2] - min[2]) * pxperunit));
    if (!cachedSpriteRenderContext) {
        let canvas = document.createElement("canvas");
        let gl = canvas.getContext("webgl2");
        let frag = gl.createShader(gl.FRAGMENT_SHADER);
        gl.shaderSource(frag, spriteShaderFragment);
        gl.compileShader(frag);
        let errfrag = gl.getShaderInfoLog(frag);
        if (errfrag) {
            throw new Error(errfrag);
        }
        let vert = gl.createShader(gl.VERTEX_SHADER);
        gl.shaderSource(vert, spriteShaderVertex);
        gl.compileShader(vert);
        let errvert = gl.getShaderInfoLog(vert);
        if (errvert) {
            throw new Error(errvert);
        }
        let shaderProgram = gl.createProgram();
        gl.attachShader(shaderProgram, vert);
        gl.attachShader(shaderProgram, frag);
        gl.linkProgram(shaderProgram);
        gl.useProgram(shaderProgram);
        let aPos = gl.getAttribLocation(shaderProgram, "aPos");
        let aCol = gl.getAttribLocation(shaderProgram, "aCol");
        let uTransform = gl.getUniformLocation(shaderProgram, "uTransform");
        gl.enableVertexAttribArray(aPos);
        gl.enableVertexAttribArray(aCol);
        cachedSpriteRenderContext = { gl, canvas, uTransform, aPos, aCol };
    }
    let { canvas, gl, uTransform, aPos, aCol } = cachedSpriteRenderContext;
    let depthscale = 1 / Math.max(max[1], -min[1]);
    let modeltoviewx = pxperunit / xsize * 2;
    let modeltoviewy = pxperunit / ysize * 2;
    let modeltoviewz = pxperunit / zsize * 2;
    let view;
    let width;
    let height;
    if (camera == "topdown") {
        view = Float32Array.from([
            modeltoviewx, 0, 0, -min[0] * modeltoviewx - 1,
            0, 0, modeltoviewz, -min[2] * modeltoviewz - 1,
            0, -depthscale, 0, 0,
            0, 0, 0, 1
        ]);
        width = xsize;
        height = zsize;
    }
    else if (camera == "front") {
        view = Float32Array.from([
            modeltoviewx, 0, 0, -min[0] * modeltoviewx - 1,
            0, -modeltoviewy, 0, max[1] * modeltoviewy - 1,
            0, 0, depthscale, 0,
            0, 0, 0, 1
        ]);
        width = xsize;
        height = ysize;
    }
    else if (camera == "side") {
        view = Float32Array.from([
            0, 0, -modeltoviewz, max[2] * modeltoviewz - 1,
            0, -modeltoviewy, 0, max[1] * modeltoviewy - 1,
            modeltoviewx, 0, 0, -min[0] * modeltoviewx - 1,
            0, 0, 0, 1
        ]);
        width = zsize;
        height = ysize;
    }
    else {
        throw new Error("unknown camera");
    }
    gl.uniformMatrix4fv(uTransform, true, view);
    canvas.width = width;
    canvas.height = height;
    gl.viewport(0, 0, width, height);
    gl.clearColor(0, 0, 0, 0);
    gl.clear(gl.COLOR_BUFFER_BIT | gl.DEPTH_BUFFER_BIT);
    gl.enable(gl.DEPTH_TEST);
    gl.depthFunc(gl.LESS);
    gl.disable(gl.CULL_FACE);
    gl.depthRange(-1000, 1000);
    for (let mesh of meshes) {
        let index = gl.createBuffer();
        gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER, index);
        gl.bufferData(gl.ELEMENT_ARRAY_BUFFER, mesh.indices, gl.STATIC_DRAW);
        let attr = gl.createBuffer();
        gl.bindBuffer(gl.ARRAY_BUFFER, attr);
        gl.bufferData(gl.ARRAY_BUFFER, mesh.buf, gl.STATIC_DRAW);
        gl.vertexAttribPointer(aPos, 3, gl.FLOAT, false, stride * mesh.buf.BYTES_PER_ELEMENT, 0);
        gl.vertexAttribPointer(aCol, 3, gl.FLOAT, false, stride * mesh.buf.BYTES_PER_ELEMENT, 3 * mesh.buf.BYTES_PER_ELEMENT);
        gl.drawElements(gl.TRIANGLES, mesh.indices.length, gl.UNSIGNED_INT, 0);
        gl.deleteBuffer(attr);
        gl.deleteBuffer(index);
    }
    //This also works but gives flipped image
    let img = new ImageData(canvas.width, canvas.height);
    gl.readPixels(0, 0, img.width, img.height, gl.RGBA, gl.UNSIGNED_BYTE, img.data, 0);
    let gpuimg = await createImageBitmap(canvas, { imageOrientation: "flipY" }); //wait why is this flipped once more?
    // let imgspace = mat3.create();
    // mat3.scale(imgspace, imgspace, [1 / pxperunit, 1 / pxperunit]);
    // mat3.translate(imgspace, imgspace, [dx, dy]);
    let imgspace = new three__WEBPACK_IMPORTED_MODULE_0__.Matrix3()
        .translate(dx, dy)
        .scale(1 / pxperunit, 1 / pxperunit);
    //img.show();
    // let cnv2d = document.createElement("canvas");
    // cnv2d.width = width;
    // cnv2d.height = height;
    // let ctx2d = cnv2d.getContext("2d");
    // ctx2d.drawImage(canvas, 0, 0);
    // let img = ctx2d.getImageData(0, 0, width, height);
    // let gpuimg = await createImageBitmap(img);
    // let imgspace = mat3.create();
    // mat3.scale(imgspace, imgspace, [1 / pxperunit, 1 / pxperunit]);
    // mat3.translate(imgspace, imgspace, [dx, dy]);
    // mat3.scale(imgspace, imgspace, [1, -1]);
    // mat3.translate(imgspace, imgspace, [0, -height]);
    let r = { imgspace, gpuimg, img };
    return r;
}


/***/ },

/***/ "./util/crc32.ts"
/*!***********************!*\
  !*** ./util/crc32.ts ***!
  \***********************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   CrcBuilder: () => (/* binding */ CrcBuilder),
/* harmony export */   crc32: () => (/* binding */ crc32),
/* harmony export */   crc32Interlaced: () => (/* binding */ crc32Interlaced),
/* harmony export */   crc32addInt: () => (/* binding */ crc32addInt),
/* harmony export */   intbuffer: () => (/* binding */ intbuffer)
/* harmony export */ });
// currently copied from rsmv
// might have to move this to alt1 libs
//based on https://blog.stalkr.net/2011/03/crc-32-forging.html
//also check out https://stackoverflow.com/questions/1514040/reversing-crc32
// Poly in "reversed" notation -- http://en.wikipedia.org/wiki/Cyclic_redundancy_check
const POLY = 0xedb88320; // CRC-32-IEEE 802.3
//POLY = 0x82F63B78 # CRC-32C (Castagnoli)
//POLY = 0xEB31D82E # CRC-32K (Koopman)
//POLY = 0xD5828281 # CRC-32Q
const crc32_table = new Uint32Array(256);
const crc32_reverse = new Uint32Array(256);
function build_crc_tables() {
    for (let i = 0; i < 256; i++) {
        let fwd = i;
        let rev = i << 24;
        for (let j = 8; j > 0; j--) {
            // build normal table
            if ((fwd & 1) == 1) {
                fwd = (fwd >>> 1) ^ POLY;
            }
            else {
                fwd >>>= 1;
            }
            //build reverse table =)
            if ((rev & 0x80000000) != 0) {
                rev = ((rev ^ POLY) << 1) | 1;
            }
            else {
                rev <<= 1;
            }
            rev &= 0xffffffff;
        }
        crc32_table[i] = fwd & 0xffffffff;
        crc32_reverse[i] = rev;
    }
}
build_crc_tables();
function crc32(buf, crc = 0, rangeStart = 0, rangeEnd = buf.length) {
    crc = crc ^ 0xffffffff;
    for (let i = rangeStart; i < rangeEnd; i++) {
        crc = (crc >>> 8) ^ crc32_table[(crc ^ buf[i]) & 0xff];
    }
    return (crc ^ 0xffffffff) >>> 0;
}
const staticintbuf = new Uint32Array(1);
const staticintbuf8 = new Uint8Array(staticintbuf.buffer);
function intbuffer(value) {
    staticintbuf[0] = (value >>> 0);
    return staticintbuf8;
}
function crc32addInt(int, crc) {
    staticintbuf[0] = (int >>> 0);
    return crc32(staticintbuf8, crc);
}
class CrcBuilder {
    constructor(initcrc = 0) {
        this.crc = initcrc ^ 0xffffffff;
    }
    addbyte(byte) {
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ (byte & 0xff)) & 0xff];
    }
    addUint16Flipped(u16) {
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ ((u16 >> 16) & 0xff)) & 0xff];
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ (u16 & 0xff)) & 0xff];
    }
    addUint16(u16) {
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ (u16 & 0xff)) & 0xff];
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ ((u16 >> 16) & 0xff)) & 0xff];
    }
    addUint32(u16) {
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ ((u16 >> 0) & 0xff)) & 0xff];
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ ((u16 >> 16) & 0xff)) & 0xff];
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ ((u16 >> 24) & 0xff)) & 0xff];
        this.crc = (this.crc >>> 8) ^ crc32_table[(this.crc ^ ((u16 >> 32) & 0xff)) & 0xff];
    }
    get() {
        return (this.crc ^ 0xffffffff) >>> 0;
    }
    fork() {
        return new CrcBuilder(this.get());
    }
}
/**
 * Used to hash parts of an interlaced buffer
 * @param buf the interlaced buffer
 * @param offset offset of byte group
 * @param stride number of bytes between the start of each occurance of the byte group
 * @param bytes number of bytes per group
 * @param chunkcount number of groups
 * @param crc optional starting value for crc
 */
function crc32Interlaced(buf, offset, stride, bytes, chunkcount, crc = 0) {
    crc = crc ^ 0xffffffff;
    for (let i = 0; i < chunkcount; i++) {
        let base = offset + i * stride;
        for (let b = 0; b < bytes; b++) {
            let index = base + b;
            crc = (crc >>> 8) ^ crc32_table[(crc ^ buf[index]) & 0xff];
        }
    }
    return (crc ^ 0xffffffff) >>> 0;
}


/***/ },

/***/ "./util/halffloat.ts"
/*!***************************!*\
  !*** ./util/halffloat.ts ***!
  \***************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   convertToNumber: () => (/* binding */ convertToNumber),
/* harmony export */   roundToFloat16Bits: () => (/* binding */ roundToFloat16Bits)
/* harmony export */ });
DataView.prototype.getFloat16 = function (offset, le) {
    return convertToNumber(this.getUint16(offset, le));
};
//from https://github.com/petamoriken/float16/blob/master/src/lib.js
// algorithm: ftp://ftp.fox-toolkit.org/pub/fasthalffloatconversion.pdf
const buffer = new ArrayBuffer(4);
const floatView = new Float32Array(buffer);
const uint32View = new Uint32Array(buffer);
const baseTable = new Uint32Array(512);
const shiftTable = new Uint32Array(512);
for (let i = 0; i < 256; ++i) {
    const e = i - 127;
    // very small number (0, -0)
    if (e < -27) {
        baseTable[i | 0x000] = 0x0000;
        baseTable[i | 0x100] = 0x8000;
        shiftTable[i | 0x000] = 24;
        shiftTable[i | 0x100] = 24;
        // small number (denorm)
    }
    else if (e < -14) {
        baseTable[i | 0x000] = 0x0400 >> (-e - 14);
        baseTable[i | 0x100] = (0x0400 >> (-e - 14)) | 0x8000;
        shiftTable[i | 0x000] = -e - 1;
        shiftTable[i | 0x100] = -e - 1;
        // normal number
    }
    else if (e <= 15) {
        baseTable[i | 0x000] = (e + 15) << 10;
        baseTable[i | 0x100] = ((e + 15) << 10) | 0x8000;
        shiftTable[i | 0x000] = 13;
        shiftTable[i | 0x100] = 13;
        // large number (Infinity, -Infinity)
    }
    else if (e < 128) {
        baseTable[i | 0x000] = 0x7c00;
        baseTable[i | 0x100] = 0xfc00;
        shiftTable[i | 0x000] = 24;
        shiftTable[i | 0x100] = 24;
        // stay (NaN, Infinity, -Infinity)
    }
    else {
        baseTable[i | 0x000] = 0x7c00;
        baseTable[i | 0x100] = 0xfc00;
        shiftTable[i | 0x000] = 13;
        shiftTable[i | 0x100] = 13;
    }
}
/**
 * round a number to a half float number bits.
 * @param {number} num
 */
function roundToFloat16Bits(num) {
    floatView[0] = num;
    const f = uint32View[0];
    const e = (f >> 23) & 0x1ff;
    return baseTable[e] + ((f & 0x007fffff) >> shiftTable[e]);
}
const mantissaTable = new Uint32Array(2048);
const exponentTable = new Uint32Array(64);
const offsetTable = new Uint32Array(64);
mantissaTable[0] = 0;
for (let i = 1; i < 1024; ++i) {
    let m = i << 13; // zero pad mantissa bits
    let e = 0; // zero exponent
    // normalized
    while ((m & 0x00800000) === 0) {
        e -= 0x00800000; // decrement exponent
        m <<= 1;
    }
    m &= ~0x00800000; // clear leading 1 bit
    e += 0x38800000; // adjust bias
    mantissaTable[i] = m | e;
}
for (let i = 1024; i < 2048; ++i) {
    mantissaTable[i] = 0x38000000 + ((i - 1024) << 13);
}
exponentTable[0] = 0;
for (let i = 1; i < 31; ++i) {
    exponentTable[i] = i << 23;
}
exponentTable[31] = 0x47800000;
exponentTable[32] = 0x80000000;
for (let i = 33; i < 63; ++i) {
    exponentTable[i] = 0x80000000 + ((i - 32) << 23);
}
exponentTable[63] = 0xc7800000;
offsetTable[0] = 0;
for (let i = 1; i < 64; ++i) {
    if (i === 32) {
        offsetTable[i] = 0;
    }
    else {
        offsetTable[i] = 1024;
    }
}
/**
 * convert a half float number bits to a number.
 * @param {number} float16bits - half float number bits
 */
function convertToNumber(float16bits) {
    const m = float16bits >> 10;
    uint32View[0] = mantissaTable[offsetTable[m] + (float16bits & 0x3ff)] + exponentTable[m];
    return floatView[0];
}


/***/ },

/***/ "./util/patchrs_napi.ts"
/*!******************************!*\
  !*** ./util/patchrs_napi.ts ***!
  \******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   getProgramFlag: () => (/* binding */ getProgramFlag),
/* harmony export */   getVertexFlag: () => (/* binding */ getVertexFlag),
/* harmony export */   hookFirstClient: () => (/* binding */ hookFirstClient),
/* harmony export */   injectClient: () => (/* binding */ injectClient),
/* harmony export */   native: () => (/* binding */ native),
/* harmony export */   returnProgramFlags: () => (/* binding */ returnProgramFlags),
/* harmony export */   returnVertexFlags: () => (/* binding */ returnVertexFlags)
/* harmony export */ });
let nativeReleaseDir = "./build/release/";
let nativeDebugDir = "./build/debug/";
function resolvePath(...parts) {
    // const path = require("path") as typeof import("path");
    // return path.resolve(cwd, ...parts);
    // can't rely on path/fs modules here since we might be running in CEF
    let cwd = (typeof process != "undefined" && process.cwd ? process.cwd() : native.debug.getCurrentWorkingDirectory());
    return cwd + "/" + parts.join("/"); // nasty
}
var native = null;
function sequentialFilename(dir, dirfiles, template) {
    // can't actually read the dir since we don't know if we are in CEF, and the plugin might not be loaded yet
    let regex = new RegExp(`${template.replace("#", "(\\d+)")}$`);
    let maxnum = 0;
    for (let file of dirfiles) {
        let m = file.match(regex);
        if (m) {
            maxnum = Math.max(maxnum, +m[1]);
        }
    }
    return [resolvePath(dir, template.replace("#", "" + (maxnum + 1))), (maxnum == 0 ? "" : resolvePath(dir, template.replace("#", "" + maxnum)))];
}
//TODO does not fix shared memory state
function reloadDll() {
    const fs = __webpack_require__(/*! fs */ "?74e4");
    let debugstat = null;
    let releasestat = null;
    try {
        debugstat = fs.statSync(resolvePath(nativeDebugDir, "addon.node"));
    }
    catch (e) { }
    try {
        releasestat = fs.statSync(resolvePath(nativeReleaseDir, "addon.node"));
    }
    catch (e) { }
    let pluginDir = "";
    if (debugstat && (!releasestat || debugstat.mtimeMs > releasestat.mtimeMs)) {
        console.log("using debug plugin");
        pluginDir = nativeDebugDir;
    }
    else if (releasestat) {
        console.log("using release plugin");
        pluginDir = nativeReleaseDir;
    }
    else {
        throw new Error("No native plugin found");
    }
    let origfile = resolvePath(pluginDir, "addon.node");
    let [newfile, lastfile] = sequentialFilename(pluginDir, fs.readdirSync(pluginDir), "addon-#.node");
    fs.copyFileSync(origfile, newfile);
    native = require(newfile);
}
// Try to load native addon - check globalThis.alt1gl first (set by launcher preload)
// With contextIsolation, _alt1gl (contextBridge proxy) is available immediately during
// module init, but alt1gl (mutable wrapper with async method patching) is created later
// by the renderer-world shim. Use a Proxy that always reads from the latest available API.
try {
    if (typeof globalThis !== "undefined" && (globalThis.alt1gl || globalThis._alt1gl)) {
        console.log("[patchrs_napi] Using global alt1gl API (bridge:", !!globalThis._alt1gl, "shim:", !!globalThis.alt1gl, ")");
        native = new Proxy({}, {
            get(_target, prop) {
                const api = globalThis.alt1gl || globalThis._alt1gl;
                if (!api)
                    throw new Error('alt1gl API not available');
                return api[prop];
            }
        });
    }
    else if (typeof require != "undefined") {
        console.log("[patchrs_napi] Attempting to load native addon...");
        reloadDll();
    }
    else {
        console.log("[patchrs_napi] Browser mode - native addon not available");
    }
}
catch (e) {
    console.error("[patchrs_napi] Failed to load native addon:", e);
}
if (native && native.debug && typeof native.debug.setLogCb === 'function')
    native.debug.setLogCb(e => {
        let m = e.match(/bufferdata (\d+)\->(\d+)/);
        if (m) {
            let dif = +m[1] - +m[2];
            if (dif > 1e6) {
                //console.log("large alloc: " + dif);
            }
        }
        else {
            //console.info(e)
        }
    });
function hookFirstClient() {
    // When running inside the launcher, native is already connected via globalThis.alt1gl/_alt1gl
    // No need to find PIDs or inject DLLs - the launcher handles all of that
    if (typeof globalThis !== "undefined" && (globalThis.alt1gl || globalThis._alt1gl)) {
        console.log("[patchrs_napi] Running inside launcher, skipping injection");
        return;
    }
    var pids = native.debug.getExePids("rs2client.exe");
    if (pids.length == 0) {
        console.log("no rs pid found");
        return;
    }
    // slightly sketchy, on intel iGPU the client forks the process and the first pid just happens to be correct
    let hook = injectClient(pids[0]);
    if (!hook.details) {
        console.log("injectdll returned false");
    }
}
function injectClient(pid) {
    let debugstat = null;
    let releasestat = null;
    // Check globalThis.alt1glNativeDir which is set by the launcher preload
    let globalNativeDir = typeof globalThis !== 'undefined' ? globalThis.alt1glNativeDir : null;
    try {
        debugstat = native.debug.statSync(resolvePath(nativeDebugDir, "addon.node"));
    }
    catch (e) { }
    try {
        releasestat = native.debug.statSync(resolvePath(nativeReleaseDir, "addon.node"));
    }
    catch (e) { }
    // Also check the global native dir if local paths don't work
    let globalstat = null;
    if (globalNativeDir) {
        try {
            globalstat = native.debug.statSync(resolvePath(globalNativeDir, "addon.node"));
        }
        catch (e) { }
    }
    let nativeDir = "";
    if (debugstat && (!releasestat || debugstat.modifiedTime > releasestat.modifiedTime)) {
        console.log("using debug gl native");
        nativeDir = nativeDebugDir;
    }
    else if (releasestat) {
        console.log("using release gl native");
        nativeDir = nativeReleaseDir;
    }
    else if (globalstat && globalNativeDir) {
        console.log("using global native dir:", globalNativeDir);
        nativeDir = globalNativeDir;
    }
    else {
        throw new Error("No native plugin found");
    }
    let origfile = resolvePath(nativeDir, "injected.dll");
    let [newfile, lastfile] = sequentialFilename(nativeDir, native.debug.readDirSync(nativeDir), "injected-#.dll");
    let needsnew;
    if (!lastfile) {
        needsnew = true;
    }
    else {
        let origfiledata = native.debug.readFileSync(origfile);
        let currentfiledata = native.debug.readFileSync(lastfile);
        needsnew = false;
        if (origfiledata.length != currentfiledata.length) {
            needsnew = true;
        }
        else {
            for (let i = 0; i < origfiledata.length; i++) {
                if (origfiledata[i] != currentfiledata[i]) {
                    needsnew = true;
                    break;
                }
            }
        }
    }
    let dllname;
    if (needsnew) {
        native.debug.copyFileSync(origfile, newfile);
        dllname = newfile;
    }
    else {
        dllname = lastfile;
    }
    console.log(dllname);
    let res = native.debug.injectDll(pid, dllname);
    let hook = { dllname, pid, details: res };
    return hook;
}
let vertexFlagCounter = new Array(32).fill(false);
function getVertexFlag() {
    let index = vertexFlagCounter.indexOf(false);
    if (index == -1) {
        throw new Error();
    }
    vertexFlagCounter[index] = true;
    return 1 << index;
}
function returnVertexFlags(flag) {
    for (let i = 0; i < 32; i++) {
        if (flag & (1 << i)) {
            vertexFlagCounter[i] = false;
        }
    }
}
let vertexProgCounter = new Array(32).fill(false);
function getProgramFlag() {
    let index = vertexProgCounter.indexOf(false);
    if (index == -1) {
        throw new Error();
    }
    vertexProgCounter[index] = true;
    return 1 << index;
}
function returnProgramFlags(flag) {
    for (let i = 0; i < 32; i++) {
        if (flag & (1 << i)) {
            vertexProgCounter[i] = false;
        }
    }
}


/***/ },

/***/ "./util/renderStream.ts"
/*!******************************!*\
  !*** ./util/renderStream.ts ***!
  \******************************/
(__unused_webpack_module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   renderStream: () => (/* binding */ renderStream)
/* harmony export */ });
/* harmony import */ var _render_renderprogram__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../render/renderprogram */ "./render/renderprogram.ts");
/**
 * Render stream helper that properly handles UI rendering at both 1080p and 4K resolutions.
 *
 * At 1080p: UI renders directly to framebuffer 0 (screen)
 * At 4K: UI renders to a separate framebuffer that gets scaled and copied to screen
 *
 * This helper detects the UI scaler and captures from the correct framebuffer.
 */

/**
 * Create a render stream that captures UI renders properly at any resolution.
 * Uses streamRenderCalls for proper uniform state capture, with 4K UI scaler detection.
 */
function renderStream(glapi, cb, options = {}) {
    const framecooldown = options.framecooldown ?? 600;
    const features = options.features ?? ["uniforms", "texturesnapshot"];
    let closed = false;
    let scalingtexture = 0;
    let mainStream = null;
    let uiStream = null;
    // Buffer to collect renders from both streams within a frame
    let pendingRenders = [];
    let lastCallbackTime = 0;
    const processRenders = (renders, source) => {
        if (closed)
            return;
        // Detect UI scaler for 4K mode
        for (const render of renders) {
            const prog = (0,_render_renderprogram__WEBPACK_IMPORTED_MODULE_0__.getProgramMeta)(render.program);
            if (prog.isUiScaler) {
                const samplers = Object.values(render.samplers);
                if (samplers.length > 0 && scalingtexture !== samplers[0].texid) {
                    scalingtexture = samplers[0].texid;
                    console.log(`[renderStream] Detected 4K UI scaler, texture ID: ${scalingtexture}`);
                    // Start UI framebuffer stream if we haven't already
                    if (!uiStream && scalingtexture > 0) {
                        uiStream = glapi.streamRenderCalls({
                            framecooldown,
                            features,
                            framebufferTexture: scalingtexture,
                        }, (uiRenders) => processRenders(uiRenders, "ui"));
                    }
                }
            }
        }
        // Add to pending renders
        pendingRenders.push(...renders);
        // Debounce callback to batch renders from same frame
        const now = Date.now();
        if (now - lastCallbackTime >= framecooldown / 2) {
            lastCallbackTime = now;
            if (pendingRenders.length > 0) {
                const toProcess = pendingRenders;
                pendingRenders = [];
                cb(toProcess);
            }
        }
    };
    // Start main stream (framebuffer 0)
    mainStream = glapi.streamRenderCalls({
        framecooldown,
        features,
        framebufferId: 0,
    }, (renders) => processRenders(renders, "main"));
    const res = {
        ended: (async () => {
            // Wait for main stream to end
            await mainStream?.ended;
            if (uiStream) {
                await uiStream.ended;
            }
        })(),
        close: async () => {
            closed = true;
            mainStream?.close();
            if (uiStream) {
                uiStream.close();
            }
        },
    };
    return res;
}


/***/ },

/***/ "../node_modules/raw-loader/dist/cjs.js!./render/shaders/sprite_fragment.glsl"
/*!************************************************************************************!*\
  !*** ../node_modules/raw-loader/dist/cjs.js!./render/shaders/sprite_fragment.glsl ***!
  \************************************************************************************/
(module) {

"use strict";
module.exports = "export default \"#version 300 es\\n\\nprecision mediump float;\\n\\nin vec3 vCol;\\n\\nout vec4 fragColor;\\n\\nvoid main(void) {\\n\\tfragColor = vec4(vCol,1);\\n}\";";

/***/ },

/***/ "../node_modules/raw-loader/dist/cjs.js!./render/shaders/sprite_vertex.glsl"
/*!**********************************************************************************!*\
  !*** ../node_modules/raw-loader/dist/cjs.js!./render/shaders/sprite_vertex.glsl ***!
  \**********************************************************************************/
(module) {

"use strict";
module.exports = "export default \"#version 300 es\\n\\nin vec3 aPos;\\nin vec3 aCol;\\n\\nuniform mat4 uTransform;\\n\\nout vec3 vCol;\\n\\nvoid main(void) {\\n\\tgl_Position = uTransform * vec4(aPos, 1.0);\\n\\n\\tvCol = aCol;\\n}\";";

/***/ },

/***/ "?74e4"
/*!********************!*\
  !*** fs (ignored) ***!
  \********************/
() {

/* (ignored) */

/***/ },

/***/ "./data/xpSources.json"
/*!*****************************!*\
  !*** ./data/xpSources.json ***!
  \*****************************/
(module) {

"use strict";
module.exports = /*#__PURE__*/JSON.parse('{"sources":[{"name":"Normal tree","xp":25,"skillId":214,"category":"woodcutting"},{"name":"Oak tree","xp":37.5,"skillId":214,"category":"woodcutting"},{"name":"Willow tree","xp":67.5,"skillId":214,"category":"woodcutting"},{"name":"Teak tree","xp":85,"skillId":214,"category":"woodcutting"},{"name":"Acadia tree","xp":80,"skillId":214,"category":"woodcutting"},{"name":"Maple tree","xp":100,"skillId":214,"category":"woodcutting"},{"name":"Mahogany tree","xp":125,"skillId":214,"category":"woodcutting"},{"name":"Yew tree","xp":187.5,"skillId":214,"category":"woodcutting"},{"name":"Crystal tree","xp":350,"skillId":214,"category":"woodcutting"},{"name":"Magic tree","xp":365,"skillId":214,"category":"woodcutting"},{"name":"Elder tree","xp":425,"skillId":214,"category":"woodcutting"},{"name":"Normal logs","xp":40,"skillId":213,"category":"firemaking"},{"name":"Oak logs","xp":60,"skillId":213,"category":"firemaking"},{"name":"Willow logs","xp":90,"skillId":213,"category":"firemaking"},{"name":"Teak logs","xp":105,"skillId":213,"category":"firemaking"},{"name":"Arctic pine logs","xp":125,"skillId":213,"category":"firemaking"},{"name":"Maple logs","xp":135.5,"skillId":213,"category":"firemaking"},{"name":"Acadia logs","xp":140,"skillId":213,"category":"firemaking"},{"name":"Mahogany logs","xp":157.5,"skillId":213,"category":"firemaking"},{"name":"Eucalyptus logs","xp":193.5,"skillId":213,"category":"firemaking"},{"name":"Yew logs","xp":202.5,"skillId":213,"category":"firemaking"},{"name":"Magic logs","xp":303.8,"skillId":213,"category":"firemaking"},{"name":"Elder logs","xp":450,"skillId":213,"category":"firemaking"},{"name":"Shrimp","xp":10,"skillId":211,"category":"fishing"},{"name":"Crayfish","xp":10,"skillId":211,"category":"fishing"},{"name":"Sardine","xp":20,"skillId":211,"category":"fishing"},{"name":"Herring","xp":30,"skillId":211,"category":"fishing"},{"name":"Anchovies","xp":40,"skillId":211,"category":"fishing"},{"name":"Trout","xp":50,"skillId":211,"category":"fishing"},{"name":"Pike","xp":60,"skillId":211,"category":"fishing"},{"name":"Salmon","xp":70,"skillId":211,"category":"fishing"},{"name":"Tuna","xp":80,"skillId":211,"category":"fishing"},{"name":"Lobster","xp":90,"skillId":211,"category":"fishing"},{"name":"Swordfish","xp":100,"skillId":211,"category":"fishing"},{"name":"Shark","xp":110,"skillId":211,"category":"fishing"},{"name":"Monkfish","xp":120,"skillId":211,"category":"fishing"},{"name":"Cavefish","xp":300,"skillId":211,"category":"fishing"},{"name":"Rocktail","xp":380,"skillId":211,"category":"fishing"},{"name":"Sailfish","xp":400,"skillId":211,"category":"fishing"},{"name":"Copper ore","xp":17.5,"skillId":209,"category":"mining"},{"name":"Tin ore","xp":17.5,"skillId":209,"category":"mining"},{"name":"Iron ore","xp":35,"skillId":209,"category":"mining"},{"name":"Coal","xp":40,"skillId":209,"category":"mining"},{"name":"Mithril ore","xp":55,"skillId":209,"category":"mining"},{"name":"Adamant ore","xp":70,"skillId":209,"category":"mining"},{"name":"Runite ore","xp":85,"skillId":209,"category":"mining"},{"name":"Impious ashes","xp":4,"skillId":201,"category":"prayer"},{"name":"Bones","xp":4.5,"skillId":201,"category":"prayer"},{"name":"Big bones","xp":15,"skillId":201,"category":"prayer"},{"name":"Baby dragon bones","xp":30,"skillId":201,"category":"prayer"},{"name":"Wyvern bones","xp":50,"skillId":201,"category":"prayer"},{"name":"Dragon bones","xp":72,"skillId":201,"category":"prayer"},{"name":"Dagannoth bones","xp":125,"skillId":201,"category":"prayer"},{"name":"Airut bones","xp":132.5,"skillId":201,"category":"prayer"},{"name":"Ourg bones","xp":140,"skillId":201,"category":"prayer"},{"name":"Frost dragon bones","xp":180,"skillId":201,"category":"prayer"},{"name":"Reinforced dragon bones","xp":190,"skillId":201,"category":"prayer"},{"name":"Shrimp (cook)","xp":30,"skillId":212,"category":"cooking"},{"name":"Sardine (cook)","xp":40,"skillId":212,"category":"cooking"},{"name":"Herring (cook)","xp":50,"skillId":212,"category":"cooking"},{"name":"Trout (cook)","xp":70,"skillId":212,"category":"cooking"},{"name":"Salmon (cook)","xp":90,"skillId":212,"category":"cooking"},{"name":"Tuna (cook)","xp":100,"skillId":212,"category":"cooking"},{"name":"Lobster (cook)","xp":120,"skillId":212,"category":"cooking"},{"name":"Swordfish (cook)","xp":140,"skillId":212,"category":"cooking"},{"name":"Monkfish (cook)","xp":150,"skillId":212,"category":"cooking"},{"name":"Shark (cook)","xp":210,"skillId":212,"category":"cooking"},{"name":"Rocktail (cook)","xp":225,"skillId":212,"category":"cooking"},{"name":"Sailfish (cook)","xp":270,"skillId":212,"category":"cooking"}]}');

/***/ }

},
/******/ __webpack_require__ => { // webpackRuntimeModules
/******/ var __webpack_exec__ = (moduleId) => (__webpack_require__(__webpack_require__.s = moduleId))
/******/ __webpack_require__.O(0, ["vendor-react","vendors"], () => (__webpack_exec__("./Entrance/index.tsx")));
/******/ var __webpack_exports__ = __webpack_require__.O();
/******/ }
]);
//# sourceMappingURL=main.bundle.js.map